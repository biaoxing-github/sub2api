# v0.1.156 可更新项评估

- 日期：2026-07-15
- 执行者：Devil
- 上游 tag：`v0.1.156`
- tag 对象：`9cc1b469a24e6f79aeec9401ad1f9534f9b98aec`
- tag 提交：`12f991dde8a58e183d4bd16a87ef6fd0df714757`
- 本地评估基线：`f33701edc1a40d9140b19d6c9f91de9101e3a5ce`

## 规模与边界

- `v0.1.155..v0.1.156` 包含 48 个 first-parent 变更组。
- 总差异为 253 个文件、31408 行新增、1710 行删除。
- 当前实际开发主线不包含独立 worktree `codex/merge-v0.1.155` 的 `f1ffeacc6f7d`；后续 `v0.1.155.1` 至 `v0.1.155.7` 从当前本地主线继续发布。因此本评估以 `f33701edc` 为合并基线。
- 主工作树原有 `.codegraph/daemon.pid` 修改及 `backend/cmd/codex-live-probe/`、`tmp_body.json` 未跟踪内容均未改动。

## 第一批：建议优先更新

以下五项已在一次性 detached worktree 中按上游顺序组合，且通过 `internal/repository`、`internal/handler` 编译、`internal/pkg/apicompat` 和 `internal/service` 测试：

1. `1f91b69ca`：GPT-5.6 长上下文账号成本统计按长上下文定价重新计算，避免统计金额偏低。
2. `65093591c`：OpenAI failover 切换日志增加代理 ID、名称、主机和端口，便于定位异常出口。
3. `8d13bbbea`：OAuth 图片输出按实际图片尺寸回填结果和 usage 统计，修复尺寸上报错误。
4. `7cb8ece8d`：内容会话 seed 扫描减少重复遍历，属于局部性能优化。
5. `65d744d0e`：Responses 转 Anthropic 的并行 `tool_use` 使用正确 block index，避免幽灵 `content_block_delta`。

这五项组合后验证结果：

- `go test ./internal/repository -count=1`：PASS。
- `go test ./internal/handler -run '^$' -count=1`：PASS。
- `go test ./internal/pkg/apicompat ./internal/service -count=1`：PASS。

## 第二批：值得更新，但需要本地适配

- `9676b19fb` 客户端断开感知 failover：价值最高，可避免客户端已断开后继续切号并误报 502；影响 `FailoverState` 及 Messages、Responses、Chat、Images、Embeddings、Grok 等入口，当前主线存在冲突，需作为独立功能组适配和回归。
- `2381b6c10`、`42d1bf298`、`cf6aa1a5c` OpenAI API-key 透传 5xx failover 与错误脱敏：本地没有上游同名 passthrough 文件，需映射到现有转发实现。
- `8e84071f2` OpenAI 原生 Responses 首输出超时：涉及 config、handler、gateway service 和 response handling，需保留本地 30 秒流量约束后手工接入。
- `9b8658590`、`40a59c9e8`、`4344f6afb` SSE JSON 边界、流式 flush 和图片 JSON 完成边界：协议价值高，但本地文件分层不同，应按行为移植而不是整组 cherry-pick。
- `4de53b61b`、`dba6cefb6` Read 工具参数终结和 Anthropic incomplete 终结：改动小但当前 apicompat 文件已本地修改，适合手工移植并补聚焦测试。
- `80560e48d` additional tools discriminator、`651bb019d` Codex 图片 function tool、`156cabd26` Grok 图片模型 Responses 拦截：单提交补丁检查通过，建议与现有图片/工具路由一起做一轮协议回归。
- `3f605c354` 指纹静态资源缓存：修复非指纹资源误用 immutable；当前主线静态缓存文件存在上下文冲突，建议手工吸收核心判定函数。
- `d34819317` 根路径 `/models` Codex 别名：当前 gateway route 定制冲突，适合按现有路由结构补入口。
- `10798abe4` DataTable 行缓存失效：当前组件和测试已本地演进，需把 row identity invalidation 行为移入现有实现。
- `ab369c363` Antigravity refresh token 保留：核心 composable 可直接套用，但创建弹窗组合时产生冲突，应只移植 refresh token fallback 逻辑。
- `30df3f68e` 内容审核关键词热路径、`1c61225b2` Ops 投影查询：性能收益明确，但组合探针分别出现上下文冲突和测试依赖缺失，需补齐依赖后再评估。
- `125b03aee` WebSocket 失败日志增加代理主机：与 `65093591c` 修改同一 handler 区域，需合并为一个本地日志改动。
- `e4a0e6942` keys ID 列、`de52f7ba8` groups ID 列：前端功能独立，但上游使用拆分 i18n，本地仍为单体语言文件，需要按本地 i18n 落位。

## 第三批：当前不建议吸收

- `4355861ef` Codex Agent Identity：跨账号导入、配额、OpenAI 转换、WebSocket、wire 和前端，约 2300 行；与本地已删除或改写的 WebSocket/账号链冲突。
- `0de768e8b`、`12f991dde`、`accb918ec` Grok OAuth 凭证 failover、池健康和管理刷新：单组最高约 5700 行，跨 repository、token refresh、OAuth reconciliation、handler 和 Ops，需完整功能链，不能作为局部补丁。
- `601a1d166`、`5b5e41450` 调度 outbox/lifecycle 最终组：跨本地调度缓存和重建语义，测试与实现规模接近 3700 行，当前冲突面过大。
- `bac925624` WebSocket 首消息超时：依赖本地缺失或已删除的 `openai_ws_http_bridge` 链，延续此前不吸收 WebSocket ingress 大组的边界。
- `3e30862dd` force-chat 直接 Anthropic/ChatCompletions 桥：虽可套用，但新增约 2000 行协议实现，必须作为独立需求评审，不与小修混合。
- `2d218fbe6` 账号一键复制：跨 repository、service、handler、wire、账号页和上游拆分 i18n，与本地三 Tab 账号表单冲突。
- `23796a1b3` Server-Timing Web API 扩展：本地缺少上游 servertiming package，不能独立吸收。

## 推荐实施顺序

1. 先建立新的隔离 worktree，以当前 `f33701edc` 及后续本地提交为基线。
2. 第一批五项按本评估顺序合并，跑聚焦测试和全量后端编译。
3. 第二批先做 cancel-aware failover，再做 OpenAI 透传/SSE 协议组；每组单独提交和验证。
4. 图片、前端表格和 ID 列作为独立批次，不与 failover 热路径混合。
5. Agent Identity、Grok OAuth 池和调度器大组保持跳过，除非后续明确要求完整能力链。

本文件只记录 tag 拉取和可更新项评估，不表示任何 `v0.1.156` 业务提交已合并、构建或部署。

## 第二阶段执行结果

- 执行基线：第一阶段提交 `381862edc`。
- 已适配 `9676b19fb`：客户端取消后停止新的选号和 failover，未提交响应按 499 归类；保留本地已删除 handler 的删除状态。
- 已适配 `80560e48d`：只解析 `type=additional_tools` 输入项，避免普通 message 中异常 `tools` 字段阻断请求。
- 已适配 `651bb019d`：检测客户端 `image_gen.imagegen` function tool，避免重复注入 hosted `image_generation` 工具和桥接提示。
- 已适配 `156cabd26`：Grok 图片模型拒绝走 Responses 端点，并提示使用 `/v1/images/generations`。
- 已适配 `ab369c363`：Antigravity 校验响应不返回新 refresh token 时，保留用户提交的原 token。
- 暂缓 passthrough、首输出超时、SSE、Read tool、静态缓存、根 `/models` 和 DataTable 组：first-parent 补丁依赖当前主线不存在的模块或测试文件，需要后续按本地实现重新设计。
- 验证：handler 取消测试、apicompat、service、两项 unit 聚焦测试、后端全包编译、Antigravity Vitest 和 Vue TypeScript 类型检查通过。
- 边界：未修改版本号、未构建镜像、未部署、未推送。
