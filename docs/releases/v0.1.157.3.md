# sub2api v0.1.157.3 发布记录

- 日期：2026-07-17
- 执行者：Devil
- 测试提交：`e62470bdc54e78192274bd2df6fa73c1ee76828b`
- 功能提交：`8c1aa9fdaf1e2bcc65c7e06ede7a89a75a5200f6`
- 构建提交：`8c1aa9fdaf1e2bcc65c7e06ede7a89a75a5200f6`
- 镜像：`sub2api:v0.1.157.3`
- 镜像 ID：`sha256:60ed01fc277338f5d1182b784ee205e51120903ba647b3a15f4a0dd5c9759862`
- OCI：version=`v0.1.157.3`，revision=`8c1aa9fdaf1e2bcc65c7e06ede7a89a75a5200f6`
- active：green；回滚：blue `sub2api:v0.1.157.2`
- 当前状态：已构建、部署、切流并完成线上观察

## 更新

1. 编辑 API Key 账号时，只要主 Base URL 在本次编辑会话中发生变化，立即清空界面中的请求 Base URL 列表。
2. 保存时显式提交 `request_base_urls: []`，使旧列表同步从数据库凭据字段中删除；不判断列表是否包含新地址。
3. 主 Base URL 未变化时继续保留原请求地址列表，敏感 API Key 仍由后端合并逻辑保留。

## 本地验证

- RED：前端回归收到 `[new]` 而不是要求的 `[]`，精确复现仅清空界面但保存阶段回填新主地址的问题。
- PASS：`EditAccountModal.spec.ts` 与 `AccountAPIKeyCredentialsFields.spec.ts` 共 34 项通过。
- PASS：后端 `TestUpdateAccount_ClearsRequestBaseURLsWhenIncomingEmpty` 通过，确认空数组覆盖旧列表并保留 API Key。
- PASS：目标 ESLint、`pnpm run typecheck`、`pnpm run build` 与 `git diff --check` 通过；仅有既有 Browserslist、动态导入和 chunk 大小提示。

## 发布验证

- PASS：从 committed HEAD `8c1aa9fda` 的 `git archive` 构建；OCI 标签与二进制启动日志均为主版本 `v0.1.157`、image version `v0.1.157.3`、commit `8c1aa9fdaf1e2bcc65c7e06ede7a89a75a5200f6`。
- PASS：仅重建 idle green；active blue、PostgreSQL、Redis 未重启。候选 `18082` health 和根页为 200，admin system、runtime metrics 与 `POST /v1/responses` 未认证均为 401。
- PASS：green 候选独立窗口 67.3 秒，13 次采样全部 healthy、restart 0，关键日志过滤 0。
- PASS：`nginx -t` 与 reload 成功，upstream 从 blue 切至 green。
- PASS：切流后独立窗口 67.9 秒，13 轮共 39 次入口健康检查通过，green restart 0，关键日志过滤 0。
- PASS：`8080`、`18081`、`18082` 的主资源均为 `/assets/index-Ca6VF3o-.js`，SHA-256 均为 `D3EA52A8FE3FFFEB5F7B65C741DCBB735A028643A26FBF28CAED3F49AF1D5542`。

## 回滚与边界

- blue `sub2api:v0.1.157.2` 保持健康运行，作为即时回滚目标。
- 部署 `.env` 备份：`D:\sub2api-deploy\.env.20260717-105110.bak`。
- upstream 备份：`D:\sub2api-deploy\proxy\upstreams\active.conf.20260717-105110.bak`。
- 未执行 Git push、镜像 registry push、数据库迁移、直接 SQL 写入、PostgreSQL 或 Redis重启。
