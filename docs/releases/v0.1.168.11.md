# v0.1.168.11 发布记录（已回滚）

- 日期：2026-08-05
- 执行者：Devil
- 状态：身份校验失败，已回退，不作为可用发布版本。

## 问题

- 功能提交 `95444a78b8b74358252496adcde40fe1ca290b9a` 已从 committed HEAD 构建并完成候选验证。
- 发布脚本错误地从陈旧的 `backend/cmd/server/VERSION` 读取 `v0.1.165`，使 `sub2api:v0.1.168.11` 的主版本不满足最新 tag `v0.1.168` 的发布约束。
- green `.11` 曾被切为 active，发现身份不一致后立即回退 blue `.10`；PostgreSQL、Redis 和 blue 容器未重建。

## 处置

- 提交 `5b0e92ff5bb6ecbab3f1a273e827ea8b7123a44f`，将发布脚本主版本来源改为最新语义化 Git tag。
- `.11` 标签保留为不可复用的审计记录；随后以新的 immutable `v0.1.168.12` 完成正式发布。
- 未执行 Git push、镜像仓库 push、数据库写入或真实付费上游请求。
