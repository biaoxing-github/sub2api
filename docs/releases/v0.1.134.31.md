# v0.1.134.31 Release Log

- 日期：2026-06-11
- 执行者：Devil
- Git 提交：`9205bbe40afb`（`feat(admin): 调整调度池筛选与人工探测模型`）
- 包含上一线上版本后的提交范围：`e94e0b8778ab..9205bbe40afb`
- 镜像标签：`sub2api:v0.1.134.31`
- 镜像 ID：`sha256:54d910b5676df0557e20cadf2898bc6cc2c862478196f31fe6abaf4e9dcdd2ce`
- 构建标签：`org.opencontainers.image.version=v0.1.134.31`，`org.opencontainers.image.revision=9205bbe40afb`
- 发布前 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.30`，Health `healthy`
- 发布前 idle：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.29`，Health `healthy`
- 发布后 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.31`，Health `healthy`
- 回滚目标：`sub2api-blue` / `sub2api:v0.1.134.30`

## 更新内容

- 账号调度池页面移除 OpenAI 模型、接口类型和图像能力筛选展示，也不再向调度池查询提交这些筛选项。
- OpenAI 账号行展示人工探测按钮，与 Anthropic / Antigravity 调度池入口保持一致。
- 人工探测先读取账号可用模型，再按账号测试弹窗默认规则选模型：OpenAI 优先 `gpt-5.5`，Anthropic 优先 `sonnet`，Gemini / Antigravity 走现有优先级排序。

## 构建记录

- 构建来源：`git archive HEAD`，确保使用已提交状态。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.31 --label org.opencontainers.image.version=v0.1.134.31 --label org.opencontainers.image.revision=9205bbe40afb --build-arg COMMIT=9205bbe40afb --build-arg VERSION=v0.1.134 --build-arg IMAGE_VERSION=v0.1.134.31 -`
- 构建结果：成功生成 `sub2api:v0.1.134.31`；前端生产构建与后端 release build 均通过。
- 构建观察：Vite 输出既有 dynamic import / chunk size / Browserslist warning，无构建失败。
- 镜像确认：`docker image inspect sub2api:v0.1.134.31` 返回 `Version=v0.1.134.31`、`Revision=9205bbe40afb`、镜像 ID `sha256:54d910b5676df0557e20cadf2898bc6cc2c862478196f31fe6abaf4e9dcdd2ce`。

## 代码验证

- GREEN：`npm run test:run -- src/views/admin/__tests__/AccountSchedulingPoolView.spec.ts` 通过，1 file / 6 tests passed。
- GREEN：`npm run typecheck` 通过。
- GREEN：`docs/feature_list.jsonl` 与 `docs/process_list.jsonl` 逐行 `ConvertFrom-Json` 解析通过。
- GREEN：`git diff --check` 通过。

## 候选验证

- 部署目标：idle `sub2api-green`，本机候选端口 `127.0.0.1:18082`。
- 部署动作：只重建 `sub2api-green`，未重启 active `sub2api-blue`、PostgreSQL 或 Redis。
- `docker-compose.green.yml` 默认镜像同步为 `sub2api:v0.1.134.31`。
- `docker exec sub2api-green /app/sub2api --version`：`Sub2API v0.1.134 (image: v0.1.134.31, commit: 9205bbe40afb, built: 2026-06-11T15:30:28Z)`。
- `sub2api-green` 候选窗口：`Status=running`，`Health=healthy` 持续超过 60 秒，镜像 `sub2api:v0.1.134.31`。
- `GET http://127.0.0.1:18082/health`：200。
- `GET http://127.0.0.1:18082/`：200。
- `GET http://127.0.0.1:18082/admin/settings`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:18082/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:18082/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:18082/v1/messages` 未登录：401。
- 候选日志过滤：启动窗口内出现 1 条 `[OpenAI] cleanup expired request snapshots failed err=pq: canceling statement due to user request`；切流前最近 1 分钟复查关键错误过滤命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-blue:8080` 切到 `sub2api-green:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `GET http://127.0.0.1:8080/health`：200。
- `GET http://127.0.0.1:8080/`：200。
- `GET http://127.0.0.1:8080/admin/settings`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:8080/v1/messages` 未登录：401。
- 切流后容器状态：`sub2api-green` Health `healthy`，`sub2api-blue` Health `healthy` 且继续运行，`sub2api-proxy` running。
- 切流后 `sub2api-green` 与 `sub2api-proxy` 关键错误日志过滤：命中 0。

## 回滚

- 当前回滚容器：`sub2api-blue`，镜像 `sub2api:v0.1.134.30`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-blue:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- `D:\sub2api-deploy\.env` 中 `ADMIN_PASSWORD` 为空，本轮无法执行管理端登录后的 `/api/v1/admin/system/version` 接口验证；镜像版本由 Docker label 与容器内 `/app/sub2api --version` 交叉确认。
- request snapshot 清理任务在候选启动窗口出现 1 条 `pq: canceling statement due to user request`，切流前最近 1 分钟与切流后窗口无重复；该观察项延续既有启动清理现象，继续留意即可。
