# sub2api v0.1.160.1 发布记录

- 日期：2026-07-17
- 执行者：Devil
- 功能提交：`a0dbc2c4d72898d3d304374a05924bc450bf24a4`
- 构建提交：`a0dbc2c4d72898d3d304374a05924bc450bf24a4`
- 镜像：`sub2api:v0.1.160.1`
- 镜像 ID：`sha256:6a8cdbd14001242ae3197874b71f462579e71df0875f6ee3e8a0d2819043889a`
- OCI：version=`v0.1.160.1`，revision=`a0dbc2c4d72898d3d304374a05924bc450bf24a4`
- active：blue；回滚：green `sub2api:v0.1.157.3`
- 当前状态：已提交、构建、部署、切流并完成线上观察

## 更新

1. 吸收 v0.1.158 的 Grok 手动与快捷端点、Claude/Codex 使用配置、Responses WebSocket v2、Codex 图片终态交接、模型能力与 reasoning-effort 元数据。
2. 吸收 v0.1.159 的 Alpha Search API Key 调度及 404/405 换号、Stripe 按需加载、OpenAI OAuth 手动凭据文案和 API Key 账号上游主页链接。
3. 吸收 v0.1.160 的显式生图意图判定；被动 `image_gen` namespace 不再误触发生图权限、并发、计费或 Responses capability，显式生图保持原规则。

## 本地验证

- PASS：`go test ./internal/service -count=1`，37.151 秒。
- PASS：handler 新增与变更聚焦测试；`go test ./... -run '^$' -count=1` 全仓编译切片。
- PASS：前端 5 个文件 42 项 Vitest、`pnpm run typecheck` 与 `pnpm run build`；生产构建完成 949 个模块并生成独立 `vendor-stripe` chunk。
- PASS：`git diff --check 46ca20e79..a0dbc2c4d`。
- BASELINE：handler 全量仍有 3 个既有失败，2 个 retry-window 期望 60 秒但实现为 30 秒，1 个 WebSocket continuity stub EOF；均已在 `46ca20e79` 复现，非本轮回归。

## 发布验证

- PASS：从 committed HEAD `a0dbc2c4d` 的 `git archive` 构建；OCI 标签与二进制启动日志均为主版本 `v0.1.160`、image version `v0.1.160.1`、commit `a0dbc2c4d72898d3d304374a05924bc450bf24a4`。
- PASS：仅重建 idle blue；active green、PostgreSQL 与 Redis 未重启。候选 `18083` health、首页、工具页和主资源为 200，admin system、runtime metrics 与 `POST /v1/responses` 未认证均为 401。
- PASS：blue 候选独立窗口 60.6 秒，7 次采样全部 healthy、restart 0，关键日志过滤 0。
- PASS：`nginx -t` 与 reload 成功，upstream 从 green 切至 blue。
- PASS：切流后独立窗口 62.1 秒，7 轮共 21 次入口健康检查通过；blue 与 green 均 healthy、restart 0，blue/proxy 关键日志过滤均为 0。
- PASS：`8080`、`18081`、`18083` 的主资源均为 `/assets/index-DYTM084w.js`，SHA-256 均为 `7C37F8857EB8848E1DF04C8007DEAE3934111FBA671A0957DBD22E3E64056D39`。

## 回滚与边界

- green `sub2api:v0.1.157.3` 保持健康运行，作为即时回滚目标。
- 部署 `.env` 备份：`D:\sub2api-deploy\.env.pre-v0.1.160.1-20260717-193312.bak`。
- upstream 备份：`D:\sub2api-deploy\proxy\upstreams\active.conf.pre-v0.1.160.1-20260717-193312.bak`。
- 未执行 Git push、镜像 registry push、数据库迁移、直接 SQL 写入、PostgreSQL 或 Redis 重启。
- 当前没有可用管理员登录态，未在线读取受保护的版本响应体或执行认证管理端视觉验收；镜像标签、运行容器、二进制版本、提交号和资源哈希已核验。
