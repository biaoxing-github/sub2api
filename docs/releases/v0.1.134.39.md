# v0.1.134.39 发布记录

- 日期：2026-06-15
- 执行者：Devil
- Git 提交：`488015772fb1`
- 镜像标签：`sub2api:v0.1.134.39`
- 镜像 ID：`sha256:f680eb1894586023a62a63c7a7833410601cfda06cc7bb81e1b6d96e954e80f8`
- 发布前 active：green，`sub2api:v0.1.134.38`
- 发布后 active：blue，`sub2api:v0.1.134.39`
- 回滚目标：`sub2api-green`，`sub2api:v0.1.134.38`，健康状态 `healthy`

## 更新内容

- 账号管理页改成高密度总览布局：桌面视图把平台、容量、今日与窗口用量、余额、分组、优先级、代理、创建/最后使用/过期时间等核心信息收进账号名称列，减少来回切换列的成本。
- 通用 `DataTable` 增加 `fitWidth` 模式，关闭无意义横向滚动和 sticky 列，让大屏账号总览更容易一次看齐。

## 验证结果

- `npm run typecheck` 通过。
- `npm run build` 通过。
- blue 候选 `http://127.0.0.1:18083/health` 200，首页 200，静态资源 200，未登录 `/api/v1/admin/system/version`、`/responses`、`/v1/messages` 均 401。
- blue 容器健康持续超过 60 秒。
- 切流后 `http://127.0.0.1:8080/health` 200，首页 200，静态资源 200，未登录上述入口均 401。
- blue/green 容器均保持 `healthy`。
- blue 候选日志仅见一次 `pq: canceling statement due to user request` 的后台清理噪声，未见 `panic`、`fatal`、`migration.*fail`、`checksum`、`bind:`、`address already in use`、`listen tcp`、`rebuild failed`。

## 回滚

如蓝侧新版本出现健康失败、冒烟失败或明显回归，将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 切回 `server sub2api-green:8080;`，执行 `docker exec sub2api-proxy nginx -t` 与 `docker exec sub2api-proxy nginx -s reload`。

## 遗留风险

- 本轮主要验证桌面账号管理页总览布局，移动端仍沿用原 DataTable 卡片路径。
- `pq:` 只出现一次，来源于后台请求快照清理，不影响服务健康；后续若持续出现再单独排查。
