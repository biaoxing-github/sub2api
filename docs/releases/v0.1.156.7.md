# sub2api v0.1.156.7 发布记录

- 日期：2026-07-16
- 执行者：Devil
- 功能合并提交：`55301cc1d4e2`
- 版本修正提交：`e003548fb357`
- 构建提交：`e003548fb357`
- 镜像：`sub2api:v0.1.156.7`
- 镜像 ID：`sha256:0916e5e634941110df6582f3cf62b388ce7993d70a1a978d39c8d93328d02c80`
- 计划 active：blue；回滚：green `sub2api:v0.1.156.5`
- 当前状态：运行时契约失败，已回滚且不复用

## 更新

1. GPT-5.6 长上下文成本按新价格区间统计；failover 日志补充代理信息，OAuth 图片用实际输出尺寸计费。
2. 客户端断开后停止选号与 failover；取消场景保持 499 语义，避免继续消耗上游请求。
3. Responses 兼容 `additional_tools`，保留客户端提供的 `image_gen.imagegen`，并拒绝 Grok 图片模型误走 Responses 端点。
4. Responses 可配置响应头与首个完整语义输出等待时间，超时最多切换一次账号；HTTP SSE 与 WebSocket 可拆分同一载荷中的拼接 JSON 文档。
5. 修复并行 `tool_use` 索引和 ghost delta；Read 工具空 pages、Anthropic incomplete/异常流均生成一致终态。
6. 根路径 `/models` 复用 `/v1/models` 的认证中间件和处理器；Antigravity 手工 refresh token 在上游未轮换时保留。
7. OpenAI 内容会话 seed 减少重复扫描与分配；嵌入式主版本由 `v0.1.152` 更新为 `v0.1.156`。

## 本地验证

- service、apicompat、handler、routes、config 聚焦/全包测试通过。
- `go test ./... -run '^$' -count=1` 全仓编译切片通过。
- Antigravity OAuth Vitest 2/2 与 `pnpm run typecheck` 通过。
- `.6` 旧主版本构建在镜像生成和部署前终止，blue 容器与部署配置未变更。

## 发布验证

- PASS：镜像 OCI version=`v0.1.156`、revision=`e003548fb357`；二进制 image_version=`v0.1.156.7`。
- PASS：仅重建 idle blue；候选冒烟通过。启动期清理日志命中一次 `pq:` 后，重新建立 71.4 秒干净窗口，8 次采样均 healthy/restart 0/HTTP 200。
- PASS：nginx 检查和切到 blue 的 reload 成功。
- FAIL：切流后发现 `/models` 返回前端 SPA HTML 200，而 `/v1/models` 正确返回 API Key 401；根别名被嵌入前端中间件提前截获。
- PASS：立即切回 green `.5`，nginx 检查/reload 和 `8080`、`18081`、`18082` 健康检查通过。
- NEXT：修复嵌入前端后端路由放行列表后顺延到 `v0.1.156.8`，`.7` 永久不复用。

## 回滚与边界

- 旧 active green `sub2api:v0.1.156.5` 保留为回滚目标，不重建。
- 无新增迁移，不写数据库，不重启 PostgreSQL 或 Redis。
- upstream 备份：`D:\sub2api-deploy\proxy\upstreams\active.conf.bak-20260716-162947`。
- 未执行 Git push 或 registry push。
