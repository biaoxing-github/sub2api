# sub2api v0.1.156.6 发布记录

- 日期：2026-07-16
- 执行者：Devil
- 功能合并提交：`55301cc1d4e2`
- 构建提交：待发布记录初始提交后回填
- 镜像：`sub2api:v0.1.156.6`
- 镜像 ID：待构建后回填
- 计划 active：blue；回滚：green `sub2api:v0.1.156.5`
- 当前状态：待构建、待候选验证、待切流

## 更新

1. GPT-5.6 长上下文成本按新价格区间统计；failover 日志补充代理信息，OAuth 图片用实际输出尺寸计费。
2. 客户端断开后停止选号与 failover；取消场景保持 499 语义，避免继续消耗上游请求。
3. Responses 兼容 `additional_tools`，保留客户端提供的 `image_gen.imagegen`，并拒绝 Grok 图片模型误走 Responses 端点。
4. Responses 可配置响应头与首个完整语义输出等待时间，超时最多切换一次账号；HTTP SSE 与 WebSocket 可拆分同一载荷中的拼接 JSON 文档。
5. 修复并行 `tool_use` 索引和 ghost delta；Read 工具空 pages、Anthropic incomplete/异常流均生成一致终态。
6. 根路径 `/models` 复用 `/v1/models` 的认证中间件和处理器；Antigravity 手工 refresh token 在上游未轮换时保留。
7. OpenAI 内容会话 seed 减少重复扫描与分配；新增首输出 staging 的内存、磁盘和清理边界测试。

## 本地验证

- `git diff --cached --check` 通过，无未解决冲突、冲突标记或新增迁移。
- `go test ./internal/service -count=1` 通过。
- `go test ./internal/pkg/apicompat -count=1` 通过。
- handler 取消感知 failover 与首输出超时聚焦测试通过。
- `go test ./internal/server/routes -count=1` 和 config 新增项聚焦测试通过。
- `go test ./... -run '^$' -count=1` 全仓编译切片通过。
- Antigravity OAuth Vitest 2/2 与 `pnpm run typecheck` 通过。

## 发布验证

- 待回填 committed HEAD、OCI 标签、二进制主版本与 `image_version` 核验。
- 待回填 idle blue 候选冒烟、60 秒健康/重启/日志观察。
- 待回填 nginx 检查、切流、三入口健康和静态资源哈希。

## 回滚与边界

- 旧 active green `sub2api:v0.1.156.5` 保留为回滚目标，不重建。
- 无新增迁移，不写数据库，不重启 PostgreSQL 或 Redis。
- 未执行 Git push 或 registry push。
