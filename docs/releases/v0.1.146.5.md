# v0.1.146.5 发布验证

日期：2026-07-09
执行者：Devil

## 基线

- Git 提交：`eff26f02c454`
- 镜像标签：`sub2api:v0.1.146.5`
- 镜像 ID：`sha256:0d19a35a9047b379966f1f5f8ebfea600584ca4dfb7468fc062b768f548d958b`
- 发布前 active：`sub2api-green:8080`
- 发布前线上镜像：`sub2api:v0.1.146.4`
- 本次 idle 候选：`sub2api-blue:8080`
- 回滚目标：`sub2api-green:8080`，镜像 `sub2api:v0.1.146.4`

## 详细更新日志

1. GPT-5.6 模型加载上线
   - 涉及模块：`frontend/src/composables/useModelWhitelist.ts`、`backend/internal/pkg/openai`、`backend/internal/service/pricing_service.go`、`backend/internal/service/billing_service.go`
   - 用户可见行为：OpenAI 模型白名单和预设映射可见 `gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`。
   - 价格行为：远端价格缺失时，三档 GPT-5.6 使用业务指定静态价格：Sol 输入 5 USD/MTok、输出 30 USD/MTok；Terra 输入 2.5 USD/MTok、输出 15 USD/MTok；Luna 输入 1 USD/MTok、输出 6 USD/MTok。

2. Grok 4.5 默认模型上线
   - 涉及模块：`backend/internal/pkg/xai/models.go`、`frontend/src/composables/useModelWhitelist.ts`
   - 用户可见行为：Grok 模型列表以 `grok-4.5` 为默认项，`grok` 和 `grok-latest` 别名指向 `grok-4.5`。

3. 本次问题修正
   - 现象：用户在页面看不到 GPT-5.6 模型。
   - 根因：`v0.1.146.5` 镜像已构建，但入口代理仍指向 active `sub2api-green`，线上实际运行 `sub2api:v0.1.146.4`。
   - 修正：将 `sub2api:v0.1.146.5` 部署到 idle `sub2api-blue`，候选验证通过后切流 `active.conf` 到 `sub2api-blue:8080`。

## 部署步骤

1. 备份并更新 `D:\sub2api-deploy\.env`
   - 备份：`D:\sub2api-deploy\.env.bak-v0.1.146.5-20260709-185952`
   - 修改：`SUB2API_BLUE_IMAGE=sub2api:v0.1.146.5`

2. 重建 idle blue
   - 命令：`docker compose -f D:\sub2api-deploy\docker-compose.blue.yml --env-file D:\sub2api-deploy\.env up -d --no-deps --force-recreate sub2api-blue`
   - 结果：`sub2api-blue` 使用 `sub2api:v0.1.146.5` 启动并进入 healthy。

3. 切流代理
   - 备份：`D:\sub2api-deploy\proxy\upstreams\active.conf.bak-v0.1.146.5-20260709-190329`
   - 修改：`active.conf` 指向 `sub2api-blue:8080`
   - 验证：`docker exec sub2api-proxy nginx -t` 通过，`docker exec sub2api-proxy nginx -s reload` 成功。

## 验证结果

- 候选 health：`http://127.0.0.1:18083/health` 返回 `200 {"status":"ok"}`
- 切流后 health：
  - `http://127.0.0.1:8080/health` 返回 `200 {"status":"ok"}`
  - `http://127.0.0.1:18081/health` 返回 `200 {"status":"ok"}`
  - `http://127.0.0.1:18083/health` 返回 `200 {"status":"ok"}`
- 受保护路由：
  - `GET http://127.0.0.1:8080/api/v1/admin/users` 返回 `401`
  - `POST http://127.0.0.1:8080/responses` 返回 `401`
  - `POST http://127.0.0.1:8080/v1/responses` 返回 `401`
- 前端资源验证：
  - `GET /assets/ModelWhitelistSelector.vue_vue_type_script_setup_true_lang-BCLhTQ0Z.js` 返回 `200 text/javascript`
  - 该资源包含 `gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`
  - 该资源包含 `GPT-5.6 Sol`、`GPT-5.6 Terra`、`GPT-5.6 Luna`
- 运行状态：
  - `sub2api-blue`：`sub2api:v0.1.146.5`，healthy，restart count 0
  - `sub2api-green`：`sub2api:v0.1.146.4`，healthy，作为回滚目标保留
- 日志观察：
  - 候选重建后首次日志包含一次 request snapshot 后台清理取消：`pq: canceling statement due to user request`
  - 后续 70 秒新日志窗口关键字命中 0
  - 切流后 65 秒观察窗口 blue/proxy 关键字命中 0

## 当前 active

- active：`sub2api-blue:8080`
- 线上镜像：`sub2api:v0.1.146.5`
- 回滚：将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 切回 `sub2api-green:8080` 并 reload nginx。

## 遗留风险

- 未执行已认证后台点击验证，原因是本地未提供可用非交互 admin 登录态。
- 用户浏览器若仍显示旧模型列表，需刷新页面以重新加载新的 hashed chunk。
