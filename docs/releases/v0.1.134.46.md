# sub2api v0.1.134.46 发布记录

- 日期：2026-06-16
- 执行者：Devil
- Git 提交：11a854f36
- 镜像标签：sub2api:v0.1.134.46
- active/idle：发布前 active=blue(sub2api:v0.1.134.45)，idle=green(sub2api:v0.1.134.44)；发布后 active=green(sub2api:v0.1.134.46)，idle=blue(sub2api:v0.1.134.45)

## 更新内容

- 修复 gptai-plus 这类 OpenAI Responses 流：上游返回 `response.completed` / `response.done` 和输出 delta，但缺少 `response.usage` 与 `data: [DONE]` 时，下游终态事件补零值 usage，并补写 `data: [DONE]`。
- 保持已有 `data: [DONE]` 的上游不重复写结束标记，避免客户端收到重复终止事件。
- 管理端调度池人工测验成功后恢复账号可调度、清理账号错误状态与 OpenAI path-health 降级状态；普通上游体检和后台体检仍只记录体检结果，不自动修复调度池状态。
- OpenAI 调度耗尽探测保留临时冷却账号作为等待候选，避免小池子冷却期被当成空池快速失败。
- 管理端账号列表 usage 摘要标题使用叶子翻译 key，避免显示根 key；补齐调度池页面 `shallowRef` 导入，恢复前端 typecheck。

## 验证结果

- `go test -tags unit ./internal/service -run "TestRateLimitService_RecordAccountProbeOutcome|TestAccountProbeService|TestAccountProbeOutcome|TestOpenAIGatewayService_ManualProbeSuccessRestoresSchedulingPoolHealth|TestOpenAISchedulerExhaustionProbe|TestOpenAIStreaming(MissingTerminalEventAfterOutputSynthesizesTerminal|TerminalEventFromSSEEventLineCompletes|TerminalEventWithoutUsageAddsClientTerminalFields|TerminalEventWithDoneDoesNotDuplicateDoneMarker|MissingTerminalEventRecordsPathHealthFailure)|TestOpenAIGatewayServiceRecordUsage_(ZeroUsageStillWritesUsageLog|MissingObservedUsageRejectsUsageLog|FeedsPathHealthSample|MissingPricingRecordsZeroCostUsageLog)" -count=1 -v`：通过。
- `go test -tags unit ./internal/handler/admin -run "TestAccountHandler_ManualProbe|TestAccountProbe(Create|Batch)|TestAccountProbeReportBatch" -count=1 -v`：通过。
- `go test ./cmd/server -run TestNoSuchTest -count=1`：通过。
- `npm run test:run -- src/views/admin/__tests__/AccountsView.bulkEdit.spec.ts -t "uses the usage summary title leaf key"`：通过。
- `npm run typecheck`：通过。
- `git diff --check`：通过，仅有 `.codegraph/daemon.pid` 与 JSONL 的 CRLF 提示。
- `git archive HEAD | docker build ... sub2api:v0.1.134.46`：通过，镜像 label revision=2b99aab9b7c6。
- green 候选 `18082`：`/health` 200、管理前端静态资源 200、未登录 `/api/v1/admin/system/version` 401、未登录 `POST /responses` 401，容器 healthy。
- green 启动日志：启动后 3 分钟扫描到一次已知 request snapshot cleanup `pq: canceling statement due to user request`；等待 70 秒观察窗后关键日志命中 0。
- 切流后 `8080` 与 `18081`：`/health` 200，管理前端 200，未登录 `/api/v1/admin/system/version` 401，未登录 `/responses` 401。
- 切流后真实 gptai-plus 长文本验证：临时隔离 key 命中 `account_id=434`，模型 `gpt-5.5`，HTTP 200；保存 `.codex/gptai-plus-done-verify-gpt55-20260616195921.sse`；`delta_events=64`、`terminal_events=2`、`usage_events=1`、`zero_usage_in_completed=true`、`done_markers=1`、`text_chars=774`、`has_exact_done_check=true`。
- 验证后清理临时 group/key/account_groups，并恢复 `account_id=434 schedulable=false`。

## 回滚目标

- 秒级回滚：将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 切回 `sub2api-blue:8080` 并 reload nginx。
- 回滚镜像：sub2api:v0.1.134.45。

## 遗留风险

- 当前 GitHub 凭据 `biaoxing-github` 推送 `Wei-Shaw/sub2api` 被 403 拒绝；本机提交、构建、部署和真实验证已完成，远端推送需切换有权限凭据后重试。
- `account_id=434` 的真实上游余额记录显示可用额度为负，本轮仅在临时验证窗口恢复可调度并验证流式协议补齐，验证后已恢复为 `schedulable=false`。
- 如果客户端请求 `gpt-5.1`，调度器会返回“no available OpenAI accounts supporting model: gpt-5.1”并进入等待流；gptai-plus 真实可用模型按本轮样本为 `gpt-5.5`。
