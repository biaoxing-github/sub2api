# sub2api v0.1.150.1 发布记录

- 日期：2026-07-10
- 执行者：Devil
- Git 提交：`4db1f5a4467b914fa25fa57c52385cf07bb4e017`
- 主版本：`v0.1.149`（上游 `v0.1.150` tag 保持的服务内嵌版本）
- 不可变镜像：`sub2api:v0.1.150.1`
- active：`sub2api-blue:8080`
- idle / 回滚：`sub2api-green:8080`，`sub2api:v0.1.149.1`

## 详细更新日志

1. OpenAI 协议与 Codex 链路
   - 涉及 `backend/internal/pkg/apicompat/`、`backend/internal/service/openai_*`、`backend/internal/handler/openai_gateway_handler.go`。
   - 吸收 `parallel_tool_calls` 双向映射、compact SSE 保活与终态补零、Windows WebSocket 重置、推理强度候选和 Codex `0.144.1` 兼容。
   - 用户可见行为：OpenAI Responses / Chat Completions 互转字段更完整，compact/流式请求和 Codex 客户端兼容性更稳定。

2. GPT-5.6 定价和探测默认值
   - 涉及 `backend/internal/pkg/openai/constants.go`、`backend/internal/service/billing_service.go`、`pricing_service.go`、模型价格资源与账户测试前后端组件。
   - GPT-5.6 cache-write 参与总上下文阈值、priority 定价与计费；模型别名补齐 Terra/Sol/Luna。
   - 用户可见行为：账号、API Key、批量、定时、模型探测、探测报告和调度池手动探测默认统一为 `gpt-5.6-terra`。

3. 支付履约和订阅恢复
   - 涉及 `payment_fulfillment.go`、`subscription_service.go`、订阅仓储/DTO/管理路由及 `SubscriptionsView.vue`。
   - 增加跨实例缓存失效、事务复用、陈旧支付 lease 恢复、履约/返利幂等补偿；支持撤销订阅恢复与恢复时间展示。
   - 用户可见行为：管理员可从已撤销列表恢复订阅；支付恢复和订阅缓存一致性更可靠。

4. 订阅缓存监听生命周期
   - 涉及 `backend/internal/service/subscription_service.go` 与 `subscription_revoke_cache_test.go`。
   - `SubscriptionService.Stop` 会取消跨实例订阅 L1 缓存失效的 Pub/Sub 上下文，底层监听随之关闭。
   - 用户可见行为：服务重建不再遗留重复 Redis Pub/Sub 监听或协程。

## 构建与候选验证

- 构建方式：`git archive --format=tar HEAD | docker build`，构建源为已提交的 `4db1f5a4467b`。
- 构建参数：USTC Alpine 镜像源、`IMAGE_VERSION=v0.1.150.1`、提交标签和 OCI 镜像标签均已写入。
- 候选：只重建 idle `sub2api-blue`，候选端口 `127.0.0.1:18083`。
- 健康：blue 连续健康超过 60 秒，`RestartCount=0`。
- 候选冒烟：`/health`、`/` 为 `200`；未登录管理版本 API、`/responses`、`/v1/responses` 均为 `401`，未出现 `502`。
- 日志：首次扩展日志范围捕获到既有 request snapshot 清理超时；随后独立 75 秒 blue 日志窗口通过，`panic`、`fatal`、`migration.*fail`、`checksum`、`pq:`、绑定端口与 rebuild 失败均为零。

## 切流与线上验证

- 已备份 `D:\sub2api-deploy\.env` 和 `D:\sub2api-deploy\proxy\upstreams\active.conf`，再将 blue 镜像变量更新为 `sub2api:v0.1.150.1`。
- 代理 upstream 从 `sub2api-green:8080` 切至 `sub2api-blue:8080`；`nginx -t` 与 reload 成功。
- `8080`、`18081`、`18083` 的 `/health` 与首页均返回 `200`，未登录管理 API 和 `/responses` 均返回 `401`。
- 三个入口的实际 `index-B5YO7pRB.js` SHA-256 一致：`E5626BE17E00E8251D714E41C9210633B6066F9A67B2FFBF923CF7E1B3B94278`。
- 切流后 blue 与代理独立 75 秒日志窗口通过；blue、green 均为 `healthy`、`RestartCount=0`。

## 回滚与遗留风险

- 回滚目标：将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 切回 `sub2api-green:8080` 后执行 `docker exec sub2api-proxy nginx -t` 和 `nginx -s reload`；green 当前为 `sub2api:v0.1.149.1` 且健康。
- 未执行真实认证上游请求或带管理员登录态的浏览器操作。
- 前端测试仍会输出既有 `common.time.never` 英文 locale 缺失警告，未在本轮无关范围内修改。
