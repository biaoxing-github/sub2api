# v0.1.134.20 Release Log

- 日期：2026-06-09
- 执行者：Devil
- Git 提交：`79b4b9480fcc`
- 镜像标签：`sub2api:v0.1.134.20`
- 镜像 ID：`sha256:624551c6e45b41c150efc0b48299a4091495f7889b16de6c2f05625c46d5a2e9`
- 构建标签：`org.opencontainers.image.version=v0.1.134.20`，`org.opencontainers.image.revision=79b4b9480fcc`
- 发布前 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.19`，Health `healthy`
- 发布后 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.20`，Health `healthy`
- 后续状态：线上特性验证发现调度池仍读取错误 upstream key，本版本已被 `sub2api:v0.1.134.21` 替代。
- 回滚目标：发布 v0.1.134.20 时为 `sub2api-blue` / `sub2api:v0.1.134.19`。

## 更新内容

- 发布通用 `GatewayService.RecordUsage` 写入共享 OpenAI path-health tracker 的修复。
- 目标是让真实 `/responses` 成功调用进入调度池样本统计。

## 验证结果

- 构建：`git archive HEAD` 构建 `sub2api:v0.1.134.20` 成功。
- 候选 green：`18082` health/root/admin/settings 均 200；未登录 admin accounts 与 `/responses` 均 401；管理端版本返回 `image_version=v0.1.134.20`。
- 候选调度池：OpenAI group=2 返回 `total=3`，但 samples 初始为 0。
- 切流：`active.conf` 从 `sub2api-blue:8080` 切到 `sub2api-green:8080`，`nginx -t` 与 reload 成功。
- 切流后：`8080` 基础冒烟通过，版本为 `image_version=v0.1.134.20`。
- 特性验证失败：切流后 DB 与 green 日志均显示真实 OpenAI `/responses` 200 已进入账号 `free6` / `dengxian-4`，但调度池 `path_health.samples` 仍为 0。

## 失败根因

- 真实样本写入使用完整账号 credentials 的 upstream key，例如 `https://ai2.hhhl.cc/v1` / `https://api.denxio.top`。
- 调度池从 scheduler snapshot 账号构建 path-health key，读取到了默认 `https://api.openai.com` key。
- 因为读写 upstream key 不一致，v0.1.134.20 没有完成用户要求的可视化样本更新。

## 回滚

- v0.1.134.20 已被 `sub2api:v0.1.134.21` 替代。
- 本轮未重启 PostgreSQL 和 Redis。

## 遗留风险

- v0.1.134.20 不作为最终保留版本；最终修复见 `docs/releases/v0.1.134.21.md`。
