# v0.1.134.17 Release Log

- 日期：2026-06-09
- 执行者：Devil
- Git 提交：`ed18ce9ab114`
- 镜像标签：`sub2api:v0.1.134.17`
- 镜像 ID：`sha256:57b37199d0e341194b7f17f59e425094c362e693d6ce800b47f68b01b20ac73c`
- 构建标签：`org.opencontainers.image.version=v0.1.134.17`，`org.opencontainers.image.revision=ed18ce9ab114`
- 发布前 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.16`，Health `healthy`
- 发布前 idle：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.11`，Health `healthy`
- 发布后 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.17`，Health `healthy`
- 回滚目标：`sub2api-green` / `sub2api:v0.1.134.16`

## 更新内容

- 发布已提交的调度池修复：调度池页面加载分组下拉并默认选中 active 的“自用”分组，不再要求手填分组 ID。
- 调度池接口新增 `platform` 查询参数，支持 OpenAI 与 Anthropic 按分组查询可调度账号。
- Anthropic 调度池复用 scheduler snapshot 分组口径，并保留池内直接停止调度操作。
- 管理端版本徽标通过 `/api/v1/admin/system/check-updates` 展示 `image_version=v0.1.134.17`。

## 构建记录

- 构建来源：`git archive HEAD`，确保使用已提交状态。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.17 --label org.opencontainers.image.version=v0.1.134.17 --label org.opencontainers.image.revision=ed18ce9ab114 --build-arg COMMIT=ed18ce9ab114 --build-arg VERSION=v0.1.134 --build-arg IMAGE_VERSION=v0.1.134.17 -`
- 构建结果：成功生成 `sub2api:v0.1.134.17`。
- 版本输出：`Sub2API v0.1.134 (image: v0.1.134.17, commit: ed18ce9ab114, built: 2026-06-09T09:49:15Z)`。

## 候选验证

- 部署目标：idle `sub2api-blue`，本机候选端口 `127.0.0.1:18083`。
- `GET http://127.0.0.1:18083/health`：200。
- `GET http://127.0.0.1:18083/`：200。
- `GET http://127.0.0.1:18083/admin`：200。
- `GET http://127.0.0.1:18083/api/v1/admin/accounts?page=1&page_size=1` 未登录：401。
- `POST http://127.0.0.1:18083/responses` 未登录：401。
- `GET http://127.0.0.1:18083/api/v1/settings/public`：200。
- `GET http://127.0.0.1:18083/api/v1/admin/system/check-updates` 管理鉴权：`current_version=v0.1.134`，`image_version=v0.1.134.17`，`build_type=release`。
- `GET http://127.0.0.1:18083/api/v1/admin/groups/all?include_inactive=false` 管理鉴权：200，找到 active 分组“自用” `id=2`。
- `GET http://127.0.0.1:18083/api/v1/admin/accounts/scheduling-pool?platform=openai&group=2` 管理鉴权：200，`platform=openai`，`group_id=2`，`total=7`。
- `GET http://127.0.0.1:18083/api/v1/admin/accounts/scheduling-pool?platform=anthropic&group=2` 管理鉴权：200，`platform=anthropic`，`group_id=2`，`total=0`。
- `sub2api-blue` 60 秒稳定窗口：`Status=running`，`Health=healthy`，镜像 `sub2api:v0.1.134.17`。
- `sub2api-blue` 自部署以来关键错误日志过滤：`panic|fatal|migration.*fail|checksum|pq:|bind:|address already in use|listen tcp|rebuild failed` 命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-green:8080` 切到 `sub2api-blue:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `GET http://127.0.0.1:8080/health`：200。
- `GET http://127.0.0.1:8080/`：200。
- `GET http://127.0.0.1:8080/admin`：200。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts?page=1&page_size=1` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401。
- `GET http://127.0.0.1:8080/api/v1/settings/public`：200。
- `GET http://127.0.0.1:8080/api/v1/admin/system/check-updates` 管理鉴权：`current_version=v0.1.134`，`image_version=v0.1.134.17`，`build_type=release`。
- `GET http://127.0.0.1:8080/api/v1/admin/groups/all?include_inactive=false` 管理鉴权：200，找到 active 分组“自用” `id=2`。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts/scheduling-pool?platform=openai&group=2` 管理鉴权：200，`platform=openai`，`group_id=2`，`total=7`。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts/scheduling-pool?platform=anthropic&group=2` 管理鉴权：200，`platform=anthropic`，`group_id=2`，`total=0`。
- `sub2api-blue` 切流后关键错误日志过滤：命中 0。
- `sub2api-proxy` 最近 10 分钟错误日志过滤：命中 0。

## 回滚

- 当前回滚容器：`sub2api-green`，镜像 `sub2api:v0.1.134.16`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-green:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- 本次发布冒烟覆盖健康、静态资源、鉴权边界、`/responses` 未授权边界、管理端版本可见性、分组下拉依赖的分组接口、OpenAI/Anthropic 调度池分组查询和关键日志过滤；未在切流后执行带真实业务 key 的上游付费请求。
