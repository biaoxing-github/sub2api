# sub2api v0.1.134.45 发布记录

- 日期：2026-06-16
- 执行者：Devil
- Git 提交：dc03035462ab
- 镜像标签：sub2api:v0.1.134.45
- active/idle：发布前 active=green(sub2api:v0.1.134.44)，idle=blue(sub2api:v0.1.134.43)；发布后 active=blue(sub2api:v0.1.134.45)，idle=green(sub2api:v0.1.134.44)

## 更新内容

- 修复 gptai-plus 这类 OpenAI Responses 流：上游返回 `response.completed` 和输出 delta 但没有 `usage` 时，不再写入 0 token / 0 成本的假 usage log。
- 保留真实 `usage=0` 的写日志能力，避免影响上游明确返回 usage 对象的路径。
- 增加请求级调度账号列表快照方法，恢复已有 hotpath 测试对同一请求内 failover 复用账号列表的覆盖。

## 验证结果

- `go test ./internal/service -run "TestSelectAccountWithLoadAwareness_ReusesRequestSchedulingSnapshot|TestSelectAccountWithLoadAwareness_PrivacyUnsetSkipsWithoutSetError|TestOpenAIStreamingTerminalEventWithoutUsageMarksUsageMissing" -count=1 -v`：通过。
- `go test ./cmd/server -run TestNoSuchTest -count=1`：通过。
- `go test -tags unit ./internal/handler -run TestNoSuchTest -count=1`：通过。
- `git archive HEAD | docker build ... sub2api:v0.1.134.45`：通过，镜像 label revision=dc03035462ab。
- blue 候选 `18083`：`/health` 200、管理前端 200、未登录 `/api/v1/admin/system/version` 401、`/responses` 401、`/v1/messages` 401，容器 healthy 持续 60 秒。
- blue 关键日志：启动期出现一次 request snapshot cleanup 的 `pq: canceling statement due to user request`，等待 90 秒观察窗后关键日志命中 0。
- 切流后 `8080` 与 `18081`：`/health` 200，未登录 `/responses` 401。
- 真实 gptai-plus 上游调用：HTTP 200，包含 `response.completed` 和输出 delta，不包含 `usage`，证实线上故障形状。

## 回滚目标

- 秒级回滚：将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 切回 `sub2api-green:8080` 并 reload nginx。
- 回滚镜像：sub2api:v0.1.134.44。

## 遗留风险

- 生产隔离临时 key 受认证/分组缓存影响未稳定命中 account_id=434，因此未继续扰动线上数据库；本轮真实验证采用 account_id=434 自身凭据直连上游和本地网关单测闭环。
