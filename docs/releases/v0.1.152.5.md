# sub2api v0.1.152.5 发布记录

- 日期：2026-07-13
- 执行者：Devil
- Git 提交：`022bc50d240a81063c83755b40a4fb43d3552174`
- 镜像：`sub2api:v0.1.152.5`
- 镜像 ID：`sha256:c1496404fb8e173412a697af5bf23bb926b5cb605cbbcfa2f3a3a146d8d31df9`
- 当前 active：`sub2api-green:8080`
- idle / 回滚目标：`sub2api-blue:8080`，`sub2api:v0.1.152.3`

## 详细更新日志

1. 扩展 `frontend/src/components/account/AccountFormTabs.vue`，在“基本设置 / 更多设置”之外新增独立“模型设置”Tab，并保留鼠标、方向键、Home 和 End 键切换能力。
2. 调整 `CreateAccountModal.vue` 与 `EditAccountModal.vue`：添加和编辑账号仍默认打开基本设置，基本设置不再显示模型限制或普通模型映射，更多设置也不再显示这些配置。
3. 将 API Key、OAuth、Vertex、Bedrock 和 Antigravity 账号的模型限制与普通模型映射统一放入模型设置 Tab；模型 Tab 不显示 Base URL、API Key 等凭据字段。
4. 调整 `AccountAPIKeyCredentialsFields.vue` 与 `AccountModelRestrictionSection.vue` 的显隐边界，保持原表单状态、已有模型配置和提交载荷不变。
5. 更新中英文 i18n 文案，并扩展账号弹窗组件测试，覆盖三个 Tab 的默认态、隔离行为和模型配置可见性。

## 用户可见行为

- 打开添加账号或编辑账号弹窗时，默认进入“基本设置”，集中显示名称、Base URL、API Key 和分组等高频字段。
- 备注、额外连接参数、代理、并发、错误策略、配额、调度和协议开关继续通过“更多设置”编辑。
- 模型限制与普通模型映射通过独立“模型设置”Tab 编辑，不再占用基本设置和更多设置。
- OpenAI 的 Compact 模式及 Compact 专属模型映射按确认后的范围继续保留在更多设置。
- 侧边栏主版本为 `v0.1.152`，版本下拉中的镜像小版本为 `v0.1.152.5`。

## 构建与验证

- TDD RED 提交 `f855c186d`：第三个 Tab、创建弹窗模型隔离和编辑弹窗模型隔离测试在实现前按预期失败。
- GREEN 业务提交 `022bc50d240a`：5 个聚焦 Vitest 文件、37 个测试全部 PASS；目标 ESLint、`npm run typecheck`、`git diff --check` 全部 PASS。
- CodeGraph 重索引完成：2302 个文件、71580 个节点。
- `sub2api:v0.1.152.4` 在候选部署前的镜像身份检查中发现二进制未注入 `image_version`，因此被拒绝；该镜像未部署、未切流，且不再复用。
- 从 committed HEAD `022bc50d240a` 构建不可变镜像 `sub2api:v0.1.152.5`：PASS。
- OCI 标签：version=`v0.1.152.5`、revision=`022bc50d240a`；容器二进制为 `Sub2API v0.1.152 (image: v0.1.152.5, commit: 022bc50d240a)`：PASS。
- 仅重建 idle green；候选 `18082` 的 `/health`、首页和主静态资源为 200，受保护管理 API、`/responses`、`/v1/responses` 未登录为 401：PASS。
- green 启动期出现一次已知 `pq: canceling statement due to user request`；后续候选独立观察窗中 7 次 health 均为 200，green healthy/restart 0，新增关键日志匹配 0。
- nginx `-t` 与 reload：PASS；active 从 blue 切换到 green。
- 切流后 `8080`、`18081`、`18082` 完整冒烟：PASS；三入口主资源均为 `/assets/index-D_ZObvdM.js`，SHA-256 均为 `71432681E35883007207744B058050FEB544819A67D7F76F69D1399FA84C45D1`。
- 切流后 70 秒观察窗：7 次三入口 health 均为 200，green healthy/restart 0，proxy running/restart 0；随后近 3 分钟精确严重日志过滤 green=0、proxy=0。
- Chrome 管理员登录态验收：侧边栏主版本 `v0.1.152`、版本下拉镜像版本 `v0.1.152.5`；新增和编辑弹窗均显示三个 Tab，基本/更多设置不显示模型限制和普通模型映射，模型设置显示 OpenAI 模型白名单与普通模型映射，未保存账号数据。
- blue `v0.1.152.3` 保持 healthy/restart 0；PostgreSQL、Redis 未重启。

## 回滚

将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 的 upstream 改回 `sub2api-blue:8080`，执行 `docker exec sub2api-proxy nginx -t`，通过后执行 `docker exec sub2api-proxy nginx -s reload`。部署前备份为 `D:\sub2api-deploy\proxy\upstreams\active.conf.bak-v0.1.152.5-20260713-200740`，环境配置备份为 `D:\sub2api-deploy\.env.bak-v0.1.152.5-20260713-200740`。

## 遗留风险

- 候选启动期的一次 PostgreSQL 查询取消未在后续候选与切流后观察窗复现；当前 green、blue、PostgreSQL 和 Redis 均保持运行且应用容器 restart 0。
- 未执行真实认证上游请求，本轮线上验证覆盖镜像身份、容器健康、静态资源、未授权路由、管理员登录态页面和日志观察。
- 未执行 Git push、镜像 registry push。
