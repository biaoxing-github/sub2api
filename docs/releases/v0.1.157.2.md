# sub2api v0.1.157.2 发布记录

- 日期：2026-07-16
- 执行者：Devil
- 功能提交：`17d554024975703297e8e6b871d3410dcf8283d8`
- 构建提交：`17d554024975703297e8e6b871d3410dcf8283d8`
- 镜像：`sub2api:v0.1.157.2`
- 镜像 ID：`sha256:68361ff11799c31a98a5d5f724b514f72fab975e14da799306756854e640a9b3`
- OCI：version=`v0.1.157.2`，revision=`17d554024`
- active：blue；回滚：green `sub2api:v0.1.157.1`
- 当前状态：已构建、部署、切流并完成线上观察

## 更新

1. 上游 HTTP 客户端池按代理与 Base URL 拓扑有界复用，并避免关闭 in-flight 请求。
2. BillingCache 在队列满、关闭或 Redis 写失败后标记不安全并有界回源，避免继续使用过期正缓存。
3. Responses、Chat Completions 与 Anthropic Messages 共享请求级调度快照；管理端新增固定低基数运行时诊断摘要。
4. Context Journal Redis 故障受 100ms 预算约束，危险 replay 继续受保护。

## 本地验证

- `go test .\\internal\\service -count=1` 通过。
- repository、admin routes 聚焦测试及 `go test .\\... -run '^$'` 编译切片通过。
- Context Journal 聚焦测试、前端 Vitest 2 文件 5 用例、`pnpm run typecheck` 与 `pnpm run build` 通过。
- `git diff --cached --check` 在功能提交前通过；前端构建仅保留既有 Browserslist 与 chunk 告警。

## 发布验证

- PASS：从 committed HEAD `17d554024` 的 `git archive` 构建，镜像标签、OCI 标签与二进制启动日志均验证为主版本 `v0.1.157`、image version `v0.1.157.2`、commit `17d554024`。
- PASS：备份部署 `.env` 与 Nginx upstream 后，仅重建 idle blue；PostgreSQL、Redis 与 active green 未重启。
- PASS：blue 使用数据库 `32/16`、Redis `1024/128` 连接池；声明限额同步为 PostgreSQL `100`、Redis `10000`。
- PASS：候选 `18083` health、前端根页和主资源为 200；runtime metrics、admin system 与 `POST /v1/responses` 未认证均为 401。
- PASS：排除已证实的既有启动期快照清理超时后，blue 独立候选窗口 85 秒 healthy/restart 0，关键日志过滤 0。
- PASS：`nginx -t` 与 reload 成功，upstream 从 green 切至 blue；切流后 91 秒 blue healthy/restart 0，关键日志过滤 0。
- PASS：`8080`、`18081`、`18083` 三入口 health 均为 200，runtime metrics 与 Responses 未认证均为 401；主资源均为 `/assets/index-Bm5FLgAF.js`，SHA-256 均为 `4026C0A4AE06AF355139485C27A2C968EBCE9485FC7B028451C01106F181A294`。

## 回滚与边界

- green `sub2api:v0.1.157.1` 保持健康运行，作为即时回滚目标；upstream 备份为 `D:\\sub2api-deploy\\proxy\\upstreams\\active.conf.20260716-235129.bak`。
- 部署 `.env` 备份为 `D:\\sub2api-deploy\\.env.20260716-235129.bak`。
- 发现既有 `openai_request_snapshots` 清理任务以 10 秒预算删除过期记录时会超时；green 在本次发布前已出现 4 次相同日志。当前表约 3.5 GB、1,673 条记录均已过期，过期索引存在。本次不执行未授权的数据清理或 schema 修改，后续应单独处理保留策略、清理批次和表维护。
- 未执行 Git push 或镜像 registry push；未执行数据库写入、迁移、PostgreSQL 或 Redis 重启。
- `-race` 仍受本机 CGo 编译器缺失限制；认证管理端视觉验收因无可用登录态未执行。
