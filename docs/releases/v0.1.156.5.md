# sub2api v0.1.156.5 发布记录

- 日期：2026-07-16
- 执行者：Devil
- 功能提交：`2f23aa446de8`
- 修复提交：`cd446b9d4b3f`
- 镜像：`sub2api:v0.1.156.5`
- 镜像 ID：`sha256:09bc63f35c7c0655cbc3d2f02a28314d65ceecce0703e2e8a70c44791b397202`
- active：green；回滚：blue `sub2api:v0.1.156.3`

## 更新

1. 平台目录展示 NewAPI 与 Sub2 上游分组倍率。
2. NewAPI 同步兼容 `/api/user/self/groups`、`/api/user/groups`、`/api/pricing` 和旧端点，合并 `data` 与根级 `group_ratio` 后写入数据库缓存。
3. 修复独立签到页未携带动态 CSP nonce，导致线上主脚本被拦截的问题。
4. 页面默认仍只读数据库，仅手动同步或既有引用刷新流程请求上游。

## 验证

- 后端 service/admin/web 测试、Vitest 3/3、类型检查、生产构建和 diff check 通过。
- 从 committed HEAD `cd446b9d4b3f` 构建；OCI version=`v0.1.156.5`、revision=`cd446b9d4b3f9d709ea268145f143c0f57b9de20`；二进制主版本=`v0.1.156`。
- 仅重建 idle green；候选 `18082` 冒烟通过，Browser 确认页面脚本执行且 CSP 错误为 0。
- 候选与切流后各 60 秒、7 次采样均 healthy/restart 0、HTTP 200，新增关键日志 0。
- nginx 检查及 reload 通过；三入口主资源 SHA-256 均为 `4C4A1D4524DA642878FB87B28212BC875BF04E17F43F9B7EDE84B7657886BAB2`。
- 三入口去除动态 nonce 后的签到页 SHA-256 均为 `EE1B30C7373AC477F89F52BB09B8F59936D63CC58FCC093DBC1D974CB8BD1F55`。
- 管理配置与 `/v1/responses` 未登录均为 401；green `.5`、blue `.3`、PostgreSQL、Redis 均 healthy/restart 0。

## 回滚与边界

- upstream 备份：`D:\sub2api-deploy\proxy\upstreams\active.conf.bak-20260716-154021`
- 环境备份：`D:\sub2api-deploy\.env.bak-20260716-153750`
- 无管理员登录态，未在线点击真实 NewAPI 同步；协议解析、缓存和渲染由自动化测试覆盖。
- `.4` 因旧隔离脚本读取主版本 `v0.1.152` 且归档缺 Dockerfile，在构建阶段失败；未创建镜像、未部署且不复用。
- 未执行 Git push 或 registry push。
