# v0.1.134.16 Release Log

- 日期：2026-06-09
- 执行者：Devil
- Git 提交：`9f5fabc166df`
- 镜像标签：`sub2api:v0.1.134.16`
- 镜像 ID：`sha256:4b061b34ede99c260a9b51378b75ef231eb524d5b1ed6f5dc8eb831a2f016389`
- 构建标签：`org.opencontainers.image.version=v0.1.134.16`，`org.opencontainers.image.revision=9f5fabc166df`
- 发布前 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.11`，Health `healthy`
- 发布前 idle：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.15`，Health `healthy`
- 发布后 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.16`，Health `healthy`
- 回滚目标：`sub2api-blue` / `sub2api:v0.1.134.11`

## 更新内容

- 发布已提交的 OpenAI 网关修正：移除全局 `openai_oauth_compat_mode=codex_direct` 对 Codex CLI 模拟的影响。
- 保留账号级 `openai_codex_cli_simulation_enabled` 作为 Codex CLI 模拟开关，降低全局覆盖风险。
- 管理端版本徽标通过 `/api/v1/admin/system/check-updates` 展示 `image_version=v0.1.134.16`。

## 构建记录

- 构建来源：`git archive HEAD`，确保使用已提交状态。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.16 --label org.opencontainers.image.version=v0.1.134.16 --label org.opencontainers.image.revision=9f5fabc166df --build-arg COMMIT=9f5fabc166df --build-arg VERSION=v0.1.134 --build-arg IMAGE_VERSION=v0.1.134.16 -`
- 构建结果：成功生成 `sub2api:v0.1.134.16`。

## 候选验证

- 部署目标：idle `sub2api-green`，本机候选端口 `127.0.0.1:18082`。
- `GET http://127.0.0.1:18082/health`：200。
- `GET http://127.0.0.1:18082/`：200。
- `GET http://127.0.0.1:18082/admin`：200。
- `GET http://127.0.0.1:18082/api/v1/admin/accounts?page=1&page_size=1` 未登录：401。
- `POST http://127.0.0.1:18082/responses` 未登录：401。
- `GET http://127.0.0.1:18082/api/v1/settings/public`：200。
- `GET http://127.0.0.1:18082/api/v1/admin/system/check-updates` 管理鉴权：`current_version=v0.1.134`，`image_version=v0.1.134.16`，`build_type=release`。
- `sub2api-green` 60 秒稳定窗口：`Status=running`，`Health=healthy`。
- `sub2api-green` 自部署以来关键错误日志过滤：`panic|fatal|migration.*fail|checksum|pq:|bind:|address already in use|listen tcp|rebuild failed` 命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-blue:8080` 切到 `sub2api-green:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `GET http://127.0.0.1:8080/health`：200。
- `GET http://127.0.0.1:8080/`：200。
- `GET http://127.0.0.1:8080/admin`：200。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts?page=1&page_size=1` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401。
- `GET http://127.0.0.1:8080/api/v1/settings/public`：200。
- `GET http://127.0.0.1:8080/api/v1/admin/system/check-updates` 管理鉴权：`current_version=v0.1.134`，`image_version=v0.1.134.16`，`build_type=release`。

## 回滚

- 当前回滚容器：`sub2api-blue`，镜像 `sub2api:v0.1.134.11`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-blue:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- 本次发布冒烟覆盖健康、静态资源、鉴权边界、`/responses` 未授权边界、管理端版本可见性和关键日志过滤；未在切流后执行带真实业务 key 的上游付费请求。
