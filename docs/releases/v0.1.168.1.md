# v0.1.168.1 发布记录

- 日期：2026-07-29
- 执行者：Devil
- Git 提交：`aee30e8b9998307fd0a4997ab5d8c4d90ab1fad7`
- 镜像：`sub2api:v0.1.168.1`
- 镜像 ID：`sha256:f64e247971557f8daa5dccdf94aa1b46f3f0c1b98d8a24cb0ea449adec98d5d8`
- 正式颜色：green（`sub2api-green`）
- 回滚目标：blue（`sub2api:v0.1.166.3`）

## 功能与用户影响

- 签到工具的平台目录新增“新增平台”和“新增账号”弹窗，避免把管理表单直接堆叠在页面中。
- 新增平台保存 provider、平台名称和站点地址；新增账号先选择所属平台，保证账号写入既有站点/账号/Key/分组数据结构。
- NewAPI 账号填写 User ID 和 Key；Sub2API 账号填写邮箱和密码，登录后读取并保存完整 API Key 与分组缓存。
- 创建成功后重新读取 SQL 配置、Key 缓存与余额缓存，既有已引用账号置顶、高亮和标识逻辑保持不变。

## 发布过程

- 最新语义 tag 为 `v0.1.168`，不可变镜像后缀使用 `.1`。
- 从已提交 HEAD `aee30e8b9998307fd0a4997ab5d8c4d90ab1fad7` 的 `git archive` 构建；归档 `D:\sub2api-build\v0.1.168.1-aee30e8b9\source.tar` SHA-256 为 `C61828A616E977230C335CB2297794C836A864EFDA97F05F9E28D156BCF954BD`。
- OCI version/revision 与容器内二进制输出一致：主版本 `v0.1.168`、镜像版本 `v0.1.168.1`、提交 `aee30e8b9998307fd0a4997ab5d8c4d90ab1fad7`。
- 备份部署 `.env` 和 active upstream 后，只重建 idle green；active blue、PostgreSQL 和 Redis 在候选阶段未重建。
- 候选通过后执行 `nginx -t`，将 upstream 从 blue 切换到 green并 reload；blue 保留为即时回滚目标。

## 验证证据

- Go service/handler/routes 聚焦验证通过；前端 Vitest 4/4、typecheck 和生产构建通过，Vite 转换 953 个模块。
- 候选 `/health`、首页、主资源和 `/newapi-checkin/index.html` 为 200；未登录管理版本、签到配置接口及 `/v1/responses` 为 401。
- 候选独立观察 61.4 秒、13 次采样全部通过，green 始终 `healthy/restart=0`，启动以来及窗口内关键日志命中 0。
- 切流后 `8080`、`18081`、`18082` 主资源均为 `/assets/index-PtttmMa-.js`，SHA-256 均为 `393BAA7DDB5B59E62AB935072F1CD7E2F898D6646CC04BF5B5FC053EE8C64143`。
- 切流后独立观察 64.0 秒、13 轮四入口 health 全部 200；green/proxy 关键日志命中 0。
- blue 回滚、green active、PostgreSQL、Redis 均为 `healthy/restart=0`。

## 遗留边界

- 功能提交不含 migration/schema 文件；本次发布未执行数据库迁移或直接 SQL。
- 未通过生产凭据实际创建新平台或账号；创建流程由本地自动化测试和线上静态/接口契约验证覆盖。
- 未执行 Git push 或镜像 push。
