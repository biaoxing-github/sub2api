# v0.1.134.35 Release Log

- 日期：2026-06-12
- 执行者：Devil
- Git 提交：`59b93fa4585f`（`fix(admin): 修复调度池探测和真实余额计算`）
- 镜像标签：`sub2api:v0.1.134.35`
- 镜像 ID：`sha256:f48b5c709d4f846d8ab6f82c68c80f01a069c3242843588d7ce70940d8d086db`
- 构建标签：`org.opencontainers.image.version=v0.1.134.35`，`org.opencontainers.image.revision=59b93fa4585f`
- 发布前 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.34`，Health `healthy`
- 发布前 idle：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.33`，Health `healthy`
- 发布后 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.35`，Health `healthy`
- 回滚目标：`sub2api-blue` / `sub2api:v0.1.134.34`

## 更新内容

- 调度池 `manual-probe` JSON 接口改为后台内存 SSE 录制，不再把账号测试 SSE 写入真实响应体，HTTP 200 返回保持纯 JSON。
- OpenAI 调度池人工探测未传模型时，不再被后端统一兜底成 `claude-opus-4-8`，改为交给账号测试服务按平台使用 OpenAI 默认 `gpt-5.5`。
- OpenAI 账号探测流接入已有响应正文异常关键词检测器，避免上游 200 返回错误文本时被误判成功。
- NewAPI quota 单位余额解析补齐 `quota/used_quota/remain_quota/total_quota` 和 `data.user` 嵌套形态，按 `500000 = $1` 换算真实余额。

## 代码验证

- GREEN：`go test -tags unit ./internal/handler/admin -run "TestAccountHandler_ManualProbe|TestManualProbe" -count=1` 通过。
- GREEN：`go test -tags unit ./internal/service -run "TestParseUpstreamBalanceResponse_(OpenAICreditGrants|NewAPIUserSelf|NewAPIQuotaUnits|NewAPIQuotaUnitsNestedUser|Sub2APIUsage|NewAPITokenUsage)|TestParseNewAPIUserSelfResponse|TestParseNewAPIUsageResponseUserBalance|TestUpstreamBalanceServiceRefreshOne|TestAccountTestService_OpenAIResponseTextErrorInterceptsProbe" -count=1` 通过。
- GREEN：`npm run test:run -- src/views/admin/__tests__/AccountSchedulingPoolView.spec.ts` 通过，1 file / 7 tests passed。
- GREEN：`npm run typecheck` 通过。
- GREEN：`git diff --cached --check` 通过；提交后本轮代码 diff 无空白错误。
- 已知非本轮失败：`go test -tags unit ./internal/handler/admin ./internal/service -count=1` 中 `internal/handler/admin` 通过，`internal/service` 仍失败在既有 OpenAI gateway 测试：`TestOpenAINonStreamingConfiguredResponseTextReturnsFailover`、`TestOpenAIGatewayServiceForward_CodexBridgePermission403RetriesWithoutBridge`、OAuth passthrough client-cancel / originator 断言、`TestOpenAIGatewayService_OpenAIPassthrough_429And529TriggerFailover` panic。

## 构建记录

- 构建来源：`git archive HEAD`，HEAD=`59b93fa4585f`。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.35 --label org.opencontainers.image.version=v0.1.134.35 --label org.opencontainers.image.revision=59b93fa4585f --build-arg COMMIT=59b93fa4585f --build-arg IMAGE_VERSION=v0.1.134.35 -`
- 构建结果：成功生成 `sub2api:v0.1.134.35`；前端生产构建使用缓存，后端 release build 通过。
- 镜像确认：`docker image inspect sub2api:v0.1.134.35` 返回 `Version=v0.1.134.35`、`Revision=59b93fa4585f`。
- 二进制确认：`/app/sub2api --version` 显示 `Sub2API 0.1.134 (image: v0.1.134.35, commit: 59b93fa4585f, built: 2026-06-12T03:51:52Z)`。

## 候选验证

- 部署目标：idle `sub2api-green`，本机候选端口 `127.0.0.1:18082`。
- 部署动作：仅以 `SUB2API_GREEN_IMAGE=sub2api:v0.1.134.35` 重建 `sub2api-green`，未重启 active `sub2api-blue`、PostgreSQL 或 Redis。
- `sub2api-green` Health `healthy` 持续超过 60 秒，镜像 `sub2api:v0.1.134.35`。
- `GET http://127.0.0.1:18082/health`：200。
- `GET http://127.0.0.1:18082/`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:18082/api/v1/admin/system/version` 未登录：401。
- `GET http://127.0.0.1:18082/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:18082/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:18082/v1/messages` 未登录：401。
- 候选日志过滤：`panic|fatal|migration.*fail|checksum|pq:|bind:|address already in use|listen tcp|rebuild failed` 命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-blue:8080` 切到 `sub2api-green:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `GET http://127.0.0.1:8080/health`：200。
- `GET http://127.0.0.1:8080/`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:8080/api/v1/admin/system/version` 未登录：401。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401。
- `POST http://127.0.0.1:8080/v1/messages` 未登录：401。
- 切流后容器状态：`sub2api-green` Health `healthy`，`sub2api-blue` Health `healthy` 且继续运行。
- 切流后 `sub2api-green` 与 `sub2api-proxy` 关键错误日志过滤：命中 0。

## 回滚

- 当前回滚容器：`sub2api-blue`，镜像 `sub2api:v0.1.134.34`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-blue:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- `D:\sub2api-deploy\.env` 中 `ADMIN_PASSWORD` 为空，本轮无法执行登录态点击复测；已用后端 handler 回归测试覆盖纯 JSON 响应和 OpenAI 默认模型，用前端回归测试覆盖调度池模型选择，用入口静态资源冒烟确认新镜像资源可访问。
- 已存在的 `internal/service` OpenAI gateway 宽包测试失败未在本轮修复，和人工探测 JSON 响应、OpenAI 空模型兜底、NewAPI quota 单位余额解析无直接依赖。
- 已缓存的旧余额快照需要下一次余额刷新或手动刷新后，才会按新换算逻辑更新显示。

## 用户反馈复核

- 2026-06-12 12:21 +08:00 复核线上 active：代理仍指向 `sub2api-green:8080`，`sub2api-green` 镜像为 `sub2api:v0.1.134.35`，二进制版本输出 `image: v0.1.134.35, commit: 59b93fa4585f`。
- 复核调度池人工探测：后端 `ManualProbe` 已调用 `TestAccountConnectionWithResultBackground`，返回体保持 `{success,result,account}`；前端读取 `result.result` 并通过可选链处理失败消息，避免 200 JSON 被 SSE 写坏或字段大小写不一致导致显示“探测失败”。
- 复核上游 200 错误文本：OpenAI 探测流已接入响应文本异常关键词检测，测试 `TestAccountTestService_OpenAIResponseTextErrorInterceptsProbe` 在干净 HEAD 上通过，避免上游 HTTP 200 但正文为错误文本时误判成功。
- 复核真实余额：NewAPI quota 单位测试 `TestParseUpstreamBalanceResponse_NewAPIQuotaUnits` 与 `TestParseUpstreamBalanceResponse_NewAPIQuotaUnitsNestedUser` 在干净 HEAD 上通过，`quota/used_quota/remain_quota/total_quota` 按 `500000 = $1` 换算。
