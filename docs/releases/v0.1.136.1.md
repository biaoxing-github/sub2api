# sub2api v0.1.136.1 发布记录

- 日期：2026-06-17
- 执行者：Devil
- Git 提交：d19b309c8
- 镜像标签：sub2api:v0.1.136.1
- active/idle：发布前 active=green(sub2api:v0.1.134.48)，idle=blue(sub2api:v0.1.134.47)；发布后 active=blue(sub2api:v0.1.136.1)，rollback=green(sub2api:v0.1.134.48)

## 更新内容

- 将主版本源从 `0.1.134` 调整为 `0.1.136`。
- 保持镜像小版本作为独立 `image_version` 暴露，构建时注入 `v0.1.136.1`。
- 管理端点击侧边栏 Sub2API 版本后，继续在下拉详情中展示镜像小版本；按钮本体只展示主版本。
- 本轮未修改页面样式。

## 验证结果

- `go test ./internal/service -run TestUpdateServiceCheckUpdateExposesImageVersionSeparately -count=1`：通过。
- `go test ./cmd/server -run TestNoSuchTest -count=1`：通过。
- `corepack pnpm vitest run src/components/common/__tests__/VersionBadge.spec.ts src/stores/__tests__/app.spec.ts`：通过，2 files / 24 tests。
- `corepack pnpm typecheck`：通过。
- `git diff --check`：通过，仅有既有 CRLF warning。
- 构建：`git archive HEAD | docker build --pull=false -t sub2api:v0.1.136.1 --build-arg VERSION=0.1.136 --build-arg IMAGE_VERSION=v0.1.136.1 --build-arg COMMIT=d19b309c89dc -`：通过。
- 镜像检查：`docker image inspect sub2api:v0.1.136.1` 显示 `org.opencontainers.image.version=v0.1.136.1`、`org.opencontainers.image.revision=d19b309c89dc`。
- 二进制版本检查：`docker run --rm sub2api:v0.1.136.1 /app/sub2api -version` 与 `docker exec sub2api-blue /app/sub2api -version` 均显示 `Sub2API 0.1.136 (image: v0.1.136.1, commit: d19b309c89dc)`。
- 候选 blue 18083：`/health` 200，首页 200，未登录 `/api/v1/admin/accounts` 401，未登录 `/responses` 401，未登录 `/v1/responses` 401。
- 候选 blue 健康：`docker inspect sub2api-blue` 显示 `ConfigImage=sub2api:v0.1.136.1`、`Health=healthy`；70 秒观察窗口关键日志命中数为 0。
- 代理切流：`active.conf` 从 `sub2api-green:8080` 切到 `sub2api-blue:8080`，`docker exec sub2api-proxy nginx -t` 与 `nginx -s reload` 成功。
- 切流后入口：8080 与 18081 `/health` 200、首页 200、未登录 `/api/v1/admin/system/version` 401、未登录 `/responses` 401。
- 浏览器验证：Playwright 打开 `http://127.0.0.1:8080/` 跳转到 `/home`，页面标题为 `Home - Sub2API`，可访问性快照显示页面非空。

## 回滚目标

- 秒级回滚：将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 切回 `sub2api-green:8080` 并 reload nginx。
- 回滚镜像：sub2api:v0.1.134.48。

## 遗留风险

- 未登录管理版本接口按预期返回 401；页面下拉中的 `image_version` 展示已由组件测试覆盖，线上匿名浏览器验证只确认首页加载。
- 旧 green 容器 `sub2api:v0.1.134.48` 保留作为回滚目标。
