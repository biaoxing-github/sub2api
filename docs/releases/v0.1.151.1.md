# sub2api v0.1.151.1 发布记录

- 日期：2026-07-12
- 执行者：Devil
- 主版本：`v0.1.151`
- 不可变镜像：`sub2api:v0.1.151.1`
- Git 提交：`92ba858d98e8`
- 发布前 active：`sub2api-green:8080`，`sub2api:v0.1.150.2`
- 发布后 active：`sub2api-blue:8080`，`sub2api:v0.1.151.1`
- 回滚目标：`sub2api-green:8080`，`sub2api:v0.1.150.2`

## 详细更新日志

1. GPT-5.6 计费与模型归一：计费服务和价格资源加入 Sol/Terra/Luna 的 cache-write 价格与 272,000 token 长上下文倍率；bare、max、reasoning/date 后缀统一归一到 Sol。
2. cache-write 用量解析：Responses、Chat Completions、Messages bridge 和 WS v2 统一识别嵌套字段；嵌套显式 0 覆盖旧顶层 fallback。
3. Codex 客户端身份：OAuth HTTP、透传、WebSocket 和用量探针按最终 User-Agent 配对 `originator`，version 最低为 `0.144.0`；Messages bridge 保留兼容形态。
4. Fast/Flex 用户策略：规则新增 `user_ids`，用户规则优先全局规则；管理端支持编辑并校验正数及重复 ID。
5. image_gen、Grok 和 setup-token：统一清理 image generation 工具声明；Grok 保留 reasoning effort；setup-token 纳入后台刷新。
6. 展示、统计与版本：模型列表和 OpenCode 配置增加 `gpt-5.6 (Sol)`；request_type 过滤兼容 legacy 行；主版本更新为 `v0.1.151`，镜像版本为 `v0.1.151.1`。
7. 数据库迁移：migration 173 允许 `usage_logs.request_type=4`，用于 cyber-policy blocked 审计。

## 验证结果

- PASS：`go test ./internal/service -count=1`。
- PASS：相关 pkg、repository、WS v2、middleware、admin handler、DTO、migrations 和 cmd/server 测试。
- PASS：`npm run test:run -- --reporter=dot` 与 `npm run typecheck`。
- PASS：migration 173 已在运行中的 PostgreSQL 定向执行，约束现场为 `request_type IN (0,1,2,3,4) NOT VALID`。
- PASS：从 committed HEAD `92ba858d98e8` 构建不可变镜像；OCI version=`v0.1.151`、revision=`92ba858d98e8`，二进制报告 image version=`v0.1.151.1`。
- PASS：仅重建 idle blue，候选 `18083` 的 health/home/静态资源为 200，管理 API、`/responses`、`/v1/responses` 未登录均为 401。
- PASS：候选观察超过 75 秒，blue 持续 healthy、restart 0；启动后一次 `pq: canceling statement due to user request` 未重复，后续独立 120 秒关键日志窗口为 0。
- PASS：`nginx -t` 和 reload 成功，upstream 从 green 切到 blue；`8080`、`18081`、`18083` 完整冒烟通过。
- PASS：切流后观察 68 秒，三个健康入口持续 200，blue healthy、restart 0，最近 120 秒关键日志为 0。
- PASS：green `sub2api:v0.1.150.2` 保持 healthy、restart 0，作为回滚目标；PostgreSQL 和 Redis 未重启。

## 遗留风险

- 未执行真实认证上游计费请求；计费由解析、价格和长上下文回归测试覆盖。
- blue 启动后约 10 秒出现过一次 request snapshot 清理 SQL 被取消，未持续复现，独立干净窗口和线上观察均通过。
- 未执行 Git push或镜像 registry push。
- 未执行管理员登录态页面操作或真实认证上游请求。
