# v0.1.177.1 发布记录

- 日期：2026-08-17
- 执行者：Devil
- 状态：已部署并验证

## 发布内容

- 将上游 `v0.1.177` 的分组用量日汇总、remote compaction v2、Codex turn-state 与指纹 opt-in，以及 Grok 媒体模型计费修正发布到生产环境。
- 功能提交：`733b54af8ac8fd1eb077e8a93351a5222a2e5559`。
- 源码由 committed HEAD 的 `git archive` 生成，工作树中 routing hint、Gemini、Mihomo、依赖锁和诊断文件等未提交内容未进入镜像。

## 数据库迁移

- 通过运行中的 PostgreSQL、`ON_ERROR_STOP=1` 和应用同款 advisory lock 手工执行 migration 222/223；未重建 PostgreSQL 容器。
- `222_group_usage_daily_rollups.sql`：`d1dea80dd961e8f4016ce95dc413ee28344adf6fce9964427452a95cb7dfc16f`。
- `223_group_usage_rollup_timezone.sql`：`68ab749e218a893f7c54d856510c8f8eb462d7ce4d8e300d5fa8b6a17cf631cc`。
- 最终状态：`timezone_name=Asia/Shanghai`，发布水位 `closed_before=2026-08-17`，生成 167 个日桶，范围 `2026-05-21..2026-08-16`。

## 镜像身份

- 主版本：`v0.1.177`
- 不可变镜像：`sub2api:v0.1.177.1`
- ImageID：`sha256:0f3994a90d8dcbb4f73593c01d5da43aa3971c480fa73af1127de3e37c1c5656`
- OCI version：`v0.1.177`
- OCI revision：`733b54af8ac8fd1eb077e8a93351a5222a2e5559`
- 二进制身份：`Sub2API v0.1.177 (image: v0.1.177.1, commit: 733b54af8ac8fd1eb077e8a93351a5222a2e5559)`
- 源码归档 SHA-256：`019C6248E19D1E95732ABBBA4D2A33BC51FA6EAFEE855D5D6A7C077BC5085732`

## 蓝绿部署

- 发布前 active：green `sub2api:v0.1.176.2`
- 候选：blue `sub2api:v0.1.177.1`
- 发布后 active：blue `sub2api:v0.1.177.1`
- 回滚目标：green `sub2api:v0.1.176.2`
- upstream：`server sub2api-blue:8080;`
- upstream 备份：`D:\sub2api-deploy\backups\active.conf.before-v0.1.177.1.20260817-113216`

## 验证证据

- 候选 `18083`：`/health`、首页和主静态资源均为 `200`；未登录管理 API、`/responses` 和 `/v1/responses` 均为 `401`。
- 候选独立观察：60.8 秒、13/13 健康，restart `0`，关键日志 `0`。
- `nginx -t` 通过后 reload 成功，加载后的 upstream 明确指向 blue。
- 切流后独立观察：62.1 秒，`8080`、`18081`、`18083` 和回滚 `18082` 共 13/13 轮健康，blue 与 proxy 关键日志均为 `0`。
- 三个新版本入口主资源：`/assets/index-C2--WY6n.js`，SHA-256 均为 `BADCA1FEE3DF4A45CB37F68D30A767EBA8EB83BBE02E222E6FA462E9BAA36735`。
- blue healthy/restart `0`；green 回滚健康；PostgreSQL、Redis healthy/restart `0`。
- OCI 标签、镜像 ID、容器内二进制身份和 committed HEAD 逐字一致。

## 回滚与遗留

- 回滚时将 `active.conf` 恢复为 `server sub2api-green:8080;`，执行 `nginx -t` 后 reload。
- 受保护的 `/api/v1/admin/system/version` 与管理端版本下拉未完成登录态验证：部署 `.env` 无可用管理员明文凭据，内置浏览器也无现成登录态；本次未伪造认证结果。
- `deploy/release-bluegreen.ps1` dry-run 仍把 12 位短 SHA 传给镜像，未用于本次构建；后续应单独修正为完整提交 SHA。
- 未执行 Git push 或镜像 push。
