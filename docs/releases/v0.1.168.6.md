# v0.1.168.6 发布记录

- 日期：2026-07-31
- 执行者：Devil
- 功能：修复 Grok 账号平台误显示为 Gemini

## 用户影响

- Grok API Key 和 OAuth 账号的平台徽标显示为 Grok，不再落入 Gemini 默认分支。
- `grok`、`xai`、大小写及首尾空格差异统一显示为 Grok；未知平台保留原始名称并使用中性样式。
- Grok 创建契约保持 `platform=grok`、`type=apikey`、`base_url=https://api.x.ai/v1`，没有数据库 schema 或 migration 变更。

## 代码与构建

- 功能提交：`886b497fcd0dde4e3b13b8af8c42c5eb178f4a19`
- 最新语义化 tag：`v0.1.168`
- 镜像：`sub2api:v0.1.168.6`
- 镜像 ID：`sha256:7ad8efbc249df137a933fcad42c52aa93eeba60bd4b8f1fd5550ed156d995aec`
- OCI revision：`886b497fcd0dde4e3b13b8af8c42c5eb178f4a19`
- OCI version：`v0.1.168`
- 二进制身份：`Sub2API v0.1.168 (image: v0.1.168.6, commit: 886b497fcd0dde4e3b13b8af8c42c5eb178f4a19)`
- `git archive HEAD` 归档：`D:\sub2api-deploy\build\v0.1.168.6-886b497f.tar`
- 归档 SHA-256：`0D53C4A0BEC9387A68D816E7CFF9680A54DA7C63965182C06207F3926C7AC91C`
- 构建上下文从上述归档重新解压，共 2853 个文件；Dockerfile 和四个功能文件的 Git blob 与提交一致。

## 部署

- 部署方式：蓝绿发布，只重建 idle blue 应用容器。
- 正式颜色：blue，`sub2api-blue` 使用 `sub2api:v0.1.168.6`。
- 回滚目标：green，`sub2api-green` 保留 `sub2api:v0.1.168.5`。
- active upstream：`sub2api-blue:8080`。
- 部署前备份：
  - `D:\sub2api-deploy\backups\.env.before-v0.1.168.6-20260731-113129.bak`
  - `D:\sub2api-deploy\backups\active.conf.before-v0.1.168.6-20260731-113129.bak`
- 新旧 upstream 的 `nginx -t` 均通过，reload 成功。
- PostgreSQL、Redis 未重启，均保持 healthy/restart 0。

## 验证证据

- 聚焦 Vitest 6/6、全量前端 Vitest、`pnpm run typecheck` 和 974 modules 生产构建均通过。
- blue 候选 smoke：`18083/health`、首页、主 JS 资源均 200；未登录 `/api/v1/admin/system/version` 和 `/v1/responses` 均 401；healthy/restart 0；启动期关键日志 0。
- 候选独立观察：63.0 秒、13 次采样的候选和当前公网 health 全部 200；blue 始终 healthy/restart 0，窗口关键日志 0。
- 切流后入口验证：`8080`、`18081`、`18083` 均 health/home/asset 200，主资源 `/assets/index-BKWgQvnT.js` 和 SHA-256 `0E3181EEBC4579985E32B7852A2970DE13AEC9B4473F4A5F6FBA56838B765E03` 一致，鉴权契约保持 401。
- 切流后独立观察：70.0 秒、13 轮 `8080`、`18081`、`18083`、`18082` health 全部 200；blue、green、PostgreSQL、Redis 均 healthy/restart 0，代理 running/restart 0；blue/proxy 关键日志 0。
- 已登录 Chrome 管理端显示主版本 `v0.1.168` 和镜像版本 `v0.1.168.6`；新增账号弹窗选择 Grok 后显示“Grok 账户授权”，提供 OAuth 与 xAI API Key 类型。
- 浏览器应用自身控制台 error/warn 为 0；两条告警来自 Chrome 扩展的 Tauri 环境提示，已排除为应用日志。

## 遗留风险

- 生产库当前 52 个账号中没有 Grok 平台账号，因此没有创建真实账号来验证列表中的生产 Grok 行；本地组件与浏览器回归已覆盖 Grok/xAI 徽标和创建请求。
- 未执行生产数据库写入、Git push 或镜像仓库 push。
