# sub2api v0.1.156.9 发布记录

- 日期：2026-07-16
- 执行者：Devil
- 功能合并提交：`55301cc1d4e2`
- 主版本修正提交：`e003548fb357`
- 根模型路由修复提交：`5c697f087ce6`
- 构建提交：`962d2db5ff24`
- 镜像：`sub2api:v0.1.156.9`
- 镜像 ID：`sha256:028c516d5b5e05eaa6f99942d8c38ad52eb7e873f69dee60549a2c830e19cc00`
- active：blue；回滚：green `sub2api:v0.1.156.5`
- 当前状态：已构建、部署、切流并完成线上观察

## 更新

1. 融入 v0.1.156 批次 1-3 的成本、failover、Responses、SSE/WS、Anthropic、图片尺寸、Antigravity 和会话 seed 更新。
2. 嵌入式主版本更新为 `v0.1.156`。
3. 根 `/models` 通过嵌入前端放行列表进入与 `/v1/models` 相同的 API Key 中间件和模型处理器。

## 本地验证

- service、apicompat、handler、routes、config、前端 Antigravity 和全仓编译切片通过。
- 嵌入前端 `/models` RED 测试复现 SPA 截获，最小修正后两套中间件完整测试通过。
- `.7` 运行时契约失败后已回滚；`.8` 网络构建失败且未生成镜像或部署。
- Docker 内 TUNA/USTC Alpine 索引及 npm registry 前置检查通过。

## 发布验证

- PASS：从 committed HEAD `962d2db5ff24` 构建；OCI version=`v0.1.156`、revision=`962d2db5ff24`；二进制 image_version=`v0.1.156.9`。
- PASS：仅重建 idle blue；health、静态资源为 200，管理 API、Responses、根 `/models` 与 `/v1/models` 未登录均为 401，模型别名错误体一致。
- PASS：启动期快照清理命中一次 `pq:` 后重建独立候选窗口；71.6 秒、8 次采样均 healthy/restart 0，模型路由持续 401，关键日志 0。
- PASS：备份 upstream 后执行 nginx 检查和 reload，代理从 green 切到 blue。
- PASS：切流后 73.7 秒、8 轮三入口健康均为 200，blue/proxy 关键日志 0；管理、Responses 和模型别名契约通过。
- PASS：`8080`、`18081`、`18083` 主资源均为 `/assets/index-DxZ-88Ol.js`，SHA-256 均为 `CE95EF8958D5457A22742EBAA7086D1652306E9A7769EFCC06F4F9A2D037DF98`。
- PASS：blue `.9`、green `.5`、PostgreSQL、Redis 均 healthy/restart 0。

## 回滚与边界

- 旧 active green `sub2api:v0.1.156.5` 保留为回滚目标，不重建。
- `.6`、`.7`、`.8` 均为失败候选，不覆盖、不复用。
- upstream 备份：`D:\sub2api-deploy\proxy\upstreams\active.conf.bak-20260716-165501`。
- 无新增迁移，不写数据库，不重启 PostgreSQL 或 Redis。
- 未执行 Git push 或 registry push。
