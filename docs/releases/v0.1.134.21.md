# v0.1.134.21 Release Log

- 日期：2026-06-09
- 执行者：Devil
- Git 提交：`d017b05e786f`
- 镜像标签：`sub2api:v0.1.134.21`
- 镜像 ID：`sha256:3809181df0df1438d1864aa45de54c2be7e2cb53e0a9f5190c3161a056aa3ba4`
- 构建标签：`org.opencontainers.image.version=v0.1.134.21`，`org.opencontainers.image.revision=d017b05e786f`
- 发布前 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.20`，Health `healthy`
- 发布前 idle：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.19`，Health `healthy`
- 发布后 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.21`，Health `healthy`
- 回滚目标：`sub2api-green` / `sub2api:v0.1.134.20`

## 更新内容

- 调度池展示账号仍使用调度快照账号，但读取 OpenAI APIKey path-health 时按账号 ID 获取完整账号，用完整 `base_url` / `request_base_urls` 计算 health key。
- 修复真实 `/responses` 样本写入自定义 upstream，而调度池读取默认 upstream 导致 `samples` 长期为 0 的问题。
- 保留 v0.1.134.20 中通用 `GatewayService.RecordUsage` 写入共享 tracker 的修复。

## 构建记录

- 构建来源：`git archive HEAD`，确保使用已提交状态。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.21 --label org.opencontainers.image.version=v0.1.134.21 --label org.opencontainers.image.revision=d017b05e786f --build-arg COMMIT=d017b05e786f --build-arg VERSION=v0.1.134 --build-arg IMAGE_VERSION=v0.1.134.21 -`
- 构建结果：成功生成 `sub2api:v0.1.134.21`。
- 镜像确认：`docker image inspect sub2api:v0.1.134.21` 返回 `Version=v0.1.134.21`、`Revision=d017b05e786f`。

## 候选验证

- 部署目标：idle `sub2api-blue`，本机候选端口 `127.0.0.1:18083`。
- `GET http://127.0.0.1:18083/health`：200。
- `GET http://127.0.0.1:18083/`：200。
- `GET http://127.0.0.1:18083/admin`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:18083/api/v1/settings/public`：200。
- `GET http://127.0.0.1:18083/api/v1/admin/accounts?page=1&page_size=1` 未登录：401。
- `POST http://127.0.0.1:18083/responses` 未登录：401。
- `GET http://127.0.0.1:18083/api/v1/admin/system/version` 管理鉴权：`version=v0.1.134`，`image_version=v0.1.134.21`。
- `GET http://127.0.0.1:18083/api/v1/admin/groups/all` 管理鉴权：200，找到分组“自用”。
- `GET http://127.0.0.1:18083/api/v1/admin/accounts/scheduling-pool?platform=openai&group=2` 管理鉴权：200，`total=3`，`schedulable_count=3`，`degraded_count=0`，`blocked_count=0`。
- 候选调度池 health key upstream：`https://ai2.hhhl.cc/v1`、`https://api.denxio.top`、`https://api.denxio.top`。
- `sub2api-blue` 60 秒稳定窗口：`Status=running`，`Health=healthy`，镜像 `sub2api:v0.1.134.21`。
- `sub2api-blue` 关键错误日志过滤：`panic|fatal|migration.*fail|checksum|pq:|bind:|address already in use|listen tcp|rebuild failed` 命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-green:8080` 切到 `sub2api-blue:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `D:\sub2api-deploy\docker-compose.blue.yml` 默认镜像同步为 `sub2api:v0.1.134.21`。
- `GET http://127.0.0.1:8080/health`：200。
- `GET http://127.0.0.1:8080/`：200。
- `GET http://127.0.0.1:8080/admin`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:8080/api/v1/settings/public`：200。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts?page=1&page_size=1` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401。
- `GET http://127.0.0.1:8080/api/v1/admin/system/version` 管理鉴权：`version=v0.1.134`，`image_version=v0.1.134.21`。
- OpenAI 调度池 group=2：`total=3`，`schedulable_count=3`，`degraded_count=0`，`blocked_count=0`，health key upstream 为真实自定义 upstream。
- Anthropic 调度池 group=2：`total=0`，`items=0`。
- 切流后 DB 对照：`2026-06-09 22:26:48+08` 后账号 `free6` 有 5 次 OpenAI usage log，5 次均有 `first_token_ms`。
- 切流后调度池最终快照：`free6` `samples=14`，`success_count=14`，`ttft_ewma_ms=14900.55`，upstream=`https://ai2.hhhl.cc/v1`。
- `sub2api-blue` 和 `sub2api-proxy` 切流后关键错误日志过滤：命中 0。

## 回滚

- 当前回滚容器：`sub2api-green`，镜像 `sub2api:v0.1.134.20`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-green:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- v0.1.134.21 已用真实线上流量验证调度池样本增长；`dengxian-4` / `dengxian-5` 在最终快照时尚未获得新样本，等待后续真实调度后会按相同 key 口径增长。
