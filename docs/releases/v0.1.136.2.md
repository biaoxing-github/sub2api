# sub2api v0.1.136.2 发布记录

- 日期：2026-06-18
- 执行者：Devil
- 镜像源 Git 提交：50eb8b82f516
- 镜像标签：sub2api:v0.1.136.2
- active/idle：发布前 active=blue(sub2api:v0.1.136.1)，idle=green(sub2api:v0.1.134.48)；发布后 active=green(sub2api:v0.1.136.2)，rollback=blue(sub2api:v0.1.136.1)

## 更新内容

- 新增本机 Codex CLI 版本读取器，启动时执行 `codex --version` 并缓存版本；本机当前输出为 `codex-cli 0.140.0`。
- OpenAI Codex 模拟请求头从静态 `0.138.0` 改为读取缓存版本，覆盖 Codex Desktop User-Agent、compact/probe/WS 相关请求和默认 `codex_cli_rs` fallback。
- 补完 Claude Code 版本同步：修正 npm `/@anthropic-ai/claude-code/latest` 的 `version` 字段解析，同时保留 `dist-tags.latest` fallback。
- Claude Code OAuth 请求头与 billing attribution 改为读取当前缓存版本，避免 User-Agent 与 `cc_version` 不一致。
- 主服务与 setup wizard 启动时都会启动 Codex/Claude 版本同步；Codex 首次同步为启动阶段同步执行，避免刚启动时继续使用旧版本。
- 补齐 `mockAccountRepoForGemini` 测试桩，使干净 HEAD 上的 service 聚焦测试不再依赖本机未跟踪文件。

## 验证结果

- `go test ./internal/pkg/openai ./internal/pkg/claude`：通过。
- `go test ./cmd/server`：通过。
- 干净临时 worktree 套用暂存区 patch 后，`go test ./internal/service -run "TestOpenAICodexCLISimulationUsesLatestClientVersion|TestApplyOpenAICodexLatestClientHeadersMatchesCapturedClientShape|TestAccountTestService_TestAccountConnection_OpenAICompactOAuthSuccessPersistsSupport|TestOpenAIGatewayService_Forward_LogsInstructionsRequiredDetails|TestOpenAIGatewayService_Forward_TransientProcessingErrorTriggersFailover"`：通过。
- `git diff --cached --check`：通过。
- 完整 `go test ./internal/service`：未通过，失败点为既有基线问题：
  - `TestAccountDisableAPIKeyWritesDisabledUntilAndCount` 的 rate_limited 时间断言差异。
  - `TestOpenAIGatewayService_Forward_WSv2ErrorEventUsageLimitPersistsRateLimit` 在测试桩路径触发 nil repo panic。
- 构建：`git archive HEAD | docker build --pull=false -t sub2api:v0.1.136.2 --build-arg VERSION=0.1.136 --build-arg IMAGE_VERSION=v0.1.136.2 --build-arg COMMIT=50eb8b82f516 -`：通过。
- 镜像检查：`docker image inspect sub2api:v0.1.136.2` 显示 `org.opencontainers.image.version=v0.1.136.2`、`org.opencontainers.image.revision=50eb8b82f516`。
- 二进制版本检查：`docker run --rm sub2api:v0.1.136.2 /app/sub2api -version` 与 `docker exec sub2api-green /app/sub2api -version` 均显示 `Sub2API 0.1.136 (image: v0.1.136.2, commit: 50eb8b82f516)`。
- 候选 green 18082：`/health` 200，首页 200，未登录 `/api/v1/admin/accounts` 401，未登录 `/responses` 401，未登录 `/v1/responses` 401。
- 候选 green 健康：`docker inspect sub2api-green` 显示 `ConfigImage=sub2api:v0.1.136.2`、`Health=healthy`、`RestartCount=0`；70 秒观察窗口关键日志命中数为 0。
- 代理切流：`active.conf` 从 `sub2api-blue:8080` 切到 `sub2api-green:8080`，`docker exec sub2api-proxy nginx -t` 与 `nginx -s reload` 成功。
- 切流后入口：8080 与 18081 `/health` 200、首页 200、未登录 `/api/v1/admin/system/version` 401、未登录 `/responses` 401、未登录 `/v1/responses` 401。
- 切流后 green 继续 `Health=healthy`、`RestartCount=0`，70 秒观察窗口关键日志命中数为 0。
- 浏览器验证：Playwright 打开 `http://127.0.0.1:8080/` 跳转到 `/home`，页面标题为 `Home - Sub2API`，可访问性快照显示页面非空。

## 回滚目标

- 秒级回滚：将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 切回 `sub2api-blue:8080` 并 reload nginx。
- 回滚镜像：sub2api:v0.1.136.1。

## 遗留风险

- 完整 service 测试包仍有上述两个既有失败，发布依据是干净 HEAD 聚焦测试、构建、候选验证和切流后冒烟均通过。
- Playwright 观察到首页存在既有 CSP inline script console error，页面仍正常跳转到 `/home` 并渲染；本轮未修改前端静态资源和 CSP。
- 旧 blue 容器 `sub2api:v0.1.136.1` 保留作为回滚目标。
