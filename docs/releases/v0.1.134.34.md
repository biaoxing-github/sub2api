# v0.1.134.34 Release Log

- 日期：2026-06-12
- 执行者：Devil
- Git 提交：`c7b059f169c5`（`fix(admin): 调整 Anthropic 默认测试模型`）
- 包含上一线上版本后的提交范围：`529d33b3b371..c7b059f169c5`
- 镜像标签：`sub2api:v0.1.134.34`
- 镜像 ID：`sha256:3b3bd45128d53726781d729ddf9e45e763b96d39ad6e9d78d58a8a341a0504e7`
- 构建标签：`org.opencontainers.image.version=v0.1.134.34`，`org.opencontainers.image.revision=c7b059f169c5`
- 发布前 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.33`，Health `healthy`
- 发布前 idle：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.32`，Health `healthy`
- 发布后 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.34`，Health `healthy`
- 回滚目标：`sub2api-green` / `sub2api:v0.1.134.33`

## 更新内容

- 保留 `v0.1.134.33` 的人工探测结果契约修复，线上 `manual-probe` 返回前端读取的小写字段。
- 账号页 Anthropic 测试弹窗默认模型改为优先 `claude-opus-4-8`。
- 调度池 Anthropic 人工探测复用账号页默认模型规则，提交 `claude-opus-4-8` 而不是 sonnet。

## 构建记录

- 构建来源：`git archive HEAD`，确保使用已提交状态。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.34 --label org.opencontainers.image.version=v0.1.134.34 --label org.opencontainers.image.revision=c7b059f169c5 --build-arg COMMIT=c7b059f169c5 --build-arg IMAGE_VERSION=v0.1.134.34 -`
- 构建结果：成功生成 `sub2api:v0.1.134.34`；前端生产构建与后端 release build 均通过。
- 构建观察：Vite 输出既有 dynamic import / chunk size / Browserslist warning，无构建失败。
- 镜像确认：`docker image inspect sub2api:v0.1.134.34` 返回 `Version=v0.1.134.34`、`Revision=c7b059f169c5`、镜像 ID `sha256:3b3bd45128d53726781d729ddf9e45e763b96d39ad6e9d78d58a8a341a0504e7`。

## 代码验证

- GREEN：`npm run test:run -- src/components/admin/account/__tests__/AccountTestModal.spec.ts src/components/account/__tests__/AccountTestModal.spec.ts src/views/admin/__tests__/AccountSchedulingPoolView.spec.ts` 通过，3 files / 19 tests passed。
- GREEN：`go test -tags unit ./internal/handler/admin -run "TestAccountHandler_ManualProbe|TestManualProbeResultResponseFromService" -count=1` 通过。
- GREEN：`npm run typecheck` 通过。
- GREEN：`git diff --check` 通过；仅提示既有 `.codegraph/daemon.pid` 换行告警。

## 候选验证

- 部署目标：idle `sub2api-blue`，本机候选端口 `127.0.0.1:18083`。
- 部署动作：只重建 `sub2api-blue`，未重启 active `sub2api-green`、PostgreSQL 或 Redis。
- `docker-compose.blue.yml` 默认镜像同步为 `sub2api:v0.1.134.34`。
- `docker exec sub2api-blue /app/sub2api --version`：`Sub2API 0.1.134 (image: v0.1.134.34, commit: c7b059f169c5, built: 2026-06-12T00:28:58Z)`。
- `sub2api-blue` 候选窗口：Health `healthy` 持续超过 60 秒，镜像 `sub2api:v0.1.134.34`。
- `GET http://127.0.0.1:18083/health`：200。
- `GET http://127.0.0.1:18083/`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:18083/api/v1/admin/system/version` 未登录：401。
- `GET http://127.0.0.1:18083/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:18083/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:18083/v1/messages` 未登录：401。
- 候选日志过滤：启动窗口出现 1 条 `[OpenAI] cleanup expired request snapshots failed err=pq: canceling statement due to user request`；后续 45 秒复查关键错误过滤命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-green:8080` 切到 `sub2api-blue:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `GET http://127.0.0.1:8080/health`：200。
- `GET http://127.0.0.1:8080/`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:8080/api/v1/admin/system/version` 未登录：401。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401。
- `POST http://127.0.0.1:8080/v1/messages` 未登录：401。
- 切流后容器状态：`sub2api-blue` Health `healthy`，`sub2api-green` Health `healthy` 且继续运行。
- 切流后 `sub2api-blue` 与 `sub2api-proxy` 关键错误日志过滤：命中 0。

## 回滚

- 当前回滚容器：`sub2api-green`，镜像 `sub2api:v0.1.134.33`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-green:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- `D:\sub2api-deploy\.env` 中 `ADMIN_PASSWORD` 为空，本轮无法执行登录态点击复测；已用前后端回归测试覆盖默认模型和人工探测响应字段，用入口静态资源冒烟确认新前端资源上线。
- request snapshot 清理任务在候选启动窗口出现 1 条 `pq: canceling statement due to user request`，后续观察窗口无重复；继续作为既有启动清理观察项留意。
