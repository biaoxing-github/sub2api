# v0.1.134.33 Release Log

- 日期：2026-06-12
- 执行者：Devil
- Git 提交：`529d33b3b371`（`fix(admin): 修复调度池人工探测结果契约`）
- 包含上一线上版本后的提交范围：`c1bccd741..529d33b3b371`
- 镜像标签：`sub2api:v0.1.134.33`
- 镜像 ID：`sha256:9710887ced132acd59636b6abc1f0c0247206fe1dc9ccfca92479302df1e3366`
- 构建标签：`org.opencontainers.image.version=v0.1.134.33`，`org.opencontainers.image.revision=529d33b3b371`
- 发布前 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.32`，Health `healthy`
- 发布前 idle：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.31`，Health `healthy`
- 发布后 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.33`，Health `healthy`
- 后续状态：该版本已被 `sub2api:v0.1.134.34` supersede，作为 `sub2api-green` 回滚目标保留。
- 回滚目标：`sub2api-blue` / `sub2api:v0.1.134.32`

## 更新内容

- 修复账号调度池人工探测接口返回 `result` 时仍被前端显示为“探测失败”的问题。
- `ManualProbeResponse.result` 不再直接暴露 Go 内部结构体，改为显式 DTO，返回 `success`、`message`、`error`、`latency_ms`、`first_token_ms`、`http_status`、`reason`。
- 失败结果同时映射 `message` 与 `error`，成功结果保留耗时字段，避免前端读取 `result.success` 得到 `undefined`。

## 构建记录

- 构建来源：`git archive HEAD`，确保使用已提交状态。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.33 --label org.opencontainers.image.version=v0.1.134.33 --label org.opencontainers.image.revision=529d33b3b371 --build-arg COMMIT=529d33b3b371 --build-arg IMAGE_VERSION=v0.1.134.33 -`
- 构建结果：成功生成 `sub2api:v0.1.134.33`；前端生产构建与后端 release build 均通过。
- 构建观察：Vite 输出既有 dynamic import / chunk size / Browserslist warning，无构建失败。
- 镜像确认：`docker image inspect sub2api:v0.1.134.33` 返回 `Version=v0.1.134.33`、`Revision=529d33b3b371`、镜像 ID `sha256:9710887ced132acd59636b6abc1f0c0247206fe1dc9ccfca92479302df1e3366`。

## 代码验证

- RED：`go test -tags unit ./internal/handler/admin -run "TestManualProbeResultResponseFromService" -count=1` 修复前失败，原因是缺少人工探测结果 DTO 转换函数。
- GREEN：`go test -tags unit ./internal/handler/admin -run "TestAccountHandler_ManualProbe|TestManualProbeResultResponseFromService" -count=1` 通过。
- GREEN：`npm run test:run -- src/views/admin/__tests__/AccountSchedulingPoolView.spec.ts` 通过，1 file / 7 tests passed。
- GREEN：`npm run typecheck` 通过。
- GREEN：`git diff --check` 通过；仅提示既有 `.codegraph/daemon.pid` 换行告警。

## 候选验证

- 部署目标：idle `sub2api-green`，本机候选端口 `127.0.0.1:18082`。
- 部署动作：只重建 `sub2api-green`，未重启 active `sub2api-blue`、PostgreSQL 或 Redis。
- `docker-compose.green.yml` 默认镜像同步为 `sub2api:v0.1.134.33`。
- `docker exec sub2api-green /app/sub2api --version`：`Sub2API 0.1.134 (image: v0.1.134.33, commit: 529d33b3b371, built: 2026-06-12T00:18:49Z)`。
- `sub2api-green` 候选窗口：Health `healthy` 持续超过 60 秒，镜像 `sub2api:v0.1.134.33`。
- `GET http://127.0.0.1:18082/health`：200。
- `GET http://127.0.0.1:18082/`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:18082/api/v1/admin/system/version` 未登录：401。
- `GET http://127.0.0.1:18082/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:18082/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:18082/v1/messages` 未登录：401。
- 候选日志过滤：启动窗口出现 1 条 `[OpenAI] cleanup expired request snapshots failed err=pq: canceling statement due to user request`；后续 45 秒复查关键错误过滤命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-blue:8080` 切到 `sub2api-green:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `GET http://127.0.0.1:8080/health`：200。
- `GET http://127.0.0.1:8080/`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:8080/api/v1/admin/system/version` 未登录：401。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401。
- `POST http://127.0.0.1:8080/v1/messages` 未登录：401。
- 切流后 `sub2api-green` 与 `sub2api-proxy` 关键错误日志过滤：命中 0。

## 回滚

- 当前该版本在 `sub2api-green` 上保留，可作为 `v0.1.134.34` 的回滚目标。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-green:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- `D:\sub2api-deploy\.env` 中 `ADMIN_PASSWORD` 为空，本轮无法执行登录态点击复测；已用后端 DTO 回归测试验证 `manual-probe` 响应字段，入口冒烟确认新镜像上线。
- request snapshot 清理任务在候选启动窗口出现 1 条 `pq: canceling statement due to user request`，后续观察窗口无重复；继续作为既有启动清理观察项留意。
