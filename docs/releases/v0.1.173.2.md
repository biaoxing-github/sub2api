# v0.1.173.2 发布记录

- 日期：2026-08-10
- 执行者：Devil
- Git 提交：`a564c0b3e7201a65f6dc8f97f069dff2da4047d5`
- 镜像：`sub2api:v0.1.173.2`
- 镜像 ID：`sha256:255e08832c87f3103d5811efd91db0d4ebca0dbdd6c0d1517c8be4b147a357e4`
- 正式颜色：green（`sub2api-green`）
- 回滚目标：blue（`sub2api:v0.1.173.1`）

## 功能与用户影响

- Channel Monitor V2 的管理端 V1/V2 模式切换、吞吐隐私设置和运行时通知链路正式上线。
- 管理端 handler 测试桩与当前 `AdminService` 接口保持一致，恢复 handler 全包测试。
- 计费回退表补齐 GLM、Kimi、MiniMax 和 Doubao 型号，远程价格缺失时不再因空条目失去回退定价。
- Anthropic context-management 测试与当前 beta/passthrough 方法签名和 Haiku 最小 beta 契约一致。

## 构建与推送

- 功能提交 `a564c0b3e7201a65f6dc8f97f069dff2da4047d5` 已推送到 `fork/codex/merge-v0.1.134-updates`，远端哈希核验一致。
- 从 committed HEAD 的 `git archive` 构建，归档 `D:\sub2api-build\v0.1.173.2-a564c0b3e\source.tar` SHA-256 为 `198D136BDEBB6B032F5A7E830EAEE7E6B107E14A22180B46F2D7C572E30FFD8F`。
- OCI version/revision 和二进制身份一致：主版本 `v0.1.173`、镜像版本 `v0.1.173.2`、提交 `a564c0b3e7201a65f6dc8f97f069dff2da4047d5`。

## 验证证据

- `go test ./internal/handler/admin -count=1` 通过。
- billing 与 Anthropic beta/passthrough 聚焦测试通过；前端 `pnpm run typecheck`、`pnpm run build` 通过。
- 候选 `/health`、首页和主资源为 200；未登录管理接口与 `/v1/responses` 为 401。
- 候选独立观察 61.7 秒、13 次采样全部通过，green `healthy/restart=0`，关键日志 0。
- 切流后 `8080`、`18081`、`18082` 主资源均为 `/assets/index-xrOfeIS2.js`，SHA-256 为 `95E9920ECE6BD87A8799DB734AB94F5767ED1B8EB1AA9237C22C17069499B086`。
- 切流后独立观察 65.6 秒、13 轮四入口 health 全部 200；green/blue 均 healthy/restart 0；green/proxy 关键日志 0；PostgreSQL、Redis healthy/restart 0。

## 边界与遗留

- 功能提交不含 migration/schema 文件，未执行直接 SQL，也未重启 PostgreSQL 或 Redis。
- `go test -tags=unit ./internal/service -count=1` 仍有任务前已存在的 OpenAI、Grok、pricing 和 Haiku mimic 等无关失败，本次未扩大范围修复。
- 当前 active 为 green `sub2api:v0.1.173.2`，blue `sub2api:v0.1.173.1` 保留为回滚目标。
