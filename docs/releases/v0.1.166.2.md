# v0.1.166.2 发布记录

- 日期：2026-07-29
- 执行者：Devil
- Git 提交：`602ac96f9510fe92751a2a5f9ba38d2218347d39`
- 镜像：`sub2api:v0.1.166.2`
- 镜像 ID：`sha256:6d94564ce7a9cd1cf3b3709d02f14a1ac19b7032aaff08ac1f0ab66e09e4d3b5`
- 正式颜色：green（`sub2api-green`）
- 回滚目标：blue（`sub2api:v0.1.166.1`）

## 功能与用户影响

- Mihomo 节点切换失败也会推进出口代次，并缓存该代失败结果；同一批并发 worker 不再依次重复请求 Controller。
- 等待切换锁的 worker 会先检查任务 context；文件调度器取消后不再把 `context canceled` 误记为新的节点切换故障。
- 20 秒切换超时改为明确提示 `20s 内未找到新的可用出口`，任务继续沿用既有 5 分钟暂停并重试当前兑换码的行为。

## 发布过程

- 最新语义 tag 为 `v0.1.166`，`v0.1.166.2` 在本机镜像、发布记录和构建目录中均未使用。
- 从提交 `602ac96f9` 的 `git archive HEAD` 构建；归档 `D:\sub2api-build\v0.1.166.2-602ac96f9\source.tar` 的 SHA-256 为 `C0F0D8ADD425769CD60B4EC936DEA2D5DDB32C9FF0A46C9D91092AD80E2B1F62`。
- 构建使用本机 `sub2api-build-node:24-pnpm9-mirror-v2` 基座；工作树内其余未提交 v166 改动未进入镜像。
- 为避免 blue worker 与 green 共享状态文件，部署前通过管理页面停止 run `24269b8f-cb19-4e3d-9611-8f9ab9df8dbf`；候选观察期间页面新建的 run `0f39aac3-535c-40b1-9841-8714010496fd` 也在切流前停止。
- 仅以 `--no-deps --force-recreate` 重建 idle green；候选通过后执行 `nginx -t`，upstream 切换到 green 并 reload，blue 保留回滚。

## 验证证据

- `go test ./internal/service -run '^TestNewAPIRedeem(Service|RunNetwork)' -count=1` 通过；server、handler、service 编译切片通过；`gofmt -d` 和目标 diff check 通过。
- 新回归测试证明 8 个并发等待者在同代切换失败时只调用 Controller 一次。
- OCI version/revision 与容器内二进制 `-version` 均匹配 `v0.1.166.2` 和完整功能提交。
- 候选独立观察 61.1 秒、13 次采样全部通过，green 始终 `healthy/restart=0`，关键日志命中 0。
- 切流后独立观察 65.1 秒、13 轮 `8080`、`18081`、`18082`、`18083` health 全部 200；green/proxy 关键日志命中 0。
- 四入口首页和主资源均为 200，未登录管理接口和 `/v1/responses` 均为 401；主资源 `/assets/index-DF9pvqia.js` SHA-256 均为 `355DE04F4EE732FEA75C6E118C5D217E14D1884EFA0909578DED189FB5264DC3`。
- 管理端版本下拉显示主版本 `v0.1.166`、镜像版本 `v0.1.166.2`；blue 回滚、PostgreSQL、Redis 均为 `healthy/restart=0`。

## 遗留边界

- 本机 `CGO_ENABLED=0` 且无 gcc/clang，未执行 `go test -race`；聚焦并发测试和普通 Go 回归均通过。
- 功能提交不含 migration/schema 文件；未执行数据库迁移、直接 SQL、Git push 或镜像 push。
- 发布完成时没有活动兑换任务，部署流程未自动重新启动任务。
