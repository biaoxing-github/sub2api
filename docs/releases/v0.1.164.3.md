# v0.1.164.3 发布记录

- 日期：2026-07-25
- 执行者：Devil
- Git 提交：`263c61036f94356fb179a0be180a34fe81a908fb`
- 镜像：`sub2api:v0.1.164.3`
- 正式颜色：green（`sub2api-green`）
- 回滚目标：blue（`sub2api:v0.1.164.2`）

## 功能与用户影响

- NewAPI 兑换工具展示与平台 `base_url` 匹配的 Sub2API 账号及其 Key 引用关系。
- 支持把兑换工具 Key 追加或覆盖到对应 Sub2API 账号。
- 已被任一 Sub2API 账号引用的 Key 使用绿色背景、描边和“已被引用”标识高亮。

## 发布过程

- 从提交 `263c61036` 的 `git archive HEAD` 构建不可变镜像。
- 镜像 OCI version 为 `v0.1.164.3`，revision 为 `263c61036f94356fb179a0be180a34fe81a908fb`。
- 仅重建 idle `sub2api-green`，未重启 PostgreSQL、Redis 或 active blue。
- green 进入 healthy 且 restart count 为 0 后，Nginx 配置检查通过并切流到 green。
- `sub2api-blue` 保持 healthy，作为回滚目标。
- 临时容器 `sub2api-redeem-dev` 已停止并保留。

## 验证说明

用户明确要求跳过测试和发布后功能验证。本次只执行了镜像构建、身份核对、候选容器健康门槛和 Nginx 配置语法检查；页面功能由用户自行验收。

## 遗留风险

- Key 高亮、追加和覆盖交互未在 `v0.1.164.3` 正式页面执行发布后验证。
