# sub2api v0.1.134.48 发布记录

- 日期：2026-06-16
- 执行者：Devil
- Git 提交：待回填
- 镜像标签：sub2api:v0.1.134.48
- active/idle：发布前 active=blue(sub2api:v0.1.134.47)，idle=green(sub2api:v0.1.134.46)；发布后待回填

## 更新内容

- 修复 gptai-plus 真实 Codex 路径缺上游 usage 时仍打印 `openai.record_usage_failed` 的问题。
- OpenAI Responses 流式请求若已确认上游没有真实 usage，则继续跳过 usage/billing 写入，但 `RecordUsage` 返回成功，避免把“上游无 usage 的已知兼容路径”记录为后台错误。
- 保持真实 `usage=0` 仍可写入 usage log，避免影响上游明确返回 usage 对象的路径。

## 验证结果

- `go test -tags unit ./internal/service -run "TestOpenAIGatewayServiceRecordUsage_(ZeroUsageStillWritesUsageLog|MissingObservedUsageSkipsUsageLog|FeedsPathHealthSample|MissingPricingRecordsZeroCostUsageLog)|TestOpenAIStreaming(TerminalEventWithoutUsageAddsClientTerminalFields|TerminalEventWithDoneDoesNotDuplicateDoneMarker)" -count=1 -v`：通过。
- `go test ./cmd/server -run TestNoSuchTest -count=1`：通过。
- `git diff --check -- backend/internal/service/openai_gateway_service.go backend/internal/service/openai_gateway_record_usage_test.go`：通过。
- 构建、候选容器、切流后入口和真实 Codex 路径日志验证：待回填。

## 回滚目标

- 秒级回滚：将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 切回 `sub2api-blue:8080` 并 reload nginx。
- 回滚镜像：sub2api:v0.1.134.47。

## 遗留风险

- 待构建部署和真实验证后回填。
