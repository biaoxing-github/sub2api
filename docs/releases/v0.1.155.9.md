# sub2api v0.1.155.9 发布记录

- 日期：2026-07-15
- 执行者：Devil
- Git 提交：`f33701edc1a4`
- 镜像：`sub2api:v0.1.155.9`
- 镜像 ID：`sha256:9f92ee3e87af1d67f4c5b69929c7e6335cd8ecca3a7a505250f352adf6822cb1`
- 当前 active：`sub2api-green:8080`
- idle / 回滚目标：`sub2api-blue:8080`，`sub2api:v0.1.155.7`

## 详细更新日志

1. `frontend/public/newapi-checkin/index.html` 在平台目录账号表格新增“余额 / 已用”列。
2. 平台目录按站点名称与用户 ID 组合键关联 `state.balances.accounts` 中的 PostgreSQL 账号余额缓存，避免同站点多账号串行显示。
3. 有缓存时显示格式化后的当前余额与累计已用，并在悬停提示中展示缓存更新时间；没有缓存时显示“暂无缓存”。
4. `frontend/src/views/admin/tools/newapiCheckinLegacy.generated.ts` 同步相同逻辑，保持嵌入管理端页面与公开静态页面一致。
5. `frontend/src/views/admin/tools/newapiCheckinLegacy.generated.test.ts` 增加当前余额 `$25` 与累计已用 `$0.02` 的渲染断言。

## 用户可见行为

- 打开签到工具的平台目录弹窗后，可直接在每个账号行查看当前余额和累计已用，方便判断账号状态。
- 页面只读取服务端已经返回的数据库余额缓存，不新增上游请求，也不会触发签到或余额刷新。
- 尚未产生余额缓存的账号明确显示“暂无缓存”。

## 构建与验证

- 发布前聚焦 Vitest 3/3、`pnpm run typecheck`、`pnpm run build` 和 `git diff --check` 通过。
- 功能提交为 `f33701edc1a4`，从该 committed HEAD 的 `git archive` 构建不可变镜像。
- 首次构建的 `sub2api:v0.1.155.8` 未注入二进制 `image_version`，在候选部署前被构建闸门拦截，未部署且未覆盖。
- 最终镜像 OCI version=`v0.1.155.9`、revision=`f33701edc1a4`；镜像内二进制包含 `image_version=v0.1.155.9`。
- 仅重建 idle green，active blue 在候选阶段持续运行；PostgreSQL 和 Redis 未重启。
- 候选端口 `18082` 的 health、首页和签到页面为 200；管理配置和 `/v1/responses` 未登录均为 401；余额列与无缓存标记存在。
- 启动初期记录一次 `pq: canceling statement due to user request`；随后独立 60 秒观察窗口 7 次采样均 healthy/restart 0、HTTP 200，新增关键日志为 0。
- nginx 配置检查和 reload 通过，active 从 blue 切换到 green；切流后 7 轮 `8080/18081/18082` health 均为 200，green/proxy 新增关键日志为 0。
- 三入口主资源均为 `/assets/index-CVzInI2T.js`，SHA-256 均为 `98d2bb2922246b15cffd9eda498c85df6a5e0cac72e44a5ec56a15c6322c758d`。
- 三入口签到页面 SHA-256 均为 `24521feeb580f5676eeba0b5de5151f6c1fff7c7f7d4aa74e68dec26ee77c8fb`，均包含“余额 / 已用”标记。
- green `v0.1.155.9`、blue `v0.1.155.7`、PostgreSQL、Redis 均 healthy/restart 0。

## 回滚

将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 的 upstream 改回 `sub2api-blue:8080`，执行 `docker exec sub2api-proxy nginx -t`，通过后执行 `docker exec sub2api-proxy nginx -s reload`。

- upstream 备份：`D:\sub2api-deploy\proxy\upstreams\active.conf.bak-20260715-221304`
- 部署环境备份：`D:\sub2api-deploy\.env.bak-20260715-220900`
- 回滚镜像：`sub2api:v0.1.155.7`

## 遗留风险

- 当前没有管理员登录态，受保护版本接口返回 401，因此未在线核对版本下拉；OCI 标签、运行镜像和二进制版本已核验。
- 本轮未执行真实余额刷新、上游同步、分组修改、Key 追加/替换或签到。
- 未执行 Git push 或镜像 registry push。
