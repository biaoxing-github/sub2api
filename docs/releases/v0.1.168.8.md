# v0.1.168.8 发布记录

- 日期：2026-08-01
- 执行者：Devil
- 功能：OpenAI Responses 90 秒响应头等待与客户端断流取消

## 用户影响

- OpenAI HTTP `/v1/responses` 流式客户端断开时，取消信号会传递到对应上游 HTTP 请求，不再无条件继续生成到终态。
- `openai_request_header_timeout_seconds` 现在直接表示等待上游响应头的秒数，生产配置为 90 秒，不再被请求体大小截短为 10/15/20 秒。
- 非流式 Responses、Chat Completions、Anthropic、图片和 WebSocket 保持既有取消与计费行为。

## 代码与构建

- 功能提交：`848e0e04ff633504c26d9cbf22bff661a2d1d1bc`
- 最新语义化 tag：`v0.1.168`
- 镜像：`sub2api:v0.1.168.8`
- 镜像 ID：`sha256:dc2cc5b9149daf82d7da0d1ddff4f6aad3cd5e9cbb1d749fb29736abfbe6db5c`
- OCI revision：`848e0e04ff633504c26d9cbf22bff661a2d1d1bc`
- OCI version：`v0.1.168.8`
- 二进制身份：`Sub2API v0.1.168 (image: v0.1.168.8, commit: 848e0e04ff633504c26d9cbf22bff661a2d1d1bc)`
- `git archive HEAD` SHA-256：`E2B6980538C87ABC85507C8F6B54F1F601B86CCE9286DC0C9B8A6F7C7F5662EC`

## 部署

- 部署方式：蓝绿发布，只重建 idle blue 应用容器。
- 正式颜色：blue，`sub2api-blue` 使用 `sub2api:v0.1.168.8`。
- 回滚目标：green，`sub2api-green` 保留 `sub2api:v0.1.168.7`。
- active upstream：`sub2api-blue:8080`。
- 切流时间：`2026-08-01 16:59:30 +08:00`。
- 部署前备份：
  - `D:\sub2api-deploy\.env.backup-v0.1.168.8-20260801-165426`
  - `D:\sub2api-deploy\proxy\upstreams\active.conf.backup-v0.1.168.8-20260801-165426`
- `GATEWAY_OPENAI_REQUEST_HEADER_TIMEOUT_SECONDS=90`，数据库 `codex_wait_guard_max_header_wait_seconds=90`。
- `nginx -t` 通过并成功 reload；PostgreSQL、Redis 未重启，均保持 healthy/restart 0。

## 验证证据

- `go test ./internal/config -count=1` 与完整 `go test ./internal/service -count=1` 通过；handler/admin/dto 和 server 编译切片通过。
- OAuth 与 API Key passthrough 取消传播、Responses 专用 context、90 秒响应头策略及既有 usage drain 回归通过。
- blue 候选 smoke：`18083/health`、首页和主 JS 资源均 200；未登录管理 API 与 `/v1/responses` 均 401；容器内 90 秒配置和二进制身份匹配。
- 候选独立观察：61.5 秒、13/13 次采样通过，blue healthy/restart 0，关键日志 0。
- 切流后 `8080`、`18081`、`18083`、`18082` 均返回 health/home/asset 200；主资源 `/assets/index-BKWgQvnT.js` SHA-256 均为 `0E3181EEBC4579985E32B7852A2970DE13AEC9B4473F4A5F6FBA56838B765E03`。
- 切流后独立观察：66.3 秒、13/13 轮四入口 health 全部 200；blue restart 0，green 保持发布前累计 restart 7，blue/proxy 关键日志 0，PostgreSQL/Redis healthy/restart 0。
- 已登录 Chrome 管理端显示主版本 `v0.1.168`，版本详情显示镜像版本 `v0.1.168.8`；应用页面 console error 为 0。

## 遗留风险

- 未主动向真实付费上游注入长请求并中途断开；取消传播由本地自动化测试和运行镜像身份验证覆盖。
- 上游在收到取消前已经生成的部分仍可能计费；终态 usage 未返回时不会虚构完整 usage。
- 未执行 Git push 或镜像仓库 push。
