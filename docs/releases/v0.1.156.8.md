# sub2api v0.1.156.8 发布记录

- 日期：2026-07-16
- 执行者：Devil
- 功能合并提交：`55301cc1d4e2`
- 主版本修正提交：`e003548fb357`
- 根模型路由修复提交：`5c697f087ce6`
- 构建提交：`5c697f087ce6`
- 镜像：`sub2api:v0.1.156.8`
- 镜像 ID：未生成
- 计划 active：blue；回滚：green `sub2api:v0.1.156.5`
- 当前状态：构建网络门禁失败，未部署且不复用

## 更新

1. 融入 v0.1.156 批次 1-3 的成本、failover、Responses、SSE/WS、Anthropic、图片尺寸、Antigravity 和会话 seed 更新。
2. 嵌入式主版本更新为 `v0.1.156`，管理端主版本和镜像小版本可分别核验。
3. 修复嵌入前端中间件截获根 `/models`：该路径现在进入与 `/v1/models` 相同的 API Key 中间件和模型处理器。

## 本地验证

- service、apicompat、handler、routes、config 和前端 Antigravity 测试通过。
- 嵌入前端中间件 `/models` 测试先失败后通过；`go test -tags embed ./internal/web -count=1` 通过。
- `go test ./internal/server/routes -count=1` 与 `go test ./... -run '^$' -count=1` 通过。
- `.7` 运行时契约失败后已回滚到 green `.5`，生产健康。

## 发布验证

- FAIL：Docker 构建阶段同时出现 TUNA Alpine DNS transient error 和 npm registry 请求失败，未完成依赖层。
- PASS：本机不存在 `sub2api:v0.1.156.8`，部署 `.env`、blue `.7` 和 active green `.5` 均未变更。
- PASS：故障后 Docker 内 TUNA 与 USTC `apk update` 各连续 2 次成功，npm registry 返回 200。
- NEXT：网络前置恢复后顺延到 `v0.1.156.9`，`.8` 永久不复用。

## 回滚与边界

- 旧 active green `sub2api:v0.1.156.5` 保留为回滚目标，不重建。
- `.6` 和 `.7` 均为失败候选，不覆盖、不复用。
- 无新增迁移，不写数据库，不重启 PostgreSQL 或 Redis。
- 未执行 Git push 或 registry push。
