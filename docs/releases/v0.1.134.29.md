# v0.1.134.29 Release Log

- 日期：2026-06-11
- 执行者：Devil
- Git 提交：`2f35bad37ae5`（`test(handler): 同步用户仓储测试桩接口`）
- 包含上一线上版本后的提交范围：`fc0c8cdd0da8..2f35bad37ae5`
- 镜像标签：`sub2api:v0.1.134.29`
- 镜像 ID：`sha256:d0a7868406b341256ca70168dce5bb93722264ca2e6f173b98db97c708cb7dc3`
- 构建标签：`org.opencontainers.image.version=v0.1.134.29`，`org.opencontainers.image.revision=2f35bad37ae5`
- 发布前 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.28`，Health `healthy`
- 发布前 idle：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.27`，Health `healthy`
- 发布后 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.29`，Health `healthy`
- 回滚目标：`sub2api-blue` / `sub2api:v0.1.134.28`

## 更新内容

- 调度耗尽探测期间先开启 SSE 响应并发送 `scheduler_probe_pending` 事件，避免长时间探测期间客户端无响应。
- 无限探测失败后按失败次数使用分级退避间隔，并按退避长度调整 SSE keepalive 心跳频率。
- `OpenAIGatewayService` 暴露 `ProbeIntervalFromErrorCount`，让 handler 复用服务层现有探测退避规则。
- 修复 handler 单元测试共享 `userHandlerRepoStub` 未实现 `GetByIDIncludeDeleted` 导致 `internal/handler` 包测试无法编译的问题。

## 构建记录

- 构建来源：`git archive HEAD`，确保使用已提交状态。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.29 --label org.opencontainers.image.version=v0.1.134.29 --label org.opencontainers.image.revision=2f35bad37ae5 --build-arg COMMIT=2f35bad37ae5 --build-arg VERSION=v0.1.134 --build-arg IMAGE_VERSION=v0.1.134.29 -`
- 构建结果：成功生成 `sub2api:v0.1.134.29`；前端构建层命中缓存，后端 release build 通过。
- 镜像确认：`docker image inspect sub2api:v0.1.134.29` 返回 `Version=v0.1.134.29`、`Revision=2f35bad37ae5`、镜像 ID `sha256:d0a7868406b341256ca70168dce5bb93722264ca2e6f173b98db97c708cb7dc3`。

## 代码验证

- GREEN：`go test -tags unit ./internal/service -run "TestOpenAISchedulerExhaustionProbe|TestProbeIntervalFromErrorCount" -count=1` 通过。
- GREEN：`go test -tags unit ./internal/handler -run TestNoSuchTest -count=1` 通过，确认 handler 包测试编译恢复。
- GREEN：`go test ./cmd/server -run TestNoSuchTest -count=1` 通过。
- GREEN：`git diff --check` 通过。
- OBSERVED：首次尝试枚举 `internal/handler` OpenAI/Responses 相关测试时，旧测试桩缺少 `GetByIDIncludeDeleted` 导致包测试编译失败；已通过 `2f35bad37ae5` 修复并重新验证。

## 候选验证

- 部署目标：idle `sub2api-green`，本机候选端口 `127.0.0.1:18082`。
- 部署动作：只重建 `sub2api-green`，未重启 active `sub2api-blue`、PostgreSQL 或 Redis。
- `docker-compose.green.yml` 默认镜像同步为 `sub2api:v0.1.134.29`。
- `docker exec sub2api-green /app/sub2api --version`：`Sub2API v0.1.134 (image: v0.1.134.29, commit: 2f35bad37ae5, built: 2026-06-11T09:50:33Z)`。
- `sub2api-green` 候选窗口：`Status=running`，`Health=healthy` 持续超过 60 秒，镜像 `sub2api:v0.1.134.29`。
- `GET http://127.0.0.1:18082/health`：200。
- `GET http://127.0.0.1:18082/`：200。
- `GET http://127.0.0.1:18082/admin/settings`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:18082/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:18082/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:18082/v1/messages` 未登录：401。
- 候选日志过滤：启动窗口内出现 1 条 `[OpenAI] cleanup expired request snapshots failed err=pq: canceling statement due to user request`；切流前最近 1 分钟复查关键错误过滤命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-blue:8080` 切到 `sub2api-green:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `GET http://127.0.0.1:8080/health`：200。
- `GET http://127.0.0.1:8080/`：200。
- `GET http://127.0.0.1:8080/admin/settings`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:8080/v1/messages` 未登录：401。
- 切流后容器状态：`sub2api-green` Health `healthy`，`sub2api-blue` Health `healthy` 且继续运行，`sub2api-proxy` running。
- 切流后 `sub2api-green` 与 `sub2api-proxy` 关键错误日志过滤：命中 0。

## 回滚

- 当前回滚容器：`sub2api-blue`，镜像 `sub2api:v0.1.134.28`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-blue:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- `D:\sub2api-deploy\.env` 中 `ADMIN_PASSWORD` 为空，本轮无法执行管理端登录后的 `/api/v1/admin/system/version` 接口验证；镜像版本由 Docker label 与容器内 `/app/sub2api --version` 交叉确认。
- request snapshot 清理任务在候选启动窗口出现 1 条 `pq: canceling statement due to user request`，切流前最近 1 分钟与切流后窗口无重复；该观察项延续既有启动清理现象，继续留意即可。
