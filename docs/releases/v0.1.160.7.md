# v0.1.160.7 发布记录

- 日期：2026-07-19
- 执行者：Devil
- 状态：已发布并完成线上验证

## 功能与用户影响

- 调度池展示当前分组内全部 `active` 且已开启调度的账号；限流、过载、临时避让、Key 冷却和线路阻断账号不再从页面消失。
- 有确定恢复点时展示下次调度时间和原因；禁用 Key 展示脱敏信息并提供恢复入口。
- 上游 503 的单 Key 冷却缩短为 5 秒、15 秒、30 秒、60 秒封顶，降低临时故障造成的停用时间。
- 同优先级账号优先选择剩余并发槽更多的账号，再按负载率和最后使用时间排序。
- 调度池改为分页读取完整账号，移除逐账号回查造成的 N+1。

## 涉及模块

- `backend/internal/handler/admin/account_handler.go`
- `backend/internal/service/account.go`
- `backend/internal/service/account_api_key_failover.go`
- `backend/internal/service/openai_gateway_service.go`
- `backend/internal/service/openai_scheduling_pool.go`
- `frontend/src/views/admin/AccountSchedulingPoolView.vue`
- `frontend/src/types/index.ts`
- `frontend/src/i18n/locales/en.ts`
- `frontend/src/i18n/locales/zh.ts`
- 对应后端与前端测试文件

## 版本身份

- 功能提交：`60e01ba0868058783e359107e1325e428a08ddd5`
- 主版本：`v0.1.160`
- 镜像：`sub2api:v0.1.160.7`
- 镜像 ID：`sha256:38adba20ff4f89bed757c4fcae5c8c9a0c2bb5fb32ef569746cbc70c788a72ec`
- 构建归档：`D:\sub2api-build\v0.1.160.7-60e01ba08680\source.tar`
- 构建归档 SHA-256：`A22E2589AC1A83F0FA9F2896BBBCAD91C59716017417F3103DD37F24508CB9A0`
- active：blue
- rollback：green `sub2api:v0.1.160.6`

## 验证证据

- service 全量、admin handler 全量、调度池 Vitest 9/9、前端 typecheck 与生产构建在发布前已通过。
- 镜像从 committed HEAD 归档构建；OCI version/revision 与镜像内二进制主版本、镜像版本和完整 commit 一致。
- 只重建 idle blue；候选 `18083` 的 health、首页和主资源为 200，受保护管理版本 API 与 `/v1/responses` 未认证为 401。
- 候选独立观察 61.1 秒，13/13 轮均为 healthy、restart 0，关键日志命中 0。
- `nginx -t` 和 reload 通过；`8080`、`18081`、`18083` 三入口接口契约一致。
- 三入口主资源均为 `/assets/index-DeAC8Ayt.js`，SHA-256 为 `FFD2ECA92CD409ED6D68000ECCC08EF43D727420723B60E9DC244674F64B0413`。
- 切流后独立观察 61.8 秒，13/13 轮共 39 次 health 全部 200；blue/proxy 关键日志命中 0。
- 复用现有管理员登录态验证 `http://localhost:8080/admin/account-scheduling-pool`：页面标题和版本正确，真实账号、统计、优先级和分组切换正常，空分组空态正确；桌面与 `390x844` 视口无空白页、框架错误层、横向溢出或操作按钮重叠。
- 只读数据库计数与页面一致：非软删除且已开启调度的 OpenAI 账号共 4 个，其中自用组 3 个、生图组 1 个、K12 与未分组池均为 0。
- 当前生产数据没有仍在冷却或永久禁用中的 Key，因此未在真实账号上点击恢复按钮；恢复状态、时间和按钮交互由 Go、Vitest 及发布前固定数据浏览器回归覆盖。
- green、PostgreSQL 和 Redis 保持 healthy、restart 0，启动时间未变化。

## 回滚

- `.env` 备份：`D:\sub2api-deploy\.env.20260719-192811.bak`
- upstream 备份：`D:\sub2api-deploy\proxy\upstreams\active.conf.20260719-192811.bak`
- 回滚时将 upstream 恢复为 `sub2api-green:8080`，执行 `nginx -t` 后 reload。

## 遗留风险与边界

- 未为生产验证主动封禁 Key、制造 503 或修改账号状态；真实禁用 Key 的恢复操作未触发。
- 带 `unit` tag 的 service 全包仍受既有 `account_base_url_test.go` 缺少 `xai` 导入阻断，本轮相关用例已在发布前通过聚焦验证。
- 本次未执行数据库迁移、直接 SQL 写入、Git push 或镜像推送。
