# v0.1.134.37 Release Log

- 日期：2026-06-12
- 执行者：Devil
- Git 提交：`4cc6624a4b63`（`fix(test): 修正 failover 状态码期望`）
- 镜像标签：`sub2api:v0.1.134.37`
- 镜像 ID：`sha256:a9cda719a4c37cac934b5dc1a200e453c90e209451eaa06ddbb46cb642af67ab`
- 构建标签：`org.opencontainers.image.version=v0.1.134.37`，`org.opencontainers.image.revision=4cc6624a4b63`
- 发布前 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.35`，Health `healthy`
- 发布前 idle：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.34`，Health `healthy`
- 发布后 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.37`，Health `healthy`
- 回滚目标：`sub2api-green` / `sub2api:v0.1.134.35`

## 更新内容

- OpenAI API Key 账号错误调度冷却已随当前 HEAD 发布：key 类错误不再写入 `api_keys_disabled`，改为账号级阶梯冷却并复用已有临时不可调度规则。
- Anthropic API Key 网关已随当前 HEAD 发布：支持账号配置多个 `base_url`，502/504 和格式类失败可在同一账号内切换请求 base URL。
- Anthropic 账号创建/编辑前端已随当前 HEAD 发布：手动填写模式支持多个 `base_url` 值的输入与回填。
- 本轮修正 `failover_loop_test.go` 中把 502 当普通切换错误的旧测试期望，使测试覆盖与 502/504 单次同账号重试语义一致。
- 镜像构建显式写入 `IMAGE_VERSION=v0.1.134.37`，运行时 `--version` 可见 `image: v0.1.134.37`。

## 代码验证

- RED：`go test -tags unit ./internal/handler -run "TestHandleFailoverError_TempUnschedule|TestHandleFailoverError_BasicSwitch|TestHandleFailoverError_ContextCanceled" -count=1` 首次失败，原因是测试仍把 502 作为通用 retryable 或普通切换状态码。
- GREEN：修正测试输入后，`go test -tags unit ./internal/handler -run "TestHandleFailoverError" -count=1` 通过。
- GREEN：`go test -tags unit ./internal/service -run "TestAnthropicRequestBaseURLs|TestGatewayService_AnthropicAPIKeyPassthrough_ForwardSwitchesRequestBaseURLOn5xx|TestGatewayService_AnthropicAPIKeyPassthrough_CountTokensSwitchesRequestBaseURLOn5xx|TestHandleUpstreamError429_OpenAIAPIKey|TestRateLimitService_HandleUpstreamError_OpenAIAPIKey" -count=1` 通过。
- GREEN：`npm run typecheck` 通过。
- GREEN：`git diff --cached --check` 在提交 `4cc6624a4b63` 前通过。

## 构建记录

- 构建来源：`git archive HEAD`，HEAD=`4cc6624a4b63`。
- `sub2api:v0.1.134.36` 曾完成本地构建，但发现未传 `IMAGE_VERSION`，二进制 `main.ImageVersion` 为空；该镜像未进入候选验证、未部署、未切流。
- 最终构建命令使用 `--build-arg VERSION=0.1.134 --build-arg IMAGE_VERSION=v0.1.134.37 --build-arg COMMIT=4cc6624a4b63 --build-arg ALPINE_APK_REPOSITORY=https://mirrors.tuna.tsinghua.edu.cn/alpine`。
- 构建结果：成功生成 `sub2api:v0.1.134.37`；前端生产构建使用缓存，后端 release build 通过。
- 镜像确认：`docker image inspect sub2api:v0.1.134.37` 返回 `Version=v0.1.134.37`、`Revision=4cc6624a4b63`。
- 二进制确认：`/app/sub2api --version` 显示 `Sub2API 0.1.134 (image: v0.1.134.37, commit: 4cc6624a4b63, built: 2026-06-12T07:52:00Z)`。

## 候选验证

- 部署目标：idle `sub2api-blue`，本机候选端口 `127.0.0.1:18083`。
- 部署动作：仅以 `SUB2API_BLUE_IMAGE=sub2api:v0.1.134.37` 重建 `sub2api-blue`，未重启 active `sub2api-green`、PostgreSQL 或 Redis。
- `sub2api-blue` Health `healthy` 持续超过 60 秒，镜像 `sub2api:v0.1.134.37`。
- `GET http://127.0.0.1:18083/health`：200。
- `GET http://127.0.0.1:18083/`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:18083/api/v1/admin/system/version` 未登录：401。
- `POST http://127.0.0.1:18083/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:18083/v1/messages` 未登录：401。
- 候选日志观察：启动阶段出现 1 条 `openai_request_snapshot` 过期快照清理 `pq: canceling statement due to user request`，发生在 10 秒清理超时路径；稳定窗口后 `panic|fatal|migration.*fail|checksum|pq:|bind:|address already in use|listen tcp|rebuild failed` 命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-green:8080` 切到 `sub2api-blue:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `GET http://127.0.0.1:8080/health`：200。
- `GET http://127.0.0.1:8080/`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:8080/api/v1/admin/system/version` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401。
- `POST http://127.0.0.1:8080/v1/messages` 未登录：401。
- 切流后容器状态：`sub2api-blue` Health `healthy`，`sub2api-green` Health `healthy` 且继续运行。
- 切流后 `sub2api-blue` 与 `sub2api-proxy` 最近 3 分钟关键错误日志过滤：命中 0。

## 回滚

- 当前回滚容器：`sub2api-green`，镜像 `sub2api:v0.1.134.35`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-green:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- `sub2api:v0.1.134.36` 是本地失败候选前构建产物，因 `IMAGE_VERSION` 为空未部署；后续不要把该标签作为发布入口。
- 当前无管理端登录态，`/api/v1/admin/system/version` 未登录按预期返回 401；本轮通过二进制 `--version` 和镜像标签验证 `image_version` 已写入运行镜像。
- 启动阶段一次性 `openai_request_snapshot` 清理超时日志未复现，但如果后续希望完全消除该启动噪声，需要单独收口清理任务的超时/取消语义。
