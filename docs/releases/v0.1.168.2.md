# v0.1.168.2 发布记录

- 日期：2026-07-30
- 执行者：Devil
- Git 提交：`63ed4797353e8afa3f16a3a22a574780040fd571`
- 镜像：`sub2api:v0.1.168.2`
- 镜像 ID：`sha256:1e92f0aad297a7c7d4c1b342ac8cd482dd41e1ed327a9490b1bfa2e6e22bcc3b`
- 正式颜色：blue（`sub2api-blue`）
- 回滚目标：green（`sub2api:v0.1.168.1`）

## 功能与用户影响

- 新增 Passkey/WebAuthn 登录和用户凭据管理；管理员可配置 RP ID、显示名称、Origins 与登录开关。
- 新增 Model Plaza 页面与管理开关，可按可见分组、模型和定价展示模型广场。
- 更新 Kimi K3、Rapid Cloud、GPT-5.6 Anthropic max effort、Claude OAuth cache breakpoint 和 Claude Sonnet 5 状态别名。
- OpenAI Responses 使用真实 message ID，透传模型映射与模型 ID 复制按官方 v0.1.168 行为更新。
- User 与 API Key 更新只写显式字段，避免陈旧快照覆盖并发余额、用量、状态、限额和分组变更。
- 保留 Grok `grok-4.5`、OpenAI 424 切号和签到平台/账号管理等现有定制。

## 数据迁移与备份

- 迁移前备份 PostgreSQL、部署 `.env` 和 active upstream；数据库 dump 位于 `D:\sub2api-deploy\backups\v0.1.168.2-20260730-192527\sub2api-pre-v0.1.168.2.dump`。
- dump 大小为 67848466 bytes，SHA-256 为 `C5754B18AA0407C9C3BFEB0480EF87E07B4FEFDE169DA9B9B79C49D8ADB8C75A`，`pg_restore -l` 校验通过。
- 使用单事务和 `ON_ERROR_STOP=1` 应用 `191_passkey_credentials.sql`，新增 `passkey_user_handles`、`passkey_credentials` 及两个索引。
- `schema_migrations` checksum 为 `d79e7093f28b1a2ba923da35d7376423683c9a5d21dffd0e581f3c45b5afd817`；迁移后表、索引和记录均已核对。

## 构建与发布

- 最新语义 tag 为 `v0.1.168`，不可变镜像后缀使用未占用的 `.2`。
- 从提交 `63ed4797353e8afa3f16a3a22a574780040fd571` 的 `git archive HEAD` 构建；归档 SHA-256 为 `3F3BDAB4C08F2FFF0E315B514FD7D02D6EEDE55DBD6B6CAD012149B147164BF5`。
- OCI version/revision 和容器内二进制一致：主版本 `v0.1.168`、镜像版本 `v0.1.168.2`、提交 `63ed4797353e8afa3f16a3a22a574780040fd571`。
- active green 保持服务，只重建 idle blue；候选通过后执行 `nginx -t`，将 upstream 从 green 切换到 blue并 reload。
- green `sub2api:v0.1.168.1` 保持运行，作为即时回滚目标；PostgreSQL 和 Redis 未重启。

## 验证证据

- 后端 Passkey、Model Plaza、Optional JWT、Grok 和 OpenAI 424 聚焦测试通过；server/handler/middleware/service/repository 编译通过。
- 前端 8 个文件、64 个 Vitest 用例通过；`pnpm run typecheck` 和 972 模块生产构建通过。
- Testcontainers PostgreSQL/Redis 下 API Key/User repository 16 个并发、余额和显式字段更新子测试通过。
- 候选 `18083` 的 health、首页、Model Plaza 页面和主资源为 200；未登录管理版本、Passkey 用户接口和 `/v1/responses` 保持鉴权合同。
- 候选独立观察 61.4 秒、13 次采样全部通过，blue 始终 `healthy/restart=0`，窗口关键日志为 0。
- 切流后 `8080`、`18081`、`18083` 主资源均为 `/assets/index-DM8yopbG.js`，SHA-256 均为 `57C4F4B035DB8001D7CECE82DCE8B26C9C531EE68670A0A4F668FB10BB45E8D7`。
- 切流后独立观察 64.8 秒、13 轮四入口 health 全部为 200；blue/proxy 关键日志为 0。
- blue active 为 `healthy/restart=0`；green rollback 保持 `healthy/restart=7`；PostgreSQL、Redis 保持 `healthy/restart=0`。

## 遗留边界

- Model Plaza 页面和路由已上线，但当前管理开关关闭，因此匿名 API 请求按预期返回 404；未擅自修改线上功能开关。
- 未执行生产 Passkey 注册或真实账号上游故障注入；接口合同、迁移状态和自动化测试已覆盖相关路径。
- 未迁入依赖完整 ChatGPT Live 产品链的 OpenAI Live store resilience。
- 未执行 Git push 或镜像 push。
