# v0.1.165.2 发布记录

- 日期：2026-07-27
- 执行者：Devil
- Git 提交：`b89762b752cda88bf07c130a8988f8a3e7a6c882`
- 镜像：`sub2api:v0.1.165.2`
- 镜像 ID：`sha256:7eacdcb8f598548f655a2f598d4235867c3851a60ef6981849c59956a087c01a`
- 正式颜色：blue（`sub2api-blue`）
- 回滚目标：green（`sub2api:v0.1.165.1`）

## 功能与用户影响

- 仅在 OpenAI `/responses/compact` 请求收到明确的上游 `400 context_length_exceeded` 时，当前账号会让出请求并尝试下一候选账号，以便压缩流程继续进行。
- 此类上下文超限不会使当前账号被冷却、禁用或写入路径健康熔断；普通非 compact 请求仍保持原有直接错误响应语义。
- 已写出 compact SSE keepalive 时，后续账号切换仍会继续；候选账号全部耗尽时保留已有 `response.failed` 终态，不输出破坏协议的裸 JSON。

## 发布过程

- 从提交 `b89762b75` 的 `git archive HEAD` 构建，归档位于 `D:\sub2api-build\v0.1.165.2-b89762b75\source.tar`，SHA-256 为 `5038217CA13CCAC84D7DAB90E6794D7A201E1BC51268F074C1E8D3674B8AF975`。
- 构建前备份 `D:\sub2api-deploy\.env` 与 `proxy\upstreams\active.conf`；构建上下文未包含工作树中既有未提交内容。
- 镜像 OCI version 为 `v0.1.165.2`，revision 为完整功能提交，created 为 `2026-07-27T08:48:46Z`；容器二进制输出 `Sub2API v0.1.165 (image: v0.1.165.2, commit: b89762b752cda88bf07c130a8988f8a3e7a6c882)`。
- active 为 green 时，仅以 `--no-deps --force-recreate` 重建 idle `sub2api-blue`，未重启 green、PostgreSQL 或 Redis。
- blue 候选通过隔离验证后，`nginx -t` 通过，正式 upstream 从 green 切换到 blue 并 reload；green 保留为回滚目标。

## 验证证据

- 提交前已通过 OpenAI compact 上下文超限切号续传的 service、handler 聚焦单测，server/handler/service 编译切片、`gofmt -d` 和 `git diff --check` 均通过。
- 候选 `18083` 的 health、首页和主资源返回 200；未登录管理接口、`POST /v1/responses` 和 `POST /responses` 返回 401。
- 候选独立观察 60.4 秒、13 次采样全部通过；blue 始终 `healthy`、restart 0，关键日志命中 0。
- 切流后 `8080`、`18081`、`18083` 独立观察 60.8 秒、每端 13 次 health 200、管理接口 401 和 Responses 401 全部通过；主资源 `/assets/index-DF9pvqia.js` SHA-256 为 `355DE04F4EE732FEA75C6E118C5D217E14D1884EFA0909578DED189FB5264DC3`，三条路径一致。
- 回滚 green 的 `18082` health 为 200；green 维持 `healthy`（发布前既有 restart 8），PostgreSQL 与 Redis 均为 `healthy/restart=0`；blue 和 proxy 在切流观察窗口内关键日志命中 0。

## 遗留风险

- 本次未向生产账号主动注入真实 `context_length_exceeded`，以避免干扰业务；切号行为由已提交的聚焦回归用例覆盖，生产层完成部署与通用入口验证。
- 未执行数据库迁移、直接 SQL、镜像推送或 Git push；blue 为当前正式版本，green 可用于回滚。
