# v0.1.172.2 发布记录

- 日期：2026-08-08
- 执行者：Devil
- 主版本：v0.1.172
- 镜像：sub2api:v0.1.172.2
- Git 提交：d254356e9dfa58ba6ea01877633f441591101c16
- 镜像 ID：sha256:77cb0832abfa2e688c4fb05a1afd0f692fdab9ba17c32fe4a087b8c6c0a83145
- 发布颜色：green
- 回滚目标：blue / sub2api:v0.1.172.1

## 功能与用户影响

- Composite 分组支持精确/前缀模型路由、端点匹配、优先级和具体平台调度。
- Responses、Chat Completions、Messages、Count Tokens、Responses WebSocket 和 Gemini 原生路径均按最终目标平台执行；未命中路由 fail-closed。
- Composite 最终路由到 OpenAI 时应用 reasoning effort 上限与映射；其他具体平台不应用 OpenAI policy。
- 管理端提供 Composite 路由 CRUD/预览，模型广场按具体平台展示同名模型。

## 数据库迁移

- `172_composite_model_routes.sql` 已执行，`composite_model_routes` 表已核验存在。
- `185_group_max_reasoning_effort.sql` 已执行。
- `186_group_reasoning_effort_mappings.sql` 已执行。

## 构建与身份

- 从 committed HEAD 的 `git archive` 构建，未包含工作树脏文件。
- OCI version：`v0.1.172`；OCI revision：`d254356e9dfa58ba6ea01877633f441591101c16`。
- 二进制版本：`Sub2API v0.1.172 (image: v0.1.172.2, commit: d254356e9)`。

## 验证证据

- 候选 green `18082`：health/home/主 JS 资源 200，未登录管理 API 和 `/v1/responses` 均 401。
- 候选独立观察约 60 秒：12/12 次 health 200，`running|healthy|0`，关键日志 0。
- `nginx -t` 通过后切流到 green，reload 成功。
- 切流后 `8080`、`18081`、`18082` health/home/鉴权边界均通过；主资源路径 `/assets/index-Cufha_3E.js` 三入口 SHA-256 均为 `C351BD8D1AF1BEDB68954526FC98352203F9D5189E048E021CDF8F90F737CC2A`。
- 切流后独立观察约 60 秒：正式、旁路、候选 health 全部 200；green/blue 均 healthy/restart 0；应用与代理关键日志 0。
- PostgreSQL/Redis healthy，restart 0，未重启。

## 最终状态

- 正式流量：green / `sub2api:v0.1.172.2`。
- 回滚容器：blue / `sub2api:v0.1.172.1`，保持 healthy。
- 未执行 Git push 或镜像 push。
- Embeddings 当前分支没有独立已注册 handler；利润准入调度未引入。
