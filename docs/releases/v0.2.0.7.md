# v0.2.0.7 发布记录

- 日期：2026-09-11
- 执行者：Devil
- Git 提交：`34425fe74eda8e74e71df8b34429e70c13b9d694`
- 主版本：`v0.2.0`
- 镜像：`sub2api:v0.2.0.7`
- 部署颜色：green
- 回滚目标：blue，`sub2api:v0.2.0.6`

## 功能与用户影响

本版本发布账号测试自定义问题、显式模型选择与模型记录修正，以及账号标签和打标时间展示。账号测试显式传入模型时优先使用该模型，未传模型时按平台读取系统设置；人工探测响应和使用日志可显示最终实际模型。

## 涉及模块

- `backend/internal/service/account_test_service.go`
- `backend/internal/handler/admin/account_handler.go`
- `frontend/src/components/account/AccountTestModal.vue`
- `frontend/src/components/admin/account/AccountTestModal.vue`
- `frontend/src/components/account/EditAccountModal.vue`
- `frontend/src/views/admin/AccountsView.vue`

## 构建与部署证据

- 从已提交 HEAD 使用 `git archive` 构建，未使用未提交工作树。
- 构建参数：`VERSION=v0.2.0`、`IMAGE_VERSION=v0.2.0.7`、`COMMIT=34425fe74eda8e74e71df8b34429e70c13b9d694`。
- 候选 green 镜像 ImageID：`sha256:7a7bfa7ea1c7690fcc023bc144cdac5ce53a047f2a16898f8020b3f7b2f0c43e`。
- 仅重建 idle green，PostgreSQL、Redis 和 active blue 未重建。
- 候选与切流后均验证 `/health` 200、首页静态资源 200、未登录管理 API 401、未登录 `/v1/responses` 401。
- nginx `-t` 成功并完成 reload；upstream 已切换至 `sub2api-green:8080`。
- green 候选及切流后各完成独立 65 秒健康观察，health 持续 healthy、restart=0，日志未命中 `panic`、`fatal`、迁移失败、端口冲突等阻断关键词。

## 回滚与遗留风险

- 回滚方式：将 `proxy/upstreams/active.conf` 指回 `sub2api-blue:8080`，执行 `docker exec sub2api-proxy nginx -t` 与 `nginx -s reload`。
- blue `v0.2.0.6` 保留运行并作为回滚目标；其容器历史 restart 计数为 1，本次未发生新增重启。
- 未推送镜像仓库；Git 功能提交已推送至 `fork/codex/merge-v0.1.134-updates`。
