# v0.1.172.1 发布记录

- 日期：2026-08-08
- 执行者：Devil
- 主版本：v0.1.172
- 镜像：sub2api:v0.1.172.1
- Git 提交：26f606697fb09974f6b4246a0db73877d609218c
- 镜像 ID：sha256:896644fbcc4447ce78e56cd02d176a33c31fe5d61c3ae08ccc5d641b146d6796
- 发布颜色：blue
- 回滚目标：green / sub2api:v0.1.168.12

## 功能与用户影响

- 统一 OpenAI 高级和兼容调度账号准入，修复共享代理 quarantine fail-open、低流量 transient streak 和调度快照取消。
- 记录上游成功响应实际声明的模型，并标记发送模型与响应模型是否不一致。
- 覆盖 OpenAI Responses、Chat Completions、Anthropic 兼容、Gemini/AI Studio、HTTP bridge 和 WebSocket v2。
- 管理端用量页支持响应模型展示、匹配状态筛选、版本变体标签和 Excel 导出。

## 数据库迁移

- `194_add_usage_log_upstream_response_model.sql` 已执行并登记。
- `195_add_usage_log_upstream_model_mismatch_index_notx.sql` 已执行并登记。
- `usage_logs.upstream_response_model`、`usage_logs.upstream_model_mismatch` 和部分并发索引均已核验存在。

## 验证证据

- 发布前后端聚焦测试、repository、openai_ws_v2、admin/dto、server/handler 编译通过。
- OCI version/revision 与二进制版本匹配镜像和 Git 提交。
- idle blue 候选 health、首页、主资源、未登录管理 API 和 Responses 合同通过。
- 候选独立观察 66 秒，13/13 次通过，restart 0，关键日志 0。
- `nginx -t` 通过后从 green 切流到 blue。
- 正式 8080、旁路 18081、blue 18083 主资源 SHA-256 均为 `2A3DEC6777D301C19A6B0095EED265CCA09AD82225FE82132666C0A77D2E70A6`。
- 切流后独立观察 61.6 秒，四入口全部 200，应用与代理关键日志 0。
- PostgreSQL 和 Redis 未重启，均为 healthy / restart 0。

## 最终状态

- 正式流量：blue / sub2api:v0.1.172.1。
- 回滚容器：green / sub2api:v0.1.168.12，保持 healthy。
- 未执行 Git push 或镜像 push。
- 分组利润准入调度不再计划引入；完整 Composite Reasoning 留作独立后续功能。
