# v0.1.166.3 发布记录

- 日期：2026-07-29
- 执行者：Devil
- Git 提交：`b239a0af8fb52a377c5b15967636ecb08d4c2532`
- 镜像：`sub2api:v0.1.166.3`
- 镜像 ID：`sha256:336b9e8ef21ea535ee967b1626234ad16957da619105e28232d7b8e1c93177ea`
- 正式颜色：blue（`sub2api-blue`）
- 回滚目标：green（`sub2api:v0.1.166.2`）

## 功能与用户影响

- 吸收 v0.1.166 的 Grok 账号测试与类型展示、设置部分更新保护、用量请求类型与筛选、模型定价数据及相关管理端交互更新。
- OpenAI Responses 原生和透传路径把上游 HTTP 424 识别为 `upstream_failed_dependency`，当前故障账号暂停调度 30 秒并交由 handler 选择下一个候选账号。
- 切号前不再向客户端提交首个 424 响应，避免客户端持续收到同一故障账号的 `Service temporarily unavailable`。

## 发布过程

- 最新语义 tag 为 `v0.1.166`，不可变镜像后缀顺延为 `.3`。
- 从已提交 HEAD `b239a0af8` 的 `git archive HEAD` 构建；归档 `D:\sub2api-build\v0.1.166.3-b239a0af8\source.tar` SHA-256 为 `7F5B3E163F59BD62B615BC6F78D372A841C7D7B9A5E75C7959015098631BCB42`。
- OCI version/revision 与容器内二进制输出一致：主版本 `v0.1.166`、镜像版本 `v0.1.166.3`、提交 `b239a0af8fb52a377c5b15967636ecb08d4c2532`。
- 仅重建 idle blue；active green、PostgreSQL 和 Redis 在候选阶段未重建。
- 候选通过后备份 `active.conf` 为 `active.conf.20260729-144501-v01663.bak`，执行 `nginx -t` 成功后切流并 reload。

## 验证证据

- 424 聚焦回归通过：分类矩阵、30 秒账号冷却、原生 Responses 切号前不写回、透传路径切号前不写回。
- v166 吸收改动的前端 Vitest、`pnpm run typecheck`、`pnpm run build` 和后端 server/handler/repository/service/config 编译切片通过。
- 候选冒烟通过：`/health`、首页和主资源为 200；`/v1/responses`、`/responses`、`/api/v1/admin/users` 未认证均为 401。
- 候选独立观察 61.3 秒、13 次采样全部通过，blue 始终 `healthy/restart=0`，关键日志命中 0。
- 切流后独立观察 63.4 秒、13 轮 `8080`、`18081`、`18083` 和回滚 `18082` health 全部 200；blue/proxy 关键日志命中 0。
- `8080`、`18081`、`18083` 主资源均为 `/assets/index-B10KcsQB.js`，SHA-256 均为 `D912D75AC8C76FB3F1DC350EC45074B4B7CF9212595F34C9C087A9FEAE5B2358`。
- 回滚 green、PostgreSQL、Redis 均为 `healthy/restart=0`。

## 遗留边界

- 线上观察窗口没有自然出现新的上游 424；切号语义由可重复的本地上游桩回归验证，未对生产上游故意注入故障。
- service 全包的既有陈旧断言、handler 既有超时断言和旧 WebSocket 测试桩问题未纳入本次功能修改；聚焦回归、编译切片和前端全量检查均通过。
- 功能提交不含 migration/schema 文件；未执行数据库迁移、直接 SQL、Git push 或镜像 push。
