# sub2api v0.1.152.1 发布记录

- 日期：2026-07-13
- 执行者：Devil
- Git 提交：`6bf0d9f51e0e7dde5516cff11f04ec83dc3be21b`
- 镜像：`sub2api:v0.1.152.1`
- 镜像 ID：`sha256:06132b174b7d8e49d5b88f3119b20234d31a24b483ad36607480f071412efd01`
- 当前 active：`sub2api-blue:8080`
- idle / 回滚目标：`sub2api-green:8080`，`sub2api:v0.1.151.2`

## 详细更新日志

1. 融合 OpenAI Responses compact 稳定性修复，补齐 continuation item ID、Codex tool bridge 和 remote compaction v2 行为，减少长会话压缩与续传中的状态丢失。
2. 融合 Anthropic cache usage 与 Messages 身份兼容更新，保留本地动态 Codex Desktop 身份学习和探针行为。
3. 融合用户级 Fast/Flex 选择和相关请求透传，保留本地 alpha/search 路由与按次计费规则。
4. 完成 Grok CLI/OAuth 双路径融合：OAuth 默认走 CLI 上游，请求携带稳定身份头，Responses 清理上游不支持参数，API Key 与 OAuth 均可转发。
5. 为 Grok 4.20 系列和 Composer 增加非零 fallback 价格映射，缓存输入价格统一按对应 fallback 模型计算。
6. 更新 Grok 剩余容量展示、API Key 创建、OAuth 交互和 UseKeyModal 行为，配额条按剩余容量显示。
7. 保留本地 Ops/NewApi、随机探针、OpenAI 上游诊断、VersionBadge 单 `v` 前缀和镜像小版本下拉展示。
8. 将主版本唯一来源 `backend/cmd/server/VERSION` 更新为 `v0.1.152`；镜像构建注入 `IMAGE_VERSION=v0.1.152.1` 和 revision `6bf0d9f51e0e`。

## 用户可见行为

- 管理端主版本更新为 `v0.1.152`，镜像小版本为 `v0.1.152.1`。
- OpenAI/Codex 长会话 compact、工具桥接和 continuation 兼容性更新生效。
- 用户可继续选择 Fast/Flex；alpha/search 本地路由与计费保持不变。
- Grok API Key 与 OAuth 路径均可用，Grok 4.20/Composer 不再落入零价格，容量条显示剩余额度。

## 构建与验证

- committed HEAD：`6bf0d9f51e0e7dde5516cff11f04ec83dc3be21b`，工作树干净。
- `git archive --format=tar HEAD | docker build ...`：PASS；只使用已提交源码构建不可变镜像。
- OCI 标签：version=`v0.1.152.1`、revision=`6bf0d9f51e0e`：PASS。
- 容器二进制 `--version`：`Sub2API v0.1.152 (image: v0.1.152.1, commit: 6bf0d9f51e0e)`：PASS。
- 融合阶段后端 `apicompat`、`service`、`repository`、`cmd/server`：PASS；Grok repository/service/unit 聚焦测试：PASS。
- 前端 `npm run typecheck`、全量 Vitest 和 VersionBadge/Grok 聚焦用例：PASS。
- 候选 blue `18083`：`/health`、首页、静态资源为 200；管理版本 API、`/responses`、`/v1/responses` 未登录为 401：PASS。
- 候选首次观察持续 healthy/restart 0，但启动期捕获一次 `pq: canceling statement due to user request`；未据此切流。
- 候选第二个独立 65 秒窗口：持续 healthy/restart 0，panic/fatal/migration/checksum/pq/bind/listen/rebuild 关键日志为 0：PASS。
- nginx `-t` 与 reload：PASS；active 从 green 切换到 blue。
- 切流后 `8080`、`18081`、`18083`：健康页、首页、静态资源均为 200，受保护接口未登录均为 401：PASS。
- 三个入口的 `/assets/index-j8WaZ3Bq.js` SHA-256 均为 `0145B7D6134A5D9AFC8E518FCAC490E2ADB684A3EB136948A756B028E85A4AC5`：PASS。
- 切流后独立 65 秒窗口：blue 持续 healthy/restart 0，proxy 持续 running/restart 0，联合关键日志为 0：PASS。
- green `v0.1.151.2` 保持 healthy；PostgreSQL、Redis 未重启。

## 回滚

将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 的 upstream 改回 `sub2api-green:8080`，执行 `docker exec sub2api-proxy nginx -t`，通过后执行 `docker exec sub2api-proxy nginx -s reload`。部署前备份为 `D:\sub2api-deploy\proxy\upstreams\active.conf.bak-v0.1.152.1-20260713-140342`，环境配置备份为 `D:\sub2api-deploy\.env.bak-v0.1.152.1-20260713-140342`。

## 遗留风险

- `go test ./internal/handler -count=1` 仍有融合前已存在的 6 个失败：2 个 OpenAI retry-window 断言和 4 个 OpenAI WebSocket stub/continuity 用例。
- blue 启动后出现过一次过期 request snapshot 清理 SQL 被取消；后续候选干净窗口和切流后窗口均未复现。
- 未执行管理员登录态页面操作、真实认证上游请求、Git push 或镜像 registry push。
