# v0.1.134.30 Release Log

- 日期：2026-06-11
- 执行者：Devil
- Git 提交：`e94e0b8778ab`（`feat(handler): failover 耗尽回退探测保活循环，502/503/429 单账号不终止`）
- 包含上一线上版本后的提交范围：`2f35bad37ae5..e94e0b8778ab`
- 镜像标签：`sub2api:v0.1.134.30`
- 镜像 ID：`sha256:169a9357b377bfc83bfcaafd52a56f6c3c6ef14be035a6267a82a2d9a624d40a`
- 构建标签：`org.opencontainers.image.version=v0.1.134.30`，`org.opencontainers.image.revision=e94e0b8778ab`
- 发布前 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.29`，Health `healthy`
- 发布前 idle：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.28`，Health `healthy`
- 发布后 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.30`，Health `healthy`
- 回滚目标：`sub2api-green` / `sub2api:v0.1.134.29`

## 更新内容

- 将调度探测等待的 SSE 心跳逻辑抽为 `sleepWithProbeKeepalive`，统一探测退避、心跳发送和客户端断开处理。
- 当 OpenAI `/responses` failover 切换次数耗尽且最后错误为 502/503/429 时，进入探测保活循环，不再直接终止单账号请求。
- failover 耗尽进入探测循环时先发送 `failover_exhausted_probe_pending` SSE 事件，并重置失败账号集合后继续选择账号。

## 构建记录

- 构建来源：`git archive HEAD`，确保使用已提交状态。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.30 --label org.opencontainers.image.version=v0.1.134.30 --label org.opencontainers.image.revision=e94e0b8778ab --build-arg COMMIT=e94e0b8778ab --build-arg VERSION=v0.1.134 --build-arg IMAGE_VERSION=v0.1.134.30 -`
- 构建结果：成功生成 `sub2api:v0.1.134.30`；前端构建层命中缓存，后端 release build 通过。
- 镜像确认：`docker image inspect sub2api:v0.1.134.30` 返回 `Version=v0.1.134.30`、`Revision=e94e0b8778ab`、镜像 ID `sha256:169a9357b377bfc83bfcaafd52a56f6c3c6ef14be035a6267a82a2d9a624d40a`。

## 代码验证

- GREEN：`go test -tags unit ./internal/service -run "TestOpenAISchedulerExhaustionProbe|TestProbeIntervalFromErrorCount|TestOpenAIGatewayServiceStreamFailover|TestOpenAIGatewayService_Forward_ModelCapacityErrorTriggersFailover" -count=1` 通过。
- GREEN：`go test -tags unit ./internal/handler -run TestNoSuchTest -count=1` 通过。
- GREEN：`go test ./cmd/server -run TestNoSuchTest -count=1` 通过。
- GREEN：`git diff --check` 通过。

## 候选验证

- 部署目标：idle `sub2api-blue`，本机候选端口 `127.0.0.1:18083`。
- 部署动作：只重建 `sub2api-blue`，未重启 active `sub2api-green`、PostgreSQL 或 Redis。
- `docker-compose.blue.yml` 默认镜像同步为 `sub2api:v0.1.134.30`。
- `docker exec sub2api-blue /app/sub2api --version`：`Sub2API v0.1.134 (image: v0.1.134.30, commit: e94e0b8778ab, built: 2026-06-11T14:39:12Z)`。
- `sub2api-blue` 候选窗口：`Status=running`，`Health=healthy` 持续超过 60 秒，镜像 `sub2api:v0.1.134.30`。
- `GET http://127.0.0.1:18083/health`：200。
- `GET http://127.0.0.1:18083/`：200。
- `GET http://127.0.0.1:18083/admin/settings`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:18083/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:18083/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:18083/v1/messages` 未登录：401。
- 候选日志过滤：启动窗口内出现 1 条 `[OpenAI] cleanup expired request snapshots failed err=pq: canceling statement due to user request`；切流前最近 1 分钟复查关键错误过滤命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-green:8080` 切到 `sub2api-blue:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `GET http://127.0.0.1:8080/health`：200。
- `GET http://127.0.0.1:8080/`：200。
- `GET http://127.0.0.1:8080/admin/settings`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:8080/v1/messages` 未登录：401。
- 切流后容器状态：`sub2api-blue` Health `healthy`，`sub2api-green` Health `healthy` 且继续运行，`sub2api-proxy` running。
- 切流后 `sub2api-blue` 与 `sub2api-proxy` 关键错误日志过滤：命中 0。

## 回滚

- 当前回滚容器：`sub2api-green`，镜像 `sub2api:v0.1.134.29`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-green:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- `D:\sub2api-deploy\.env` 中 `ADMIN_PASSWORD` 为空，本轮无法执行管理端登录后的 `/api/v1/admin/system/version` 接口验证；镜像版本由 Docker label 与容器内 `/app/sub2api --version` 交叉确认。
- request snapshot 清理任务在候选启动窗口出现 1 条 `pq: canceling statement due to user request`，切流前最近 1 分钟与切流后窗口无重复；该观察项延续既有启动清理现象，继续留意即可。
