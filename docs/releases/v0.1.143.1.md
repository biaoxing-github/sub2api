# v0.1.143.1 Release Record

- Date: 2026-07-03
- Actor: Devil
- Build source commit: `522de1755b1f`
- Feature commit included: `522de1755b1f`
- Image tag: `sub2api:v0.1.143.1`
- Image ID: `sha256:aaa17f5ff13c55a0b4b1e054c241c95d9a62de0d66aa877ed3a76c500b0e11ba`
- Image label version: `v0.1.143`
- Image label revision: `522de1755b1f`
- Before release active: blue, `sub2api:v0.1.140.2`
- Before release idle: green, `sub2api:v0.1.140.1`
- After release active: green, `sub2api:v0.1.143.1`
- Rollback target: blue, `sub2api:v0.1.140.2`

## Detailed Changelog

Compared with `v0.1.140.2`, this release absorbs the P0 stability and compatibility fixes from upstream `v0.1.142` and `v0.1.143`.

1. Anthropic streaming compatibility for Claude Code and thinking-disabled flows
   - Modules: `backend/internal/service/gateway_service.go`, `backend/internal/service/gateway_streaming_test.go`
   - Change: suppresses thinking blocks when the request disables thinking, and emits Claude Code noop delta keepalive for CLI versions `>= 2.1.193`.
   - User-visible impact: streaming keeps moving without dead air or protocol drift on newer Claude Code clients.

2. OpenAI gateway failover and retry hardening
   - Modules: `backend/internal/service/openai_gateway_service.go`, `backend/internal/service/openai_ws_forwarder.go`, `backend/internal/service/openai_ws_protocol_forward_test.go`, `backend/internal/service/openai_ws_ratelimit_signal_test.go`
   - Change: HTTP 413 is returned directly; pre-write WS failures for rate limit, auth, read, dial, acquire, event, and missing-final cases become failover; zero retry budget now exhausts immediately; `codex.rate_limits` events persist into account runtime limiting.
   - User-visible impact: fewer stale hangs and more predictable failover behavior when upstream rejects or throttles a request.

3. OpenAI API key and OAuth fallback compatibility
   - Modules: `backend/internal/service/openai_gateway_service.go`, `backend/internal/service/gateway_service.go`, `backend/internal/service/gateway_anthropic_apikey_passthrough_test.go`, `backend/internal/service/gateway_count_tokens_test.go`
   - Change: WS ingress records selected API key for cooldown isolation, OAuth `count_tokens` falls back to local estimation when upstream does not support it, and 413/429/401 paths stay isolated to the relevant key.
   - User-visible impact: one noisy key no longer drags the whole account down, and count-tokens remains usable on providers with weaker support.

4. Billing/model compatibility absorption
   - Modules: `backend/internal/service/billing_service.go`, `backend/internal/service/openai_codex_transform.go`, `backend/internal/service/openai_model_alias.go`, `backend/internal/service/openai_model_mapping_test.go`
   - Change: GPT-5.5 Pro is mapped and billed consistently; long-context cache creation/read prices scale with the same multiplier as input-side pricing.
   - User-visible impact: pricing and model routing stay consistent for the newer GPT-5.5 Pro family.

5. Antigravity and Claude OAuth request cleanups
   - Modules: `backend/internal/pkg/antigravity/request_transformer.go`, `backend/internal/repository/claude_oauth_service.go`
   - Change: reasoning-model requests avoid incompatible tool config and thinking parameters, while Claude OAuth token exchange stops adding unnecessary `expires_in`.
   - User-visible impact: fewer provider-side compatibility errors during model and OAuth operations.

## Pre-Release Verification

- Passed: `go test -tags unit ./internal/service -run '<P0 focused Anthropic/OpenAI/WS/count_tokens tests>' -count=1 -timeout 3m`.
- Passed: `go test ./internal/pkg/antigravity -count=1`.
- Passed: `go test ./internal/repository -run 'TestClaudeOAuth' -count=1`.
- Passed: `go test ./cmd/server -run TestNoSuchTest -count=1`.
- Passed: JSONL tail parse for `docs/feature_list.jsonl` and `docs/process_list.jsonl`.
- Passed: `git diff --cached --check`.
- FAIL: broader `go test -tags unit ./internal/service -count=1 -timeout 5m` still has 9 known non-P0 failures in group resolution, load awareness, and request-phase context cancellation.
- LIMIT: no authenticated admin API smoke was run because `ADMIN_PASSWORD` is empty in `D:\sub2api-deploy\.env`; no real upstream OpenAI request was run.

## Build And Image

- Build source: committed HEAD `522de1755b1f`.
- Image tag: `sub2api:v0.1.143.1`.
- Image ID: `sha256:aaa17f5ff13c55a0b4b1e054c241c95d9a62de0d66aa877ed3a76c500b0e11ba`.
- Image labels: `org.opencontainers.image.version=v0.1.143`, `org.opencontainers.image.revision=522de1755b1f`.
- Binary check: `docker run --rm --entrypoint /app/sub2api sub2api:v0.1.143.1 --version` printed `Sub2API v0.1.143 (image: v0.1.143.1, commit: 522de1755b1f, built: 2026-07-03T08:43:57Z)`.
- Build context: `git archive --format=tar -o tmp\release-build-v0.1.143.1-522de1755b1f.tar HEAD`, extracted to `tmp\release-build-v0.1.143.1-522de1755b1f`.
- Build note: Docker build explicitly used `ALPINE_APK_REPOSITORY=https://mirrors.ustc.edu.cn/alpine` to avoid the known TUNA Alpine 403 path.

## Candidate Deployment Verification

- Deployment target: idle green, `sub2api-green`, candidate port `127.0.0.1:18082`.
- Deployment env: `D:\sub2api-deploy\.env` now sets `SUB2API_GREEN_IMAGE=sub2api:v0.1.143.1`; blue remains `SUB2API_BLUE_IMAGE=sub2api:v0.1.140.2`.
- Safety: only idle `sub2api-green` was recreated; active `sub2api-blue`, PostgreSQL, Redis, and `sub2api-proxy` were not restarted during candidate deployment.
- GREEN: candidate `http://127.0.0.1:18082/health` returned 200.
- GREEN: candidate home page returned 200 and `/assets/index-CLRY6ujm.js` returned 200.
- GREEN: candidate unauthenticated `GET /api/v1/admin/system/version` and `GET /api/v1/admin/users` returned 401.
- GREEN: candidate unauthenticated `POST /responses` and `POST /v1/responses` returned 401, not 502.
- GREEN: `sub2api-green` ran image `sub2api:v0.1.143.1`, stayed `healthy`, and had `Restart=0`.
- GREEN: after a 75 second candidate observation window, `18082 /health` returned 200 and green app logs had zero critical release pattern matches.

## Cutover And Online Verification

- Cutover file: `D:\sub2api-deploy\proxy\upstreams\active.conf` changed from `sub2api-blue:8080` to `sub2api-green:8080`.
- GREEN: `docker exec sub2api-proxy nginx -t` passed.
- GREEN: `docker exec sub2api-proxy nginx -s reload` succeeded at `2026-07-03T16:48:45+08:00`.
- GREEN: post-cutover public `http://127.0.0.1:8080/health` returned 200.
- GREEN: post-cutover local proxy `http://127.0.0.1:18081/health` returned 200.
- GREEN: both `8080` and `18081` returned 200 for the home page and `/assets/index-CLRY6ujm.js`.
- GREEN: both `8080` and `18081` returned 401 for unauthenticated `GET /api/v1/admin/system/version`.
- GREEN: both `8080` and `18081` returned 401 for unauthenticated `POST /responses` and `POST /v1/responses`.
- GREEN: after a 65 second post-cutover window, `8080`, `18081`, and `18082` `/health` returned 200; `sub2api-green` stayed `healthy Status=running Restart=0`.
- GREEN: post-cutover app and proxy log scans over the last 120 seconds had zero critical release pattern matches.
- Current state: active green `sub2api:v0.1.143.1`; rollback blue `sub2api:v0.1.140.2`.

## Push Status

- BLOCKED: `git push -u origin codex/merge-v0.1.134-updates` failed with GitHub 403: `Permission to Wei-Shaw/sub2api.git denied to biaoxing-github`.
- Local commits are present and deploy source commit is `522de1755b1f`; remote publication requires credentials with write access to `Wei-Shaw/sub2api`.

## Rollback

If the new green active container shows health-check failures, panic/fatal logs, migration errors, bind/listen errors, or widespread gateway failures, switch the proxy back to blue:

```nginx
upstream sub2api_active {
  server sub2api-blue:8080;
  keepalive 32;
}
```

Then run:

```powershell
docker exec sub2api-proxy nginx -t
docker exec sub2api-proxy nginx -s reload
```

Rollback image: `sub2api:v0.1.140.2` on `sub2api-blue`.
