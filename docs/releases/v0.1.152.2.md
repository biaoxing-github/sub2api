# sub2api v0.1.152.2 发布记录

- 日期：2026-07-13
- 执行者：Devil
- Git 提交：`9b42afcdf0e7bd2150f336dca9dbdcb7abc23ffa`
- 镜像：`sub2api:v0.1.152.2`
- 镜像 ID：`sha256:2be52cae4e66d5c655cee0dc4499a73d6f8b7d348650cd64787c7053a4a4a170`
- 当前 active：`sub2api-green:8080`
- idle / 回滚目标：`sub2api-blue:8080`，`sub2api:v0.1.152.1`

## 详细更新日志

1. 修改 `backend/internal/service/account_test_service.go` 的 OpenAI 人工账号测试路由：非图片、非 compact 的 API Key 账号不再根据 `openai_responses_supported=false` 回退 `/v1/chat/completions`，统一请求 `/v1/responses`。
2. 人工测试请求继续复用现有 Responses payload 和 SSE 解析器，请求体使用 `input` 结构与 `stream:true`，并显式设置 `Accept: text/event-stream`。
3. 保留图片生成测试、remote compact 测试及真实网关兼容/回退行为，不扩大本轮协议变更范围。
4. 更新 `backend/internal/service/account_test_service_openai_test.go`，将旧 Chat 路径的成功、Codex 模拟头、4xx、超时、非 JSON SSE 和 API Key 禁用断言迁移到 Responses streaming。

## 用户可见行为

- 管理端发送 OpenAI 模拟测验/账号连接测试时，上游请求使用 Responses streaming，不再发送 Chat Completions 请求。
- 测验内容继续通过后端 `TestEvent` SSE 实时显示，`response.output_text.delta` 作为增量内容，`response.completed` 作为完成信号。
- 即使账号能力标记为不支持 Responses，人工模拟测验仍按用户要求使用 `/v1/responses`；图片和 compact 测试保持原行为。
- 主版本仍为 `v0.1.152`，镜像小版本更新为 `v0.1.152.2`。

## 构建与验证

- TDD RED：旧实现收到 Responses SSE 时仍走 `/v1/chat/completions` 并失败，证明测试覆盖真实旧行为。
- 协议相关 6 个 unit 用例：PASS，覆盖成功流、Codex 模拟头、4xx、超时、非 JSON SSE 和选中 API Key 禁用。
- `go test -tags unit ./internal/service -run 'TestAccountTestService_(OpenAIAPIKeyResponses|OpenAIResponsesPath|TestAccountConnectionWithResult)' -count=1`：PASS。
- `go test ./internal/service ./cmd/server -count=1`：PASS。
- 从 committed HEAD `9b42afcdf0e7` 使用 `git archive` 构建不可变镜像：PASS。
- OCI 标签：version=`v0.1.152.2`、revision=`9b42afcdf0e7`；容器二进制版本为 `Sub2API v0.1.152 (image: v0.1.152.2, commit: 9b42afcdf0e7)`：PASS。
- 仅重建 idle green；候选 `18082` 的 `/health`、首页、主静态资源为 200，管理版本 API、`/responses`、`/v1/responses` 未登录为 401：PASS。
- 候选独立观察 75 秒：green 持续 healthy/restart 0，关键日志匹配 0：PASS。
- nginx `-t` 与 reload：PASS；active 从 blue 切换到 green。
- 切流后 `8080`、`18081`、`18082` 完整冒烟：PASS；三入口 `/assets/index-j8WaZ3Bq.js` SHA-256 均为 `0145B7D6134A5D9AFC8E518FCAC490E2ADB684A3EB136948A756B028E85A4AC5`。
- 切流后独立观察 77 秒：公网 health 持续 200，green healthy/restart 0，proxy running/restart 0，联合关键日志匹配 0：PASS。
- blue `v0.1.152.1` 保持 healthy/restart 0；PostgreSQL、Redis 未重启。

## 回滚

将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 的 upstream 改回 `sub2api-blue:8080`，执行 `docker exec sub2api-proxy nginx -t`，通过后执行 `docker exec sub2api-proxy nginx -s reload`。部署前备份为 `D:\sub2api-deploy\proxy\upstreams\active.conf.bak-v0.1.152.2-20260713-150220`，环境配置备份为 `D:\sub2api-deploy\.env.bak-v0.1.152.2-20260713-150220`。

## 遗留风险

- 扩展 OpenAI service unit 运行中有 2 个融合后既有 OAuth originator 断言差异：测试仍期望 `codex_cli_rs`，实际为 `Codex Desktop`；与本轮 API Key 模拟测验协议切换无关。
- 未执行管理员登录态页面操作或真实认证上游账号的模拟测验，线上验证覆盖镜像身份、容器健康、静态资源、未授权路由和日志观察。
- 未执行 Git push 或镜像 registry push。
