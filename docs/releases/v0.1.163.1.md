# v0.1.163.1 发布记录

- 日期：2026-07-23
- 执行者：Devil
- 状态：已发布并完成线上验证

## 功能与用户影响

- Grok OAuth 和 API Key 账号均可从上游同步模型目录；OAuth 请求复用正确的 CLI 身份与模型标识解析。
- Grok 上游内容策略 403 会向客户端返回 `invalid_request_error`，不会切换账号、临时下线账号或写入配额快照。
- OpenAI Responses 的 `custom`、`tool_search` 和 `namespace` 客户端工具会降级为 Grok 可识别的 function 工具，并在 JSON、SSE 和流式回程恢复原始协议形态。

## 涉及模块

- `backend/internal/service/upstream_models.go`：Grok OAuth/API Key 模型目录同步与模型标识兼容。
- `backend/internal/service/grok_upstream_errors.go`、`openai_gateway_grok.go`：内容策略 403 的请求级隔离与账号状态保护。
- `backend/internal/pkg/apicompat`、`openai_gateway_grok_tool_protocol.go`：Responses 客户端工具和 namespace 的请求/回程兼容。

## 版本身份

- 功能提交：`b49516a4cbf1097cd75531b16c5d0178692078f2`、`7de09defd372e83d5a5fbe9f36012505f87715f1`
- 上游来源提交：`ef8275aed fix(grok): sync OAuth models and isolate policy 403s`，已按当前分支结构适配至上述功能提交。
- 主版本：`v0.1.163`
- 镜像：`sub2api:v0.1.163.1`
- 镜像 ID：`sha256:138b658b994b9b07425a989d754b9e30cb946e03c485ebc8da2aa03efb48cca1`
- 构建归档：`D:\sub2api-build\v0.1.163.1-7de09def\source.tar`
- 构建归档 SHA-256：`771FA0C5BB1DB7B918D1F16244D803F611DDD75D90FAFFA1C0C5B606280B495B`
- OCI version/revision：`v0.1.163.1` / `7de09defd372e83d5a5fbe9f36012505f87715f1`
- 切流时间：`2026-07-23T03:57:50Z`
- active：blue
- rollback：green `sub2api:v0.1.162.2`

## 验证证据

- `go test -tags=qa ./internal/pkg/apicompat -count=1`、`go test -tags=qa ./internal/service -count=1` 和 `go test -tags=qa ./... -run '^$' -count=1` 均通过。
- 镜像由 committed HEAD 的 `git archive` 构建；归档哈希、OCI 标签和镜像内 `-version` 输出均匹配主版本、镜像版本和完整提交。
- 只重建 idle blue；候选 `18083` 的 health、首页和主资源为 200，`/api/v1/admin/system/version` 与 `POST /v1/responses` 未认证均为 401。
- 候选独立观察 60 秒，13/13 轮 healthy/restart 0，关键日志命中 0。
- `nginx -t` 与 reload 通过；切流后 `8080`、`18081`、`18083` 的 health、管理 API 和 Responses 契约一致。
- 三入口主资源均为 `/assets/index-BxZZs9Us.js`，SHA-256 为 `A42501E984F11953736320115E368D316E12A31339BEE11FAA47A538AA308401`。
- 切流后独立观察 60 秒，三入口各 39 次 health 200、管理 API 401 和 Responses 401；blue/proxy 关键日志均为 0。
- 浏览器验证线上首页的桌面与 390x844 移动首屏、登录导航和控制台；页面内容完整，控制台无 warning/error。

## 回滚

- `.env` 备份：`D:\sub2api-deploy\.env.20260723-115332.bak`
- upstream 备份：`D:\sub2api-deploy\proxy\upstreams\active.conf.20260723-115332.bak`
- 回滚时将 upstream 恢复为 `sub2api-green:8080`，执行 `nginx -t` 后 reload；green 保持 healthy 可立即承接流量。

## 遗留风险与边界

- 未主动向真实 Grok 上游发送内容策略请求或执行真实模型同步；相应协议行为由自动化测试覆盖，线上验证覆盖镜像身份、网关契约、静态资源和持续运行状态。
- 本次不含数据库迁移或直接 SQL；PostgreSQL、Redis 和旧 active green 均未重建。
- 未执行 Git push 或镜像推送。
