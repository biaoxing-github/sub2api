# sub2api v0.1.155.2 发布记录

- 日期：2026-07-15
- 执行者：Devil
- Git 提交：`9e055eeba14358b5ed37395ca2167a13b9553596`
- 镜像：`sub2api:v0.1.155.2`
- 镜像 ID：`sha256:ed19ef17dfdcce6235d03a354d3fc8b76062a9a97483093a1a5953477873d8c9`
- 当前 active：`sub2api-green:8080`
- idle / 回滚目标：`sub2api-blue:8080`，`sub2api:v0.1.155.1`

## 详细更新日志

1. `NewAPICheckinConfigAccountSummary` 增加完整 `access_key`，仅通过受保护的管理员配置接口提供给签到工具页面。
2. sub2api 只读账号的 API Key 默认继续显示 `sk-前2位***后4位`，逐账号点击“显示”后才把完整 Key 写入 DOM，点击“隐藏”后恢复脱敏并从 DOM 移除。
3. 平台目录 Key 单元格支持长值换行、文本选择和稳定按钮尺寸，桌面及窄屏页面均不产生整体横向溢出。
4. service、handler 和 Vitest 增加完整 Key 接口契约、默认脱敏、显示及隐藏回归测试；并发测试夹具由无锁 slice 改为有缓冲 channel。
5. 生产 `sub-vcnovb` 已在写入前备份配置表，并幂等增加 8 个邮箱账号；当前共 9 个启用账号，新增线路为 `ip-slot-sub2-01` 至 `ip-slot-sub2-08`。
6. 真实账号 Key 未写入源码、发布记录或前端静态资源；数据库与发布验证仅使用长度和首尾脱敏值。

## 用户可见行为

- 管理员进入“工具箱 -> NewApi 签到 -> 平台目录”后，sub2api 账号仍默认展示脱敏 Key。
- 每个已配置完整 Key 的 sub2api 账号旁提供“显示/隐藏”按钮，可按需查看并选择完整 Key。
- NewAPI 上游生成的 API Key 继续只展示脱敏值，本轮不改变其展示规则。
- 侧边栏主版本应为 `v0.1.155`，版本下拉镜像小版本应为 `v0.1.155.2`。

## 构建与验证

- 业务提交 `9e055eeba143`；NewAPICheckin service、admin handler、repository 聚焦测试通过，server 二进制构建通过。
- 前端目标 Vitest 3/3、Vue typecheck 和生产构建通过；仅保留既有 Browserslist、动态导入和大 chunk 提示。
- 首次本地镜像预检发现遗漏 `VERSION=v0.1.155`，二进制主版本错误地报告 `v0.1.152`；该产物在候选部署、推送前被删除，并从同一 committed HEAD 重新构建。
- 最终镜像 OCI version=`v0.1.155.2`、revision=`9e055eeba143`；二进制报告 `Sub2API v0.1.155 (image: v0.1.155.2, commit: 9e055eeba143)`。
- 仅重建 idle green；候选 `18082` 的 health、首页和主资源为 200，受保护管理 API、`/responses`、`/v1/responses` 未登录均为 401。
- 候选启动期出现一次 `pq: canceling statement due to user request`；随后独立 65 秒观察窗 13 次 health=200、green healthy/restart 0，关键日志新增 0。
- nginx 配置检查和 reload 通过，active 从 blue 切换到 green。
- 切流后 `8080/18081/18082` 的 health、首页和主资源为 200，受保护接口未登录为 401；三入口主资源均为 `/assets/index-BAHd3kXu.js`，SHA-256 均为 `083C09C9C35E47FE7FD196FC2599E36F495EC8B11F155C97BB80BB07B7BECC49`。
- 切流后 65 秒共 13 组、三入口 39 次 health 均为 200；green/proxy restart 0，green 关键日志和 proxy error 日志新增 0。
- 线上管理主资源包含 `data-toggle-access-key`、完整 Key 标签和 `aria-pressed`，且不包含本轮 8 个真实 Key 的明文前缀。
- 生产库复核 `sub-vcnovb` 为 9 个账号、9 个启用、9 个 Key 长度 67；未调用上游、未签到、未刷新套餐或模型。
- Chrome 验证公开签到页在 1707px 和 390x844 下非空且无页面整体横向溢出；管理路由因现有登录态过期跳转登录页，未修改管理员密码或伪造令牌。
- PostgreSQL 和 Redis 未重启；blue `v0.1.155.1` 保持 healthy/restart 0，作为回滚目标保留。

## 回滚

将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 的 upstream 改回 `sub2api-blue:8080`，执行 `docker exec sub2api-proxy nginx -t`，通过后执行 `docker exec sub2api-proxy nginx -s reload`。

- 部署配置备份：`D:\sub2api-deploy\.env.bak-v0.1.155.2-20260715-131424`
- upstream 备份：`D:\sub2api-deploy\proxy\upstreams\active.conf.bak-v0.1.155.2-20260715-131424`
- 数据配置备份：`D:\sub2api-deploy\db-backups\newapi-checkin-config-before-sub-vcnovb-8-accounts-20260715-124340.sql`

## 遗留风险

- 当前 Chrome 管理登录态已过期，部署环境 `ADMIN_PASSWORD` 为空；本轮未自动验证登录态平台目录和版本徽标。完整 Key 交互由 Go/Vitest、本地 Chrome mock 页面和线上静态主资源检查共同覆盖。
- Windows CGO 未启用，未执行 `go test -race`；并发测试夹具改为 channel 后，NewAPICheckin service 聚焦测试连续 3 轮及本轮全包聚焦测试均通过。
- 未执行 Git push 或镜像 registry push。
