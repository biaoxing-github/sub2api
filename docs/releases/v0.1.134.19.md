# v0.1.134.19 Release Log

- 日期：2026-06-09
- 执行者：Devil
- Git 提交：`92d113d86e96`
- 镜像标签：`sub2api:v0.1.134.19`
- 镜像 ID：`sha256:870e9eca1d4cc770155413da93b94a877757fae601fc9c0edfd6d8ae051b8c9f`
- 构建标签：`org.opencontainers.image.version=v0.1.134.19`，`org.opencontainers.image.revision=92d113d86e96`
- 发布前 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.18`，Health `healthy`
- 发布前 idle：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.17`，Health `healthy`
- 发布后 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.19`，Health `healthy`
- 回滚目标：`sub2api-green` / `sub2api:v0.1.134.18`

## 更新内容

- 发布已提交的调度池待探测样本口径修复：真实 OpenAI 成功调用在 `RecordUsage` 成功出口写入账号级 `OpenAIPathHealthTracker.RecordSuccess`。
- 调度池“待探测”仍以账号级 path health 样本数为准；真实调用现在会累积样本，达到最小样本数后可自动进入“快且稳、可用偏慢、不稳定”等雷达状态。
- 调度器运行时成功统计统一由 `recordOpenAIAccountSuccessfulCall` 负责，避免同一次真实调用重复统计。
- 管理端版本接口展示 `image_version=v0.1.134.19`。

## 构建记录

- 构建来源：`git archive HEAD`，确保使用已提交状态。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.19 --label org.opencontainers.image.version=v0.1.134.19 --label org.opencontainers.image.revision=92d113d86e96 --build-arg COMMIT=92d113d86e96 --build-arg VERSION=v0.1.134 --build-arg IMAGE_VERSION=v0.1.134.19 -`
- 构建结果：成功生成 `sub2api:v0.1.134.19`。
- 镜像确认：`docker image inspect sub2api:v0.1.134.19` 返回 `Version=v0.1.134.19`、`Revision=92d113d86e96`。

## 候选验证

- 部署目标：idle `sub2api-blue`，本机候选端口 `127.0.0.1:18083`。
- `GET http://127.0.0.1:18083/health`：200。
- `GET http://127.0.0.1:18083/`：200。
- `GET http://127.0.0.1:18083/admin`：200。
- 管理前端静态 JS/CSS 资源：200。
- `GET http://127.0.0.1:18083/api/v1/settings/public`：200。
- `GET http://127.0.0.1:18083/api/v1/admin/accounts?page=1&page_size=1` 未登录：401。
- `POST http://127.0.0.1:18083/responses` 未登录：401。
- `GET http://127.0.0.1:18083/api/v1/admin/system/version` 管理鉴权：`version=v0.1.134`，`image_version=v0.1.134.19`。
- `GET http://127.0.0.1:18083/api/v1/admin/groups/all` 管理鉴权：200，找到分组“自用”。
- `GET http://127.0.0.1:18083/api/v1/admin/accounts/scheduling-pool?platform=openai&group=2` 管理鉴权：200。
- `GET http://127.0.0.1:18083/api/v1/admin/accounts/scheduling-pool?platform=anthropic&group=2` 管理鉴权：200。
- `sub2api-blue` 稳定窗口：`Status=running`，`Health=healthy`，镜像 `sub2api:v0.1.134.19`。
- `sub2api-blue` 关键错误日志过滤：`panic|fatal|migration.*fail|checksum|pq:|bind:|address already in use|listen tcp|rebuild failed` 命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-green:8080` 切到 `sub2api-blue:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `D:\sub2api-deploy\docker-compose.blue.yml` 默认镜像同步为 `sub2api:v0.1.134.19`。
- `GET http://127.0.0.1:8080/health`：200。
- `GET http://127.0.0.1:8080/`：200。
- `GET http://127.0.0.1:8080/admin`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:8080/api/v1/settings/public`：200。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts?page=1&page_size=1` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401。
- `GET http://127.0.0.1:8080/api/v1/admin/system/version` 管理鉴权：`version=v0.1.134`，`image_version=v0.1.134.19`。
- `GET http://127.0.0.1:8080/api/v1/admin/groups/all` 管理鉴权：200，选中分组 `2:自用`，分组总数 3。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts/scheduling-pool?platform=openai&group=2` 管理鉴权：200，`total=6`，`schedulable=6`，`degraded=0`，`blocked=0`，`items=6`。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts/scheduling-pool?platform=anthropic&group=2` 管理鉴权：200，`total=0`，`schedulable=0`，`degraded=0`，`blocked=0`，`items=0`。
- `sub2api-blue` 切流后关键错误日志过滤：命中 0。
- `sub2api-proxy` 最近 10 分钟关键错误日志过滤：命中 0。

## 回滚

- 当前回滚容器：`sub2api-green`，镜像 `sub2api:v0.1.134.18`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-green:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- 本次发布冒烟覆盖健康、静态资源、鉴权边界、`/responses` 未授权边界、管理端版本可见性、调度池 OpenAI/Anthropic 分组查询和关键日志过滤；未在切流后执行带真实业务 key 的上游付费请求。
