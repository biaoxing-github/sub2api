# sub2api v0.1.134.47 发布记录

- 日期：2026-06-16
- 执行者：Devil
- Git 提交：待回填
- 镜像标签：sub2api:v0.1.134.47
- active/idle：发布前 active=green(sub2api:v0.1.134.46)，idle=blue(sub2api:v0.1.134.45)；发布后待回填

## 更新内容

- 基于已验证的 `v0.1.134.46` 修复重新生成不可变发布镜像，继续验证 gptai-plus Responses 流的 `usage` 与 `data: [DONE]` 补齐行为。
- 本轮不新增业务代码改动，目标是按发布流程重新构建、部署、切流和真实验证。

## 验证结果

- `go test -tags unit ./internal/service -run "TestOpenAIStreaming(TerminalEventWithoutUsageAddsClientTerminalFields|TerminalEventWithDoneDoesNotDuplicateDoneMarker)|TestOpenAIGatewayServiceRecordUsage_(ZeroUsageStillWritesUsageLog|MissingObservedUsageRejectsUsageLog)" -count=1 -v`：通过。
- `go test ./cmd/server -run TestNoSuchTest -count=1`：通过。
- 构建、候选容器、切流后入口和真实 gptai-plus 验证：待回填。

## 回滚目标

- 秒级回滚：将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 切回 `sub2api-green:8080` 并 reload nginx。
- 回滚镜像：sub2api:v0.1.134.46。

## 遗留风险

- 待构建部署和真实验证后回填。
