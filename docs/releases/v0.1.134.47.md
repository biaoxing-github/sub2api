# sub2api v0.1.134.47 发布记录

- 日期：2026-06-16
- 执行者：Devil
- Git 提交：a4f984c70b2c
- 镜像标签：sub2api:v0.1.134.47
- active/idle：发布前 active=green(sub2api:v0.1.134.46)，idle=blue(sub2api:v0.1.134.45)；发布后 active=blue(sub2api:v0.1.134.47)，idle=green(sub2api:v0.1.134.46)

## 更新内容

- 基于已验证的 `v0.1.134.46` 修复重新生成不可变发布镜像，继续验证 gptai-plus Responses 流的 `usage` 与 `data: [DONE]` 补齐行为。
- 本轮不新增业务代码改动，目标是按发布流程重新构建、部署、切流和真实验证。

## 验证结果

- `go test -tags unit ./internal/service -run "TestOpenAIStreaming(TerminalEventWithoutUsageAddsClientTerminalFields|TerminalEventWithDoneDoesNotDuplicateDoneMarker)|TestOpenAIGatewayServiceRecordUsage_(ZeroUsageStillWritesUsageLog|MissingObservedUsageRejectsUsageLog)" -count=1 -v`：通过。
- `go test ./cmd/server -run TestNoSuchTest -count=1`：通过。
- `git archive HEAD | docker build ... sub2api:v0.1.134.47`：通过，镜像 label revision=cca6089f35e1。
- blue 候选 `18083`：`/health` 200、管理前端 200、未登录 `/api/v1/admin/system/version` 401、未登录 `POST /responses` 401，容器 healthy。
- blue 启动日志：启动期出现一次已知 request snapshot cleanup `pq: canceling statement due to user request`；等待 70 秒观察窗后关键日志命中 0。
- 切流后 `8080` 与 `18081`：`/health` 200；管理前端 200；未登录 `/api/v1/admin/system/version` 401；未登录 `/responses` 401；切流后 blue 关键日志命中 0。
- 切流后真实 gptai-plus 长文本验证：临时隔离 key 命中 `account_id=434`，模型 `gpt-5.5`，HTTP 200；保存 `.codex/gptai-plus-v047-verify-gpt55-20260616203223.sse`；`delta_events=32`、`terminal_events=2`、`usage_events=1`、`zero_usage_in_completed=true`、`done_markers=1`、`text_chars=673`、`has_exact_v047_done_check=true`。
- 验证后清理临时 group/key/account_groups，并恢复 `account_id=434 schedulable=false`。

## 回滚目标

- 秒级回滚：将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 切回 `sub2api-green:8080` 并 reload nginx。
- 回滚镜像：sub2api:v0.1.134.46。

## 遗留风险

- 当前 GitHub 凭据对 `Wei-Shaw/sub2api` 仍无推送权限；本轮执行本机构建、部署、切流和真实验证。
- `account_id=434` 的真实上游余额记录此前显示可用额度为负，本轮只在临时验证窗口恢复可调度并验证流式协议补齐，验证后已恢复为 `schedulable=false`。
