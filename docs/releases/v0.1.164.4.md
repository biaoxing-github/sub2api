# v0.1.164.4 发布记录

- 日期：2026-07-25
- 执行者：Devil
- Git 提交：`c5d923d81d0d930e804816808b388ce55965cdf5`
- 镜像：`sub2api:v0.1.164.4`
- 正式颜色：blue（`sub2api-blue`）
- 回滚目标：green（`sub2api:v0.1.164.3`）

## 功能与用户影响

- NewAPI 兑换账号中包含已引用 Key 的账号置顶，并在账号主行高亮显示引用 Key 数量。
- 兑换工具的 Key 展示、分组管理、数据库引用与关联操作拆分到同一弹窗的三个 Tab 中。
- 签到工具的平台目录按站点将包含已引用 Key 的账号置顶，并在账号行高亮显示引用数量。
- 已引用 Key 在弹窗和签到工具 Key 列表内继续置顶；显示、创建、分组更新、追加和覆盖行为保持不变。

## 发布过程

- 从提交 `c5d923d81` 的 `git archive HEAD` 构建不可变镜像，归档 SHA-256 为 `08D5BA3EC132C2F7E4C8FB96E071BA61FCE431732A6863AE5742DA85BAF25570`。
- 镜像 OCI version 为 `v0.1.164.4`，revision 为 `c5d923d81d0d930e804816808b388ce55965cdf5`。
- 仅重建 idle `sub2api-blue`，未重启 PostgreSQL、Redis 或 active green。
- blue 候选通过健康、未登录接口、日志和 60 秒稳定窗口后，Nginx upstream 切换到 blue。
- `sub2api-green` 保持 `v0.1.164.3`、healthy、restart 0，作为回滚目标。

## 验证证据

- 聚焦 Vitest：2 个测试文件、7 条测试全部通过。
- `pnpm run typecheck` 通过。
- `pnpm run build` 通过。
- 候选 `/health` 和首页返回 200；未登录管理接口与 `POST /v1/responses` 返回 401。
- 切流前后 blue 均 healthy、restart 0，候选和正式观察窗口异常日志命中 0。
- `8080`、`18081`、`18083` 引用同一静态资源，SHA-256 为 `E7CF0CFDE77F12EFC4CA034546567BFBB174F3A564E0C47F3392150D05C65843`。

## 遗留风险

- 按用户要求未执行正式页面人工交互验收，账号高亮、Tab 切换和关联操作由用户自行验证。
