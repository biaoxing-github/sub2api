# v0.1.168.7 发布记录

- 日期：2026-07-31
- 执行者：Devil
- 功能：归一化 DeepSeek 原生 Responses reasoning 事件

## 用户影响

- DeepSeek 原生 Responses 在上游没有原生 reasoning summary 时，将 `response.reasoning_text.delta/done` 转为 `response.reasoning_summary_text.delta/done`，并补齐 summary part 的 added/done 生命周期事件。
- reasoning output item 的终态内容同步移动到 `summary`，`response.output_item.done`、`response.completed.response.output`、普通非流式 JSON 和 SSE-to-JSON 保持一致。
- 仅 DeepSeek 路径启用转换；原生 summary、非 DeepSeek、1M Responses 请求、工具调用、usage 与现有计费数据保持原行为。

## 代码与构建

- 功能提交：`9ff70e157c8ba1d1bfbbf08421410633fb0a1919`
- 最新语义化 tag：`v0.1.168`
- 镜像：`sub2api:v0.1.168.7`
- 镜像 ID：`sha256:9c983f66b4a1e967388d9bfd9e95ed2b3e36dad1c1c172293947dc41beed8515`
- OCI revision：`9ff70e157c8ba1d1bfbbf08421410633fb0a1919`
- OCI version：`v0.1.168.7`
- 二进制身份：`Sub2API v0.1.168 (image: v0.1.168.7, commit: 9ff70e157c8ba1d1bfbbf08421410633fb0a1919)`
- `git archive HEAD` SHA-256：`81A102429244BBBAC1DA949E0C7BD466926469F1CA980A2654D6F619539C043B`

## 部署

- 部署方式：蓝绿发布，只重建 idle green 应用容器。
- 正式颜色：green，`sub2api-green` 使用 `sub2api:v0.1.168.7`。
- 回滚目标：blue，`sub2api-blue` 保留 `sub2api:v0.1.168.6`。
- active upstream：`sub2api-green:8080`。
- 切流时间：`2026-07-31 17:25:37 +08:00`。
- 部署前备份：
  - `D:\sub2api-deploy\.env.backup-v0.1.168.7-20260731-172230`
  - `D:\sub2api-deploy\proxy\upstreams\active.conf.backup-v0.1.168.7-20260731-172230`
- `nginx -t` 通过并成功 reload；PostgreSQL、Redis 未重启，均保持 healthy/restart 0。

## 验证证据

- DeepSeek 转换器、流式/非流式接入和既有 passthrough/compact 聚焦回归通过；`go test ./internal/service -count=1` 与 server/handler/service 编译切片通过。
- green 候选 smoke：`18082/health`、首页和主 JS 资源均 200；未登录管理 API 与 `/v1/responses` 均 401；healthy/restart 0；关键日志 0。
- 候选独立观察：61.4 秒、13/13 次采样通过，关键日志 0。
- 切流后入口验证：`8080`、`18081`、`18082` 的 health、首页和主资源均 200；资源 `/assets/index-BKWgQvnT.js` 的 SHA-256 均为 `0E3181EEBC4579985E32B7852A2970DE13AEC9B4473F4A5F6FBA56838B765E03`。
- 切流后独立观察：64.6 秒、13/13 轮 `8080`、`18081`、`18082`、`18083` health 全部 200；green/blue 均 healthy/restart 0，green/proxy 关键日志 0。
- 已登录 Chrome 管理端显示主版本 `v0.1.168`，版本详情显示镜像版本 `v0.1.168.7`。
- 最终复核：green `.7`、blue `.6`、PostgreSQL、Redis 均 healthy/restart 0；`active.conf` 与 `nginx -T` 均指向 green。

## 遗留风险

- 未主动向生产 DeepSeek 上游注入请求；协议转换由本地自动化测试覆盖，线上验证聚焦发布身份、入口契约和运行稳定性。
- unit-tag 全包与 `go vet ./internal/service` 仍有当前分支既有失败，已在 `.codex/testing.md` 记录，不涉及本次改动。
- 未执行生产数据库写入、Git push 或镜像仓库 push。
