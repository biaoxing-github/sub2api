# v0.1.134.28 Release Log

- 日期：2026-06-11
- 执行者：Devil
- Git 提交：`fc0c8cdd0da8`（`test(account): 固定 API Key 禁用恢复测试时间`）
- 包含上一线上版本后的提交范围：`e8b2b676e..fc0c8cdd0da8`
- 镜像标签：`sub2api:v0.1.134.28`
- 镜像 ID：`sha256:11bd45565c2688a2d4d6d04883b7e6565a315564484563b8b42b97f298e94204`
- 构建标签：`org.opencontainers.image.version=v0.1.134.28`，`org.opencontainers.image.revision=fc0c8cdd0da8`
- 发布前 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.27`，Health `healthy`
- 发布前 idle：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.26`，Health `healthy`
- 发布后 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.28`，Health `healthy`
- 回滚目标：`sub2api-green` / `sub2api:v0.1.134.27`

## 更新内容

- 吸收 v0.1.136 网关能力：idempotency UTF-8 截断、`MarkResponseCommitted` 基础设施、OpenAI failover 缓存、调度日志优化和账号组调度索引迁移。
- 补齐 Gemini / Antigravity / Bedrock 请求路径的 committed 标记、beta token 清理和顶层字段过滤。
- 管理端调度池新增手动探测入口；调度器支持按错误次数分级探测间隔，并把 exhausted 循环接入 30/60 分钟探测节奏。
- 账号页用量卡片改为账号池汇总；Anthropic API Key 支持上游余额配置、刷新和编辑页展示。
- API Key 临时禁用支持按错误次数分级自动恢复，过期禁用记录会懒恢复。
- 删除 `AccountService.RecoverAccountAfterManualProbe` 孤儿实现，保留实际使用的恢复路径。
- 测试修复：API Key 禁用恢复测试固定 `Account.nowForTest`，避免 2023 年测试数据在 2026 年当前时间下被自动恢复。

## 构建记录

- 构建来源：`git archive HEAD`，确保使用已提交状态。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.28 --label org.opencontainers.image.version=v0.1.134.28 --label org.opencontainers.image.revision=fc0c8cdd0da8 --build-arg COMMIT=fc0c8cdd0da8 --build-arg VERSION=v0.1.134 --build-arg IMAGE_VERSION=v0.1.134.28 -`
- 构建结果：成功生成 `sub2api:v0.1.134.28`；前端生产 build 与后端 release build 均通过。
- 构建观察：Vite 输出既有 dynamic import / chunk size warning，无构建失败。
- 镜像确认：`docker image inspect sub2api:v0.1.134.28` 返回 `Version=v0.1.134.28`、`Revision=fc0c8cdd0da8`、镜像 ID `sha256:11bd45565c2688a2d4d6d04883b7e6565a315564484563b8b42b97f298e94204`。

## 代码验证

- GREEN：`go test -tags unit ./internal/service -run "TestAccountGetAPIKey|TestAccountDisableAPIKey|TestAccountGetAPIKeys|TestAccountRemoveAPIKey|TestAccountRestoreAPIKey|TestBuildAccountUsageSummary|TestUpstreamBalanceService|TestOpenAISchedulerExhaustionProbe|TestRecoverAccountAfterManualProbe|TestIdempotency|TestOpenAIPathHealth" -count=1` 通过。
- GREEN：`go test -tags unit ./internal/handler/admin -run "TestAccountHandler(GetUsageSummary|DashboardSummary|ActionItems|RefreshUpstreamBalance|ManualProbe)" -count=1` 通过。
- GREEN：`go test ./cmd/server -run TestNoSuchTest -count=1` 通过。
- GREEN：`npm run test:run -- src/api/__tests__/admin.accounts.spec.ts src/components/account/__tests__/EditAccountModal.spec.ts src/components/admin/account/__tests__/AccountUsageSummaryPanel.spec.ts src/views/admin/__tests__/AccountSchedulingPoolView.spec.ts src/composables/__tests__/useModelWhitelist.spec.ts` 通过，5 files / 54 tests passed。
- GREEN：`npm run test:run -- src/views/admin/__tests__/AccountsView.bulkEdit.spec.ts -t "passes total account cost sorting to the server and displays usage totals"` 通过。
- GREEN：`npm run typecheck` 通过。
- GREEN：`git diff --check` 通过。
- OBSERVED：`go test -tags unit ./internal/service ./internal/handler/admin -count=1` 宽包运行中 `internal/handler/admin` 通过，但 `internal/service` 仍命中既有 OpenAI 旧链路测试期望差异和 stub nil panic；本轮按账号/调度/管理端影响面采用聚焦测试作为发布挡板。
- OBSERVED：完整账号页 bulk edit 测试组合中排序用例存在异步/状态串扰；该用例单独运行通过，本轮未把组合串扰作为发布阻断。

## 候选验证

- 部署目标：idle `sub2api-blue`，本机候选端口 `127.0.0.1:18083`。
- 部署动作：只重建 `sub2api-blue`，未重启 active `sub2api-green`、PostgreSQL 或 Redis。
- `docker-compose.blue.yml` 默认镜像同步为 `sub2api:v0.1.134.28`。
- `docker exec sub2api-blue /app/sub2api --version`：`Sub2API v0.1.134 (image: v0.1.134.28, commit: fc0c8cdd0da8, built: 2026-06-11T08:21:10Z)`。
- `sub2api-blue` 候选窗口：`Status=running`，`Health=healthy`，镜像 `sub2api:v0.1.134.28`。
- `GET http://127.0.0.1:18083/health`：200，`{"status":"ok"}`。
- `GET http://127.0.0.1:18083/`：200。
- `GET http://127.0.0.1:18083/admin/settings`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:18083/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:18083/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:18083/v1/messages` 未登录：401，确认 Anthropic 入口鉴权边界正常。
- 候选日志过滤：启动窗口内出现 1 条 `[OpenAI] cleanup expired request snapshots failed err=pq: canceling statement due to user request`；切流前最近 1 分钟复查关键错误过滤命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-green:8080` 切到 `sub2api-blue:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `GET http://127.0.0.1:8080/health`：200，`{"status":"ok"}`。
- `GET http://127.0.0.1:8080/`：200。
- `GET http://127.0.0.1:8080/admin/settings`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:8080/v1/messages` 未登录：401。
- 切流后容器状态：`sub2api-blue` Health `healthy`，`sub2api-green` Health `healthy` 且继续运行，`sub2api-proxy` running。
- 切流后 `sub2api-blue` 与 `sub2api-proxy` 关键错误日志过滤：命中 0。

## 回滚

- 当前回滚容器：`sub2api-green`，镜像 `sub2api:v0.1.134.27`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-green:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- `D:\sub2api-deploy\.env` 中 `ADMIN_PASSWORD` 为空，本轮无法执行管理端登录后的 `/api/v1/admin/system/version` 接口验证；镜像版本由 Docker label 与容器内 `/app/sub2api --version` 交叉确认。
- request snapshot 清理任务在候选启动窗口出现 1 条 `pq: canceling statement due to user request`，切流前最近 1 分钟与切流后窗口无重复；该观察项延续既有启动清理现象，继续留意即可。
- 宽包 service 测试仍有 OpenAI 旧链路测试噪声；已在 `verification.md` 保留具体失败类别，后续应单独整理。
