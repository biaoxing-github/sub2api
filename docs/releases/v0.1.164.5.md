# v0.1.164.5 发布记录

- 日期：2026-07-26
- 执行者：Devil
- Git 提交：`d545dbb62ff6f48a6648203dd3422b6fe6f6197a`
- 镜像：`sub2api:v0.1.164.5`
- 镜像 ID：`sha256:58e96300b85039453649e23189c7b3843b96a1c5ccadfdcc1397da180e17cd79`
- 正式颜色：green（`sub2api-green`）
- 回滚目标：blue（`sub2api:v0.1.164.4`）

## 功能与用户影响

- OpenAI API Key 账号的 Responses、Chat、fallback、图片、WebSocket、模型同步和各类探测统一使用账号级 Codex 客户端身份。
- 最终 HTTP 传输边界会把空 User-Agent 或 `Go-http-client/*` 改为 Codex User-Agent，账号自定义非 Go UA 和真实 Codex UA 保持不变。
- OAuth 账号继续使用既有 Codex 身份配对逻辑；外部通知等非账号上游请求不在本次模拟范围内。

## 发布过程

- 从提交 `d545dbb62` 的 `git archive HEAD` 构建不可变镜像，归档 SHA-256 为 `9328E94E1DF55548A792E1234965CA227362F0D4C1726B6828E624E5F78E53B4`。
- 镜像 OCI version 为 `v0.1.164.5`，revision 为完整功能提交；容器二进制输出 `Sub2API v0.1.164 (image: v0.1.164.5)`。
- 只重建 idle `sub2api-green`，未重建当时 active blue，也未重启 PostgreSQL 或 Redis。
- green 候选通过冒烟和独立 60 秒观察后，`nginx -t` 通过，正式 upstream 从 blue 切换到 green。
- 切流后完成独立 65 秒观察；blue 保持 healthy，作为回滚目标。

## 验证证据

- 身份聚焦测试、完整 service、完整 repository 和 handler 编译切片通过；`git diff --check` 通过。
- 候选 `18082` 的 `/health`、首页和主资源返回 200；未登录管理接口和 `POST /v1/responses` 返回 401。
- 候选独立窗口 13 次健康采样全部通过，green 始终 healthy、restart 0，关键日志命中 0。
- 切流后 `8080`、`18081`、`18082`、`18083` 的健康检查均返回 200；主资源 SHA-256 为 `E7CF0CFDE77F12EFC4CA034546567BFBB174F3A564E0C47F3392150D05C65843`。
- 发布后独立窗口 13 轮四端口健康检查全部通过；green、PostgreSQL、Redis 始终 healthy、restart 0，green 和代理关键日志命中 0。
- 无名公益（508）真实手工请求不再返回 nginx 403，改为上游应用层 `503 Service temporarily unavailable`，总耗时 1224ms；green 日志记录 `status_code=503`、`reason=service_unavailable`。
- 君公益（494）真实手工请求成功，模型 `gpt-5.6-sol`，首 Token 4364ms、总耗时 4624ms，账号保持 `active/probe_success`。
- 从切流开始，green 日志中 `Go-http-client/1.1`、`Access forbidden` 和真实 403 状态均为 0 次。

## 遗留风险

- 无名公益已穿过客户端限制，但其上游服务当前仍返回独立 503，因此账号保持 `error/schedulable=false`，不能作为可用业务出口。
- 账号 508 的 `error_message` 字段仍保留切流前历史 403 文本；本次新请求的实际状态以 green 运行日志中的 503 为准，历史错误文案刷新不属于本次客户端身份修复。
- 回滚 blue 当前 healthy，restart 计数为 6；该计数在本次发布接手前已存在，本次未重建或重启 blue。
