# v0.1.134.18 Release Log

- 日期：2026-06-09
- 执行者：Devil
- Git 提交：`72372cab5040`
- 镜像标签：`sub2api:v0.1.134.18`
- 镜像 ID：`sha256:d9056cc3f0cb3acf7190b8a1220980b7df81ca29d2288ea1ae77019fb47c1c22`
- 构建标签：`org.opencontainers.image.version=v0.1.134.18`，`org.opencontainers.image.revision=72372cab5040`
- 发布前 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.17`，Health `healthy`
- 发布前 idle：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.16`，Health `healthy`
- 发布后 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.18`，Health `healthy`
- 回滚目标：`sub2api-blue` / `sub2api:v0.1.134.17`

## 更新内容

- 发布已提交的调度池异常展示修复：调度池健康列复用账号页可用性雷达 badge，直接展示“不稳定、余额风险、冷却中、待探测”等状态。
- 调度池原因列合并调度原因、派生健康原因、雷达原因和负载建议原因。
- 补齐 `light_abnormal`、`moderate_abnormal`、`temp_unschedulable`、`quota_low`、`quota_exhausted`、`disabled` 等派生健康状态的标签颜色和行底色。
- 管理端版本徽标通过 `/api/v1/admin/system/check-updates` 展示 `image_version=v0.1.134.18`。

## 构建记录

- 构建来源：`git archive HEAD`，确保使用已提交状态。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.18 --label org.opencontainers.image.version=v0.1.134.18 --label org.opencontainers.image.revision=72372cab5040 --build-arg COMMIT=72372cab5040 --build-arg VERSION=v0.1.134 --build-arg IMAGE_VERSION=v0.1.134.18 -`
- 构建结果：成功生成 `sub2api:v0.1.134.18`。
- 构建输出确认前端产物包含新的 `AccountSchedulingPoolView-m9WAgfjX.js` 和 `AccountAvailabilityRadarBadge` chunk。

## 候选验证

- 部署目标：idle `sub2api-green`，本机候选端口 `127.0.0.1:18082`。
- `GET http://127.0.0.1:18082/health`：200。
- `GET http://127.0.0.1:18082/`：200。
- `GET http://127.0.0.1:18082/admin`：200。
- 管理前端静态 JS 资源：200。
- `GET http://127.0.0.1:18082/api/v1/admin/accounts?page=1&page_size=1` 未登录：401。
- `POST http://127.0.0.1:18082/responses` 未登录：401。
- `GET http://127.0.0.1:18082/api/v1/settings/public`：200，公开版本 `version=v0.1.134`。
- `GET http://127.0.0.1:18082/api/v1/admin/system/version` 管理鉴权：`version=v0.1.134`，`image_version=v0.1.134.18`。
- `GET http://127.0.0.1:18082/api/v1/admin/system/check-updates` 管理鉴权：`image_version=v0.1.134.18`，`build_type=release`。
- `GET http://127.0.0.1:18082/api/v1/admin/groups/all` 管理鉴权：200，找到分组“自用”。
- `GET http://127.0.0.1:18082/api/v1/admin/accounts/scheduling-pool?platform=openai&group=2` 管理鉴权：200，`total=6`，其中 6 条带可用性雷达异常状态 `needs_probe/normal`。
- `GET http://127.0.0.1:18082/api/v1/admin/accounts/scheduling-pool?platform=anthropic&group=2` 管理鉴权：200，`total=0`。
- `sub2api-green` 60 秒稳定窗口：`Status=running`，`Health=healthy`，镜像 `sub2api:v0.1.134.18`。
- `sub2api-green` 关键错误日志过滤：`panic|fatal|migration.*fail|checksum|pq:|bind:|address already in use|listen tcp|rebuild failed` 命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-blue:8080` 切到 `sub2api-green:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `D:\sub2api-deploy\docker-compose.green.yml` 默认镜像同步为 `sub2api:v0.1.134.18`，`docker compose -f docker-compose.green.yml config --services` 通过。
- `GET http://127.0.0.1:8080/health`：200。
- `GET http://127.0.0.1:8080/`：200。
- `GET http://127.0.0.1:8080/admin`：200。
- 管理前端静态 JS 资源：200。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts?page=1&page_size=1` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401。
- `GET http://127.0.0.1:8080/api/v1/admin/system/version` 管理鉴权：`version=v0.1.134`，`image_version=v0.1.134.18`。
- `GET http://127.0.0.1:8080/api/v1/admin/system/check-updates` 管理鉴权：`image_version=v0.1.134.18`，`build_type=release`。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts/scheduling-pool?platform=openai&group=2` 管理鉴权：200，`total=6`，其中 6 条带可用性雷达异常状态 `needs_probe/normal`。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts/scheduling-pool?platform=anthropic&group=2` 管理鉴权：200，`total=0`。
- `sub2api-green` 切流后关键错误日志过滤：命中 0。
- `sub2api-proxy` 最近 15 分钟关键错误日志过滤：命中 0。

## 回滚

- 当前回滚容器：`sub2api-blue`，镜像 `sub2api:v0.1.134.17`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-blue:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- 本次发布冒烟覆盖健康、静态资源、鉴权边界、`/responses` 未授权边界、管理端版本可见性、调度池 OpenAI/Anthropic 分组查询、真实调度池雷达异常字段返回和关键日志过滤；未在切流后执行带真实业务 key 的上游付费请求。
