# v0.1.168.5 发布记录

- 日期：2026-07-31
- 执行者：Devil
- 功能：管理端仪表盘账号消费榜

## 用户影响

- 仪表盘新增“账号消费榜”，按账号成本从高到低展示账号、平台、请求数、Token 和账号成本。
- 支持“今天”“近 24 小时”“近 7 天”三个时间范围，切换后同步刷新榜单和汇总。
- 账号成本沿用 `COALESCE(account_stats_cost, total_cost) * account_rate_multiplier` 口径；无数据库 schema 或 migration 变更。

## 代码与构建

- 功能提交：`c43398693093639d65e636f82d8bbf9b82a03c89`
- 最新语义化 tag：`v0.1.168`
- 镜像：`sub2api:v0.1.168.5`
- 镜像 ID：`sha256:4291ee4fc784fe73ad42181401420475fa657520559e0619e7eb9727a6e8a612`
- OCI revision：`c43398693093639d65e636f82d8bbf9b82a03c89`
- OCI version：`v0.1.168`
- 二进制身份：`Sub2API v0.1.168 (image: v0.1.168.5, commit: c43398693093639d65e636f82d8bbf9b82a03c89)`
- `git archive HEAD` 归档：`D:\sub2api-deploy\build\v0.1.168.4-c4339869.tar`
- 归档 SHA-256：`F753EE8371457FC7AF704442ADC99E9BC1A4DC9F49FD7B9ECE2A7D1B0D0BF0A8`
- `v0.1.168.4` 因 OCI 标签不完整记录为失败候选，未部署、未覆盖；正式候选使用不可复用的新后缀 `.5`。

## 部署

- 部署方式：蓝绿发布，只重建 idle green 应用容器。
- 正式颜色：green，`sub2api-green` 使用 `sub2api:v0.1.168.5`。
- 回滚目标：blue，`sub2api-blue` 保留 `sub2api:v0.1.168.2`。
- active upstream：`sub2api-green:8080`。
- 部署前备份：
  - `D:\sub2api-deploy\backups\.env.before-v0.1.168.5-20260731-084115.bak`
  - `D:\sub2api-deploy\backups\active.conf.before-v0.1.168.5-20260731-084115.bak`
- `nginx -t` 两次通过，reload 成功。
- PostgreSQL、Redis 未重启，均保持 running/restart 0。

## 验证证据

- 后端 repository/handler 聚焦测试通过；service/server/routes 编译与测试切片通过。
- 前端新增组件测试通过；全量 Vitest、`pnpm run typecheck`、生产构建（974 modules）通过；`git diff --check` 通过。
- 生产 PostgreSQL 只读核验：近 7 天 11268 条用量记录，带账号记录 11268 条，账号成本汇总 `1332.55102143`；新 SQL 返回真实账号排名。
- green 候选 smoke：`18082/health`、首页、主 JS 资源均 200；未登录 `/api/v1/admin/system/version` 和 `/v1/responses` 均 401；healthy/restart 0；关键日志 0。
- 候选独立观察：61 秒、13 次采样全部 `running|healthy|0`，health 全 200，关键日志 0。
- 切流后入口验证：`8080`、`18081`、`18082` 均 health 200，主资源 `/assets/index-Cvuep_TW.js` 和 SHA-256 `D11AAB3D5EB8BD0ED4372FBE829B5DD730CE86C083EB2B494570D662FCB6AA48` 一致，鉴权契约保持 401。
- 切流后独立观察：64 秒、13 轮公网 health 全 200；green/proxy 关键日志 0；blue/green healthy，green restart 0，blue restart 8 未增加。
- 已登录 Chrome 管理端真实交互：仪表盘展示“账号消费榜”，默认“今天”降序；切换“近 24 小时”和“近 7 天”后榜单与汇总刷新，首位成本高于后续行；移动宽度 390x844 的 DOM 仍完整显示榜单和三个时间 tab。
- 浏览器控制台仅有 Tauri 环境提示，无应用 error/warn；桌面截图已捕获，移动截图 API 在远程 CDP 上超时，DOM/交互验证不受影响。

## 遗留风险

- 未执行真实业务请求注入或生产数据库写入；本次功能仅读取现有用量数据。
- 未推送镜像仓库；Git 功能分支和本次发布记录分支将推送到 fork 远端。
