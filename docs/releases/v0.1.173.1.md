# v0.1.173.1 发布记录

- 日期：2026-08-10
- 执行者：Devil
- Git 提交：`a31b04cbff4db643a14491a5a89381395814296e`
- 镜像：`sub2api:v0.1.173.1`
- 镜像 ID：`sha256:fec90b50211c6b99b305c91e2f935deff9afb388e7b3e145ab4f5a4d42565917`
- 正式颜色：blue（`sub2api-blue`）
- 回滚目标：green（`sub2api:v0.1.172.2`）

## 功能与用户影响

- 平台目录过滤 NewAPI/Sub2API 上游明确标记为已删除的 Key，仅展示仍存在的 Key 及其分组。
- 删除签到平台后，清理平台账号、API Key 缓存、余额缓存、签到历史、月度记录和运行明细，避免再次导入复用旧数据。
- 现有完整 Key 查看、分组更新以及向主平台账号追加或替换 Key 的能力保持不变。

## 发布过程

- 最新语义 tag 为 `v0.1.173`，本版本线首个不可变镜像为 `v0.1.173.1`。
- 从 committed HEAD 的 `git archive` 构建，归档 `D:\sub2api-build\v0.1.173.1-a31b04cbf\source.tar` SHA-256 为 `A910C65B0F5973A419D928A7C5206882942182B8781064510A40CA1AE7E37121`。
- OCI version/revision 与容器内二进制一致：主版本 `v0.1.173`、镜像版本 `v0.1.173.1`、提交 `a31b04cbff4db643a14491a5a89381395814296e`。
- 仅重建 idle blue；候选通过后执行 `nginx -t`，upstream 从 green 切到 blue并 reload。

## 验证证据

- service/repository 签到聚焦测试、repository 删除事务 SQLMock、routes/server 编译和前端签到 Vitest 4/4 通过。
- 候选 `/health`、首页、主资源和签到工具页为 200；未登录管理接口与 `/v1/responses` 为 401。
- 候选独立观察 61.2 秒、13 次采样全部 200，blue `healthy/restart=0`，关键日志 0。
- 切流后 `8080`、`18081`、`18083` 主资源均为 `/assets/index-Ciq7rlf7.js`，SHA-256 为 `EEC924C406FEF905A72F1B0B23E82978952C34F0F2F5B1451D36D67868979A4B`。
- 切流后独立观察 63.5 秒、13 轮四入口 health 全部 200；blue/proxy 关键日志 0；PostgreSQL、Redis healthy/restart 0。

## 镜像清理与边界

- 删除未被容器或部署配置引用的历史镜像：`v0.1.172.1`、`v0.1.168.8`、`v0.1.168.9`、`v0.1.168.10`、`v0.1.168.11`、`v0.1.168.12`。
- 当前仅保留 active `v0.1.173.1` 和 rollback `v0.1.172.2`。
- 功能提交不含 migration/schema 文件；未执行直接 SQL、Git push 或镜像 push。
