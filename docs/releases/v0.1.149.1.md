# sub2api v0.1.149.1 发布记录

- 日期：2026-07-10
- 执行者：Devil
- 主版本：`v0.1.149`
- 镜像版本：`sub2api:v0.1.149.1`
- 构建提交：`e56621837035`
- 功能提交：`70d6606ef`、`37a994938`、`e56621837`
- 镜像 ID：`sha256:a278012b8ecc621fe2ceeb728b6984c00639f8de850d0b4013ee1842b0825c8a`
- 发布后 active：`sub2api-green:8080`
- 发布后 rollback：`sub2api-blue:8080` / `sub2api:v0.1.146.7`

## 详细更新日志

1. Grok 图片、配额、OAuth 和模型同步：
   - 涉及模块：`backend/internal/pkg/xai`、`backend/internal/service/openai_images.go`、`account_usage_service.go`、`frontend/src/components/account/AccountUsageCell.vue`、`frontend/src/composables/useGrokOAuth.ts`、`useModelWhitelist.ts`。
   - 用户可见行为：Grok Imagine 图片模型可被识别；JSON/multipart 转发移除上游不接受的 `size`，但保留原尺寸计费；账号页显示被动额度和本地当日账号/用户费用；OAuth 失败显示结构化恢复提示；新增 Composer 2.5 alias。

2. 管理员角色闭环：
   - 涉及模块：`backend/internal/service/admin_service.go`、`backend/internal/handler/admin/user_handler.go`、用户创建/编辑前端组件。
   - 用户可见行为：管理员可创建或编辑 `user/admin` 角色；非法角色被拒绝；最后一个管理员和当前操作者不能被错误降级；角色变更会记录审计并失效认证缓存。

3. 用户 Token 排行和用量页三标签：
   - 涉及模块：dashboard handler、usage repository、`UserTokenRanking.vue`、`UsageView.vue`、`UsageFilters.vue`、`UsageTable.vue`。
   - 用户可见行为：用量页提供用量、错误、用户排行三个标签；排行支持输入/输出/缓存/总 Token、请求数和费用排序；点击用户可回到用量明细；错误和排行每次重新进入都会读取最新数据库结果；首 Token 与总耗时合并为延迟健康列。

4. Ops 错误筛选和稳定分页：
   - 涉及模块：`backend/internal/handler/admin/ops_handler.go`、`backend/internal/service/ops_models.go`、`backend/internal/repository/ops_repo.go`、`OpsErrorLogTable.vue`。
   - 用户可见行为：用户、API Key、模型、分类、状态和排序参数真实进入 SQL；普通错误列表保留客户端可见状态守卫；上游专用列表可查看恢复态错误；排序列使用白名单并以 `id` 稳定分页。

5. 版本与数据边界：
   - `backend/cmd/server/VERSION` 更新为 `v0.1.149`。
   - 本次没有 migration、schema、SQL、新表或历史数据迁移；既有 `usage_logs`、`ops_error_logs`、`users`、`api_keys`、`accounts`、`groups` 继续承载排行、费用、错误和角色维度。

## 提交前验证

- PASS：Grok service 聚焦测试和 `internal/pkg/xai` 包测试。
- PASS：Grok 前端 3 个文件共 29 tests。
- PASS：管理员角色 4 个 `unit` 测试。
- PASS：dashboard/Ops handler 与 usage/Ops repository 聚焦测试。
- PASS：用量页 4 个前端文件共 14 tests。
- PASS：前端 `npm run typecheck` 和目标文件 scoped ESLint。
- PASS：`go test -tags unit ./cmd/server -run TestNonExistent -count=0`。
- PASS：三次提交前 `git diff --cached --check`。

## 构建结果

- 从 committed HEAD `e56621837035` 的 `git archive` 构建，不包含 `.codegraph/daemon.pid`、`backend/cmd/codex-live-probe/`、`tmp_body.json`。
- 镜像 label：`version=v0.1.149.1`、`revision=e56621837035`。
- 容器二进制自报：`Sub2API v0.1.149 (image: v0.1.149.1, commit: e56621837035, built: 2026-07-10T04:52:49Z)`。
- Docker 生产构建包含前端 `pnpm run build` 和后端 Go release 构建，均成功。

## 部署与验证

- 发布前 active：`sub2api-blue/sub2api:v0.1.146.7`，healthy，restart 0。
- 只重建 idle `sub2api-green`，未重启 active blue、PostgreSQL、Redis 或 proxy。
- 候选 `18082`：health 200、根页 200、管理用量页 200、静态资源 200；管理 API、`/responses`、`/v1/responses` 未授权均为 401。
- 候选 65 秒窗口：green healthy、restart 0、关键日志 0。
- 代理 upstream 从 `sub2api-blue:8080` 切换到 `sub2api-green:8080`；`nginx -t` 和 reload 成功。
- 切流后 `8080`、`18081`：health/根页/管理用量页均 200；管理 API 和两个 Responses 路径未授权均为 401。
- 线上 `UsageView-BYT-VFT7.js` 返回 200，且包含新错误页/排行功能标记。
- 切流后 65 秒窗口：green/proxy 关键日志均 0；green/blue healthy，restart 0；`8080/18081/18082` health 均 200。
- PostgreSQL、Redis、proxy 的启动时间保持不变，restart 0。

## 备份与回滚

- 环境备份：`D:\sub2api-deploy\.env.bak-20260710-125658-v0.1.149.1`
- upstream 备份：`D:\sub2api-deploy\proxy\upstreams\active.conf.bak-20260710-125658-v0.1.149.1`
- 回滚目标：`sub2api-blue:8080`，镜像 `sub2api:v0.1.146.7`。
- 回滚动作：把 `active.conf` 改回 `server sub2api-blue:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`，然后复查 `8080/18081/18083`。

## 遗留风险

- 全量前端 ESLint 仍有 207 个既有 Settings prop mutation/旧测试保留名错误；本轮目标文件 scoped ESLint 为 0。
- 未执行带真实 API Key 的 GPT-5.6/Grok 上游请求，也未执行带管理登录态的浏览器点击流程；本轮覆盖编译产物、公开 bundle、路由保护、数据库查询链路测试和运行时健康。
- 未执行 Git push 或镜像 registry push；当前发布使用本机不可变镜像。
