# sub2api v0.1.152.3 发布记录

- 日期：2026-07-13
- 执行者：Devil
- Git 提交：`edacdd5e729b7357a11b0b0f15a2ed655255178f`
- 镜像：`sub2api:v0.1.152.3`
- 镜像 ID：`sha256:d05db672c8380a2eb9492c01b028fc66e9b929a647ab1133ac8156f8af017f2a`
- 当前 active：`sub2api-blue:8080`
- idle / 回滚目标：`sub2api-green:8080`，`sub2api:v0.1.152.2`

## 详细更新日志

1. 新增 `frontend/src/components/account/AccountFormTabs.vue`，为新增和编辑账号弹窗提供共享的“基本设置 / 更多设置”Tab，并支持鼠标点击和方向键切换。
2. 调整 `CreateAccountModal.vue` 与 `EditAccountModal.vue`：弹窗每次打开都回到基本设置，默认只呈现名称、平台或账号类型、Base URL、API Key、模型限制和分组等高频字段。
3. 调整 `AccountBasicInfoFields.vue` 与 `AccountAPIKeyCredentialsFields.vue`：名称和备注可按 Tab 独立渲染，API Key 账号的核心凭证与额外连接参数可分区展示，原有表单状态和提交载荷不变。
4. 备注、额外连接参数、上游登录、代理与并发、错误处理、配额调度和协议开关保留在更多设置中，不删除既有账号配置能力。
5. 更新中英文 i18n 文案，并新增/扩展 5 个组件测试文件，覆盖默认 Tab、切换行为、键盘操作和字段显隐。

## 用户可见行为

- 打开添加账号或编辑账号弹窗时，默认进入“基本设置”，高频字段集中显示，首屏不再被低频配置占满。
- 需要配置备注、代理、并发、错误策略、配额或协议开关时，可切换到“更多设置”。
- 编辑已有 OpenAI API Key 账号时，基本设置继续显示现有 Key 管理、模型限制与分组；切换 Tab 不会提交或清空表单数据。
- 侧边栏主版本仍为 `v0.1.152`，版本下拉中的镜像小版本更新为 `v0.1.152.3`。

## 构建与验证

- TDD RED：新增 Tab、名称/备注独立渲染、API Key 核心/高级字段分区及弹窗默认 Tab 测试在实现前按预期失败。
- 聚焦 Vitest：5 个文件、37 个测试全部 PASS。
- 本轮 12 个 Vue/TypeScript/i18n 文件 ESLint：PASS；`npm run typecheck`：PASS；`git diff --check`：PASS。
- 从 committed HEAD `edacdd5e729b` 使用 `git archive` 构建不可变镜像：PASS。
- OCI 标签：version=`v0.1.152.3`、revision=`edacdd5e729b`；容器二进制版本为 `Sub2API v0.1.152 (image: v0.1.152.3, commit: edacdd5e729b)`：PASS。
- Docker 多阶段前端生产构建和 Go backend build：PASS。
- 仅重建 idle blue；候选 `18083` 的 `/health`、首页、主静态资源为 200，管理版本 API、账号 API、`/responses`、`/v1/responses` 未登录为 401：PASS。
- blue 启动期出现一次已知 `pq: canceling statement due to user request`；随后独立约 66 秒观察窗中 7 次 health 均为 200，blue healthy/restart 0，新增关键日志匹配 0。
- nginx `-t` 与 reload：PASS；active 从 green 切换到 blue。
- 切流后 `8080`、`18081`、`18083` 完整冒烟：PASS；三入口 `/assets/index-DzeliCh1.js` SHA-256 均为 `1939A9E31667FD62F66B9B218ECA6DAFB06555A0ABF88A37E6E93E5C355C156D`。
- 切流后约 66 秒观察窗：三入口 health 持续 200，blue/proxy restart 0，新增关键日志匹配 0。
- Chrome 管理员登录态验收：侧边栏唯一显示 `v0.1.152`，版本下拉唯一显示镜像版本 `v0.1.152.3`；新增和编辑弹窗默认基本设置及更多设置切换均符合预期，未保存任何账号数据。
- green `v0.1.152.2` 保持 healthy/restart 0；PostgreSQL、Redis 未重启。

## 回滚

将 `D:\sub2api-deploy\proxy\upstreams\active.conf` 的 upstream 改回 `sub2api-green:8080`，执行 `docker exec sub2api-proxy nginx -t`，通过后执行 `docker exec sub2api-proxy nginx -s reload`。部署前备份为 `D:\sub2api-deploy\proxy\upstreams\active.conf.bak-v0.1.152.3-20260713-192931`，环境配置备份为 `D:\sub2api-deploy\.env.bak-v0.1.152.3-20260713-192931`。

## 遗留风险

- 候选启动期的一次 PostgreSQL 查询取消未在后续候选与切流后独立观察窗复现；当前 blue、green、PostgreSQL 和 Redis 均保持运行且 restart 0。
- 未执行真实认证上游请求，本轮线上验证覆盖镜像身份、容器健康、静态资源、未授权路由、管理员登录态页面和日志观察。
- 未执行 Git push、镜像 registry push。
