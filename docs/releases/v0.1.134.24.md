# v0.1.134.24 Release Log

- 日期：2026-06-10
- 执行者：Devil
- Git 提交：`5b3f22d50b06`（`feat(gateway): 接通 Anthropic 单账号退避配置`）
- 镜像标签：`sub2api:v0.1.134.24`
- 镜像 ID：`sha256:9a1177b4b4588cb3bd2ae746250d35338d908b2f6a90284595fb0ecf2e66b5f9`
- 构建标签：`org.opencontainers.image.version=v0.1.134.24`，`org.opencontainers.image.revision=5b3f22d50b06`
- 发布前 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.20`，Health `healthy`
- 发布前 idle：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.22`，Health `healthy`
- 发布后 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.24`，Health `healthy`
- 回滚目标：`sub2api-green` / `sub2api:v0.1.134.20`

## 更新内容

- 接通 `anthropic_single_account_backoff_seconds` 到 Anthropic `/v1/messages` failover 状态，单账号退避默认 2 秒，正数配置可覆盖。
- 接通同一退避配置到 Anthropic 平台 `/v1/chat/completions` failover 状态，避免该路径继续使用硬编码退避。
- 补齐 `openai_scheduler_cooldown_multiplier` 与 `anthropic_scheduler_cooldown_multiplier` 的默认值和正数校验，非法倍率在配置校验阶段失败。

## 构建记录

- 构建来源：`git archive HEAD`，确保使用已提交状态。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.24 --label org.opencontainers.image.version=v0.1.134.24 --label org.opencontainers.image.revision=5b3f22d50b06 --build-arg COMMIT=5b3f22d50b06 --build-arg VERSION=v0.1.134 --build-arg IMAGE_VERSION=v0.1.134.24 -`
- 构建结果：成功生成 `sub2api:v0.1.134.24`；Vite 输出存在常规 chunk size warning，无构建失败。
- 镜像确认：`docker image inspect sub2api:v0.1.134.24` 返回 `Version=v0.1.134.24`、`Revision=5b3f22d50b06`。

## 候选验证

- 部署目标：idle `sub2api-blue`，本机候选端口 `127.0.0.1:18083`。
- 部署动作：只重建 `sub2api-blue`，未重启 active `sub2api-green`、PostgreSQL 或 Redis。
- `docker-compose.blue.yml` 默认镜像同步为 `sub2api:v0.1.134.24`。
- `docker exec sub2api-blue /app/sub2api --version`：`Sub2API v0.1.134 (image: v0.1.134.24, commit: 5b3f22d50b06, built: 2026-06-10T04:31:01Z)`。
- `sub2api-blue` 60 秒稳定窗口：`Status=running`，`Health=healthy`，镜像 `sub2api:v0.1.134.24`。
- `GET http://127.0.0.1:18083/health`：200，`{"status":"ok"}`。
- `GET http://127.0.0.1:18083/`：200。
- `GET http://127.0.0.1:18083/admin/settings`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:18083/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:18083/responses` 未登录：401，确认不是 nginx 502。
- 候选日志过滤：启动窗口内出现 1 条 `[OpenAI] cleanup expired request snapshots failed err=pq: canceling statement due to user request`；切流前近 5 分钟复查未再出现 `panic|fatal|migration.*fail|checksum|pq:|bind:|address already in use|listen tcp|rebuild failed` 命中。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-green:8080` 切到 `sub2api-blue:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `GET http://127.0.0.1:8080/health`：200，`{"status":"ok"}`。
- `GET http://127.0.0.1:8080/`：200。
- `GET http://127.0.0.1:8080/admin/settings`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401，确认不是 nginx 502。
- 切流后容器状态：`sub2api-blue` Health `healthy`，`sub2api-green` Health `healthy` 且继续运行，`sub2api-proxy` running。
- 切流后 `sub2api-blue` 和 `sub2api-proxy` 关键错误日志过滤：命中 0。

## 回滚

- 当前回滚容器：`sub2api-green`，镜像 `sub2api:v0.1.134.20`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-green:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- `D:\sub2api-deploy\.env` 中 `ADMIN_PASSWORD` 为空，本轮无法执行管理端登录后的 `/api/v1/admin/system/version` 接口验证；镜像版本由 Docker label 与容器内 `/app/sub2api --version` 交叉确认。
- 候选启动窗口内的 request snapshot 清理 `pq: canceling statement due to user request` 只出现 1 次，切流前后复查未重复；后续观察同一关键词即可。
