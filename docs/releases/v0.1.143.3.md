# v0.1.143.3 Release Record

- Date: 2026-07-03
- Actor: Devil
- Build source commit: `aaf5363c6494`
- Feature commit included: `aaf5363c6494`
- Image tag: `sub2api:v0.1.143.3`
- Image ID: `sha256:23ba0766aff8973db12ace28b2194e707fa126fac4ffa50362f619a0ca242b45`
- Image label version: `v0.1.143`
- Image label revision: `aaf5363c6494`
- Before release active: blue, `sub2api:v0.1.143.2`
- Before release idle: green, `sub2api:v0.1.143.1`
- After release active: green, `sub2api:v0.1.143.3`
- Rollback target: blue, `sub2api:v0.1.143.2`

## Detailed Changelog

Compared with `v0.1.143.2`, this release ships the P2 polish and low-risk stability absorption from upstream `v0.1.142` and `v0.1.143`.

1. Aggregated JSON response Content-Type normalization
   - Modules: `backend/internal/service/openai_gateway_service.go`, `backend/internal/service/gateway_forward_as_chat_completions.go`, `backend/internal/service/gateway_forward_as_responses.go`, `backend/internal/service/response_header_filter.go`
   - Change: aggregated non-streaming JSON responses now force `application/json; charset=utf-8` after upstream headers are copied.
   - User-visible impact: clients no longer see stale upstream `text/event-stream` or custom Content-Type values on final JSON bodies, while true SSE passthrough remains unchanged when no final JSON response exists.

2. API key suffix metadata
   - Modules: `backend/internal/handler/dto/types.go`, `backend/internal/handler/dto/mappers.go`, `backend/internal/handler/dto/account_mapper_redact_test.go`, `frontend/src/types/index.ts`
   - Change: account `api_key_items` now include the last four characters as `suffix`, alongside `fingerprint` and `masked`.
   - User-visible impact: operators can identify API keys by suffix without exposing raw key material.

3. Expired 5h reset display cleanup
   - Modules: `backend/internal/service/account_usage_service.go`, `backend/internal/service/account_usage_service_test.go`
   - Change: expired Codex/setup-token 5h windows now clear `resets_at` and set utilization plus remaining seconds to zero.
   - User-visible impact: expired reset windows no longer display stale reset time or old utilization in account usage views.

## Pre-Release Verification

- Passed: `go test ./internal/handler/dto -count=1`, output `ok github.com/Wei-Shaw/sub2api/internal/handler/dto 0.042s`.
- Passed: `go test ./internal/service -run '^(TestBuildCodexUsageProgressFromExtra_ZerosExpiredWindow|TestAccountUsageService_EstimateSetupTokenUsageZerosExpiredSessionWindow|TestOpenAINonStreamingContentTypeForcesJSON|TestOpenAINonStreamingContentTypeDefault|TestHandleSSEToJSON_CompletedEventReturnsJSON|TestHandlePassthroughSSEToJSON_CompletedEventReturnsJSONContentType|TestHandleNonStreamingResponse_APIKeyFallsBackToSSEBodyWhenContentTypeIsWrong|TestHandleCCBufferedFromAnthropic_PreservesMessageStartCacheUsageAndReasoning|TestHandleResponsesBufferedStreamingResponse_PreservesMessageStartCacheUsage|TestForwardAsChatCompletions_BufferedTerminalWithoutUpstreamCloseReturns)$' -count=1`, output `ok github.com/Wei-Shaw/sub2api/internal/service 0.072s`.
- Passed: `npm run typecheck`, `vue-tsc --noEmit`.
- Passed: `git diff --cached --check`; only JSONL LF/CRLF warnings were printed.
- Passed: JSONL tail parse for `docs/feature_list.jsonl` and `docs/process_list.jsonl`.
- LIMIT: no authenticated admin API smoke was run because `ADMIN_PASSWORD` is empty; no real upstream OpenAI request was run.

## Build And Image

- Build source: committed HEAD `aaf5363c6494`.
- Dry-run command: `powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\release-bluegreen.ps1 -ImageVersion v0.1.143.3 -AllowDirty`.
- Build/deploy command: `powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\release-bluegreen.ps1 -ImageVersion v0.1.143.3 -Execute -Cutover -AllowDirty`.
- Image tag: `sub2api:v0.1.143.3`.
- Image ID: `sha256:23ba0766aff8973db12ace28b2194e707fa126fac4ffa50362f619a0ca242b45`.
- Image labels: `org.opencontainers.image.version=v0.1.143`, `org.opencontainers.image.revision=aaf5363c6494`.
- Binary check: `docker run --rm --entrypoint /app/sub2api sub2api:v0.1.143.3 --version` printed `Sub2API v0.1.143 (image: v0.1.143.3, commit: aaf5363c6494, built: 2026-07-03T11:34:00Z)`.
- Build context: release script used `git archive --format=tar HEAD` into a temporary context, so the image did not include unrelated dirty files.
- Build note: Docker build completed successfully. Vite emitted existing chunk-size, Browserslist, and dynamic import warnings, but build exited successfully.

## Candidate Deployment Verification

- Deployment target: idle green, `sub2api-green`, candidate port `127.0.0.1:18082`.
- Deployment env: `D:\sub2api-deploy\.env` now sets the green image to `sub2api:v0.1.143.3`; blue remains the rollback container on `sub2api:v0.1.143.2`.
- Safety: only idle `sub2api-green` was recreated; active `sub2api-blue`, PostgreSQL, Redis, and `sub2api-proxy` were not restarted during candidate deployment.
- GREEN: candidate `http://127.0.0.1:18082/health` returned 200.
- GREEN: candidate home page returned 200 and `/assets/index-C91TdOFj.js` returned 200.
- GREEN: candidate unauthenticated `GET /api/v1/admin/users` returned 401.
- GREEN: candidate unauthenticated `POST /responses` and `POST /v1/responses` returned 401, not 502.
- GREEN: `sub2api-green` ran image `sub2api:v0.1.143.3`, stayed `healthy`, and had `Restart=0`.
- GREEN: clean candidate log scan over a fresh 120 second window had zero critical release pattern matches.
- NOTE: an initial broader candidate log scan saw one startup cleanup line, `pq: canceling statement due to user request`; it did not recur in the fresh candidate window or the post-cutover window.
- NOTE: the release script stopped before automatic cutover because PowerShell treated a startup warning emitted on `docker logs` stderr as a native command error. Manual candidate verification passed, then cutover proceeded manually.

## Cutover And Online Verification

- Cutover file: `D:\sub2api-deploy\proxy\upstreams\active.conf` changed from `sub2api-blue:8080` to `sub2api-green:8080`.
- GREEN: `docker exec sub2api-proxy nginx -t` passed.
- GREEN: `docker exec sub2api-proxy nginx -s reload` succeeded at `2026-07-03T19:40:14+08:00`.
- GREEN: post-cutover public `http://127.0.0.1:8080/health` returned 200.
- GREEN: post-cutover local proxy `http://127.0.0.1:18081/health` returned 200.
- GREEN: candidate `http://127.0.0.1:18082/health` returned 200.
- GREEN: `8080`, `18081`, and `18082` returned 200 for the home page and `/assets/index-C91TdOFj.js`.
- GREEN: `8080`, `18081`, and `18082` returned 401 for unauthenticated `GET /api/v1/admin/users`.
- GREEN: `8080`, `18081`, and `18082` returned 401 for unauthenticated `POST /responses` and `POST /v1/responses`.
- GREEN: after a 65 second post-cutover window, all three health endpoints still returned 200.
- GREEN: `sub2api-green` stayed `healthy Status=running Restart=0`; `sub2api-blue` stayed `healthy Status=running Restart=0`.
- GREEN: post-cutover app and proxy log scans over the last 120 seconds had zero critical release pattern matches.
- Current state: active green `sub2api:v0.1.143.3`; rollback blue `sub2api:v0.1.143.2`.

## Push Status

- BLOCKED: `git push -u origin codex/merge-v0.1.134-updates` failed with GitHub 403: `Permission to Wei-Shaw/sub2api.git denied to biaoxing-github`.
- Local feature commit is present and deploy source commit is `aaf5363c6494`; remote publication requires credentials with write access to `Wei-Shaw/sub2api`.

## Rollback

If the new green active container shows health-check failures, panic/fatal logs, migration errors, bind/listen errors, or widespread gateway failures, switch the proxy back to blue:

```nginx
upstream sub2api_active {
  server sub2api-blue:8080;
  keepalive 32;
}
```

Then run:

```powershell
docker exec sub2api-proxy nginx -t
docker exec sub2api-proxy nginx -s reload
```

Rollback image: `sub2api:v0.1.143.2` on `sub2api-blue`.
