# v0.1.134.32 Release Log

- 日期：2026-06-11
- 执行者：Devil
- Git 提交：`29ae82de1755`（`fix(admin): 修复调度池人工探测失败提示崩溃`）
- 包含上一线上版本后的提交范围：`12897c096..29ae82de1755`
- 镜像标签：`sub2api:v0.1.134.32`
- 镜像 ID：`sha256:43bc1f33c73723f21d174b59e9b0a147ab64706ba4e0468fc954822f6c35c81e`
- 构建标签：`org.opencontainers.image.version=v0.1.134.32`，`org.opencontainers.image.revision=29ae82de1755`
- 发布前 active：`sub2api-green:8080`，镜像 `sub2api:v0.1.134.31`，Health `healthy`
- 发布前 idle：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.30`，Health `healthy`
- 发布后 active：`sub2api-blue:8080`，镜像 `sub2api:v0.1.134.32`，Health `healthy`
- 回滚目标：`sub2api-green` / `sub2api:v0.1.134.31`

## 更新内容

- 修复账号调度池人工探测失败返回缺少 `result` 时，前端读取 `result.message` 导致页面抛出 `Cannot read properties of undefined (reading 'message')` 的问题。
- `manualProbeAccount` 前端类型将 `result` 标记为可选，与后端 `ManualProbeResponse.result,omitempty` 保持一致。
- 调度池失败提示优先展示 `result.message` / `result.error`，缺失时回退到通用人工探测失败文案。

## 构建记录

- 构建来源：`git archive HEAD`，确保使用已提交状态。
- 构建命令：`docker build --pull=false -t sub2api:v0.1.134.32 --label org.opencontainers.image.version=v0.1.134.32 --label org.opencontainers.image.revision=29ae82de1755 --build-arg COMMIT=29ae82de1755 --build-arg VERSION=v0.1.134 --build-arg IMAGE_VERSION=v0.1.134.32 -`
- 构建结果：成功生成 `sub2api:v0.1.134.32`；前端生产构建与后端 release build 均通过。
- 构建观察：Vite 输出既有 dynamic import / chunk size / Browserslist warning，无构建失败。
- 镜像确认：`docker image inspect sub2api:v0.1.134.32` 返回 `Version=v0.1.134.32`、`Revision=29ae82de1755`、镜像 ID `sha256:43bc1f33c73723f21d174b59e9b0a147ab64706ba4e0468fc954822f6c35c81e`。

## 代码验证

- RED：`npm run test:run -- src/views/admin/__tests__/AccountSchedulingPoolView.spec.ts -t "omits result"` 先复现 `Cannot read properties of undefined (reading 'message')`。
- GREEN：`npm run test:run -- src/views/admin/__tests__/AccountSchedulingPoolView.spec.ts -t "omits result"` 通过，1 项回归测试通过。
- GREEN：`npm run test:run -- src/views/admin/__tests__/AccountSchedulingPoolView.spec.ts` 通过，1 file / 7 tests passed。
- GREEN：`npm run typecheck` 通过。
- GREEN：`git diff --check` 通过。

## 候选验证

- 部署目标：idle `sub2api-blue`，本机候选端口 `127.0.0.1:18083`。
- 部署动作：只重建 `sub2api-blue`，未重启 active `sub2api-green`、PostgreSQL 或 Redis。
- `docker-compose.blue.yml` 默认镜像同步为 `sub2api:v0.1.134.32`。
- `docker exec sub2api-blue /app/sub2api --version`：`Sub2API v0.1.134 (image: v0.1.134.32, commit: 29ae82de1755, built: 2026-06-11T15:49:47Z)`。
- `sub2api-blue` 候选窗口：`Status=running`，`Health=healthy` 持续超过 60 秒，镜像 `sub2api:v0.1.134.32`。
- `GET http://127.0.0.1:18083/health`：200。
- `GET http://127.0.0.1:18083/`：200。
- `GET http://127.0.0.1:18083/admin/settings`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:18083/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:18083/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:18083/v1/messages` 未登录：401。
- 候选日志过滤：启动窗口出现 1 条 `[OpenAI] cleanup expired request snapshots failed err=pq: canceling statement due to user request`；最近 1 分钟复查关键错误过滤命中 0。

## 切流验证

- 代理 upstream：`D:\sub2api-deploy\proxy\upstreams\active.conf` 从 `sub2api-green:8080` 切到 `sub2api-blue:8080`。
- `docker exec sub2api-proxy nginx -t`：success。
- `docker exec sub2api-proxy nginx -s reload`：success。
- `GET http://127.0.0.1:8080/health`：200。
- `GET http://127.0.0.1:8080/`：200。
- `GET http://127.0.0.1:8080/admin/settings`：200。
- 管理前端静态 JS/CSS 资源：6/6 返回 200。
- `GET http://127.0.0.1:8080/api/v1/admin/accounts` 未登录：401。
- `POST http://127.0.0.1:8080/responses` 未登录：401，确认不是 nginx 502。
- `POST http://127.0.0.1:8080/v1/messages` 未登录：401。
- 切流后容器状态：`sub2api-blue` Health `healthy`，`sub2api-green` Health `healthy` 且继续运行，`sub2api-proxy` running。
- 切流后 `sub2api-blue` 与 `sub2api-proxy` 关键错误日志过滤：命中 0。

## 回滚

- 当前回滚容器：`sub2api-green`，镜像 `sub2api:v0.1.134.31`，Health `healthy`，仍保持运行。
- 回滚方式：把 `D:\sub2api-deploy\proxy\upstreams\active.conf` 改回 `server sub2api-green:8080;`，执行 `docker exec sub2api-proxy nginx -t` 和 `docker exec sub2api-proxy nginx -s reload`。
- 本次未重启 PostgreSQL 和 Redis。

## 遗留风险

- `D:\sub2api-deploy\.env` 中 `ADMIN_PASSWORD` 为空，本轮无法执行管理端登录后的点击复测；已用前端回归测试覆盖缺失 `result` 的返回体，并用入口静态资源冒烟确认新前端资源上线。
- request snapshot 清理任务在候选启动窗口出现 1 条 `pq: canceling statement due to user request`，最近 1 分钟与切流后窗口无重复；该观察项延续既有启动清理现象，继续留意即可。
