# v0.1.152 Upstream Absorption Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将上游 v0.1.152 的功能和缺陷修复融合到本地 v0.1.151.2 定制代码中，并以本地自动化测试证明兼容性。

**Architecture:** 使用 `v0.1.151..v0.1.152` 净补丁作为完整输入，在独立分支进行三方应用；冲突按功能域解决，保留本地额外行为。先验证基线，再逐域运行上游新增测试，最后执行跨域回归。

**Tech Stack:** Go 1.26、Gin、Ent、PostgreSQL migration、Vue 3、TypeScript、Pinia、Vitest、pnpm。

---

### Task 1: 建立基线和融合输入

**Files:**
- Read: `backend/go.mod`
- Read: `frontend/package.json`
- Generate temporarily: `.codex/v0.1.152.patch`

- [ ] 运行 `go test ./internal/pkg/apicompat ./internal/handler ./internal/service -count=1`，记录基线失败。
- [ ] 运行 `corepack pnpm test:run` 和 `corepack pnpm typecheck`，记录前端基线。
- [ ] 用 `git diff --binary v0.1.151 v0.1.152` 生成净补丁，并执行 `git apply --3way --index`。
- [ ] 列出全部冲突文件，按后续任务分组解决。

### Task 2: 融合 Codex 工具和 compact 修复

**Files:**
- Modify: `backend/internal/pkg/apicompat/chatcompletions_responses_bridge.go`
- Modify: `backend/internal/pkg/apicompat/responses_to_anthropic.go`
- Modify: `backend/internal/service/openai_codex_transform.go`
- Modify: `backend/internal/service/openai_compact_sse_keepalive.go`
- Modify: `backend/internal/service/openai_compact_body_signal.go`
- Test: `backend/internal/pkg/apicompat/chatcompletions_responses_bridge_custom_tools_test.go`
- Test: `backend/internal/service/openai_codex_message_item_id_test.go`
- Test: `backend/internal/service/openai_compact_sse_keepalive_test.go`

- [ ] 先运行新增聚焦测试，确认本地基线缺少上游行为或应用后出现冲突失败。
- [ ] 解决 custom/tool_search、namespace、cache creation token、续链 ID 和 keepalive writer 冲突。
- [ ] 运行 `go test ./internal/pkg/apicompat ./internal/service -run 'Custom|ToolSearch|Namespace|CacheCreation|MessageItem|Compact|Keepalive' -count=1`。

### Task 3: 融合 alpha/search、计费和分组 schema

**Files:**
- Create: `backend/internal/handler/openai_alpha_search.go`
- Create: `backend/internal/service/openai_alpha_search.go`
- Create: `backend/migrations/174_group_web_search_price_per_call.sql`
- Modify: `backend/ent/schema/group.go`
- Modify: `backend/internal/service/billing_service.go`
- Modify: `frontend/src/views/admin/GroupsView.vue`
- Modify: `frontend/src/types/index.ts`
- Test: `backend/internal/service/openai_alpha_search_test.go`
- Test: `backend/internal/service/openai_alpha_search_billing_test.go`

- [ ] 融合路由、服务、价格字段、Ent 生成物和 migration。
- [ ] 保证按次价格只在成功且实际发生搜索调用时计费，非 alpha/search 请求不受影响。
- [ ] 运行 `go test ./internal/handler ./internal/service ./internal/repository ./internal/server/routes -run 'AlphaSearch|WebSearchPrice|AvailableGroups' -count=1`。
- [ ] 运行 GroupsView 相关 Vitest 和 TypeScript typecheck。

### Task 4: 融合 Grok 路由、缓存和账号能力

**Files:**
- Modify: `backend/internal/service/openai_gateway_grok.go`
- Create: `backend/internal/service/openai_gateway_grok_cache.go`
- Create: `backend/internal/service/openai_gateway_grok_chat_bridge.go`
- Modify: `backend/internal/service/grok_quota_service.go`
- Modify: `frontend/src/components/account/CreateAccountModal.vue`
- Modify: `frontend/src/components/account/UsageProgressBar.vue`
- Test: `backend/internal/service/openai_gateway_grok_test.go`
- Test: `backend/internal/service/openai_gateway_grok_cache_test.go`
- Test: `backend/internal/service/openai_gateway_grok_chat_bridge_test.go`

- [ ] 解决 OAuth/API Key、CLI proxy、OpenCode adapter、prompt cache 和 reasoning 参数冲突。
- [ ] 保留本地随机探测提示词，并更新受影响测试断言。
- [ ] 运行 `go test ./internal/service -run 'Grok|PromptCache|Quota' -count=1`。
- [ ] 运行 Grok 账号组件和 composable 的 Vitest。

### Task 5: 融合其余稳定性修复和前端 Fast/Flex

**Files:**
- Modify: `backend/internal/handler/ops_error_logger.go`
- Modify: `backend/internal/handler/openai_gateway_handler.go`
- Modify: `backend/internal/service/openai_ws_pool.go`
- Create: `frontend/src/views/admin/settings/OpenAIFastPolicyUserSelector.vue`
- Modify: `frontend/src/views/admin/SettingsView.vue`
- Test: `backend/internal/handler/ops_capture_writer_nil_test.go`
- Test: `frontend/src/views/admin/settings/__tests__/OpenAIFastPolicyUserSelector.spec.ts`

- [ ] 融合 writer nil 守卫、WebSocket 和 endpoint 稳定性修复。
- [ ] 接入 Fast/Flex 用户搜索选择器及中英文文案。
- [ ] 运行对应 Go 聚焦测试、组件 Vitest 和 typecheck。

### Task 6: 版本、格式化和全量验证

**Files:**
- Modify: `backend/cmd/server/VERSION`
- Append: `docs/feature_list.jsonl`
- Append: `docs/process_list.jsonl`
- Append: `operations-log.md`
- Append: `verification.md`

- [ ] 将主版本写为 `v0.1.152`，验证版本接口和 VersionBadge 单 `v` 规则测试。
- [ ] 对变更 Go 文件执行 `gofmt`，运行 `git diff --check`。
- [ ] 运行相关后端 package 测试和 `go test ./cmd/server -count=1`。
- [ ] 运行 `corepack pnpm test:run`、`corepack pnpm typecheck` 和范围 ESLint。
- [ ] 追加两份 JSONL 会话流水及测试证据，检查未混入原工作树内容。
- [ ] 按功能范围暂存并执行 `git diff --cached --check`，生成中文融合提交。

