# v0.1.149 吸收完成清单

- 日期：2026-07-10
- 执行者：Devil
- 上游标签：`v0.1.149`
- 标签提交：`dd1a116f4879992f04bb6ddbc77069bd503c3f4a`
- 比较范围：`v0.1.147..v0.1.149`
- 增量规模：72 files changed，3467 insertions，195 deletions
- 本地服务版本：`v0.1.149`
- 完成边界：代码吸收与本地验证已完成；提交、构建和部署结果见 `docs/releases/v0.1.149.1.md`

## 版本事实

- 上游没有发布 `v0.1.148` tag，当前 tag 序列由 `v0.1.147` 直接进入 `v0.1.149`。
- 当前本地分支仍保留 v0.1.146 定制结构和大量未提交吸收改动；上游 v0.1.147 已拆分多个大型 service/repository 文件。
- 因此不建议直接 cherry-pick v0.1.149 提交，应按行为和测试手工移植到当前单体文件。

## P0

| 编号 | 上游改动 | 当前状态 | 吸收建议 |
| --- | --- | --- | --- |
| 149-P0-01 | Codex compact body-signal 请求原始 `stream:true` 时，把上游 unary compact JSON 合成为 Responses SSE，补齐 `response.output_item.done` 和 `response.completed`，避免客户端无限重连并重复消耗 compact 配额 | 已完成 | 已移植到当前单体网关结构；路径型 compact 与非 2xx JSON 语义保持不变 |
| 149-P0-02 | `response.failed` 流内错误使用归一化 body 和语义状态码匹配错误透传规则；覆盖 Responses、passthrough、Chat Completions bridge、CC/Messages；按 `account.Platform` 匹配 openai/grok，并补记 Ops 上游错误事件 | 已完成 | 已统一透传、Ops 记录与失败 usage 去重计费语义 |

## P1

| 编号 | 上游改动 | 当前状态 | 吸收建议 |
| --- | --- | --- | --- |
| 149-P1-01 | Grok 图像/OAuth/用量修复：识别 `grok-imagine*` 图像模型、移除上游不接受的 `size`、保留原请求计费信息、OAuth 错误统一解析、展示用户侧费用 | 已完成 | JSON 与 multipart 转发都移除 `size`，原尺寸继续用于计费；账号页展示本地当日账号/用户费用 |
| 149-P1-02 | Grok quota probe 固定使用稳定探测模型，不再受账号模型映射影响；返回实际探测模型；新增 `composer-2.5` alias | 已完成适用部分 | 已加入被动额度快照、实际观察来源与 `composer-2.5` alias；不新增独立主动探测服务 |
| 149-P1-03 | 管理员创建/编辑用户时可设置 `user/admin` 角色；非法值校验；最后一个管理员不可降级；角色变更记录审计日志 | 已完成 | 创建/编辑 DTO、前端表单、角色校验、最后管理员保护、审计与认证缓存失效已闭环 |
| 149-P1-04 | 用量页新增用户 Token 排行，支持输入/输出/缓存/总 Token、请求数、费用、Top N、排序和点击下钻 | 已完成 | 复用用户 breakdown 接口并加入 SQL 排序白名单；排行点击用户可回到用量页并回显筛选 |
| 149-P1-05 | 用量页改为用量/错误/排行三个 tab；首 Token 和总耗时合并成延迟健康列；长时长显示为分钟/小时 | 已完成 | 错误页和排行页每次进入读取最新数据；排行首次进入懒挂载；错误筛选真实透传到 Ops SQL |

## P2 / 暂缓

| 编号 | 上游改动 | 当前状态 | 决策 |
| --- | --- | --- | --- |
| 149-P2-01 | 版本徽章列出最近 3 个正式版本，并支持下载历史 release 二进制在线回退 | 本项目生产采用不可变 Docker 镜像和 blue/green proxy 切流，回滚目标由 active/idle 容器管理 | 不直接吸收在线二进制回退；可只借鉴“显示历史版本和手动回退指引”，实际回滚仍走现有 blue/green 流程 |
| 149-P2-02 | 用量页布局、卡片和 flat 模式重构 | 当前页面有本地定制和较多未提交前端改动 | 与 Token 排行/延迟列一起单独做 UI 回归，不和网关 P0 混合 |
| 149-SKIP-01 | sponsors、README、VERSION、CI/Go 工具链同步 | 当前版本线、构建镜像和 CI 策略均为本地定制 | 不吸收，除非单独升级工具链或发布主版本 |

## 实施结果

1. `149-P0-01` compact JSON -> SSE bridge：完成。
2. `149-P0-02` response.failed 语义错误透传和 Ops 对齐：完成。
3. `149-P1-01` Grok Imagine、OAuth、转发与费用展示：完成。
4. `149-P1-03` 用户角色管理完整闭环：完成。
5. `149-P1-04` Token 排行与 `149-P1-05` 三页签/延迟健康：完成。
6. `149-P2-01` 在线二进制回退：明确不吸收，继续使用不可变镜像蓝绿回滚。

## P0 验收标准

- compact body-signal + `stream:true` 返回合法 Responses SSE，包含每个 output item 的 `response.output_item.done` 和最终 `response.completed`。
- compact path-based unary 请求保持 JSON，不被错误转换。
- compact 非 2xx 错误保持原 HTTP 状态和 JSON 错误体。
- `response.failed` 的关键词规则和错误码规则在 Responses、passthrough、Chat Completions、Messages 四类路径都能命中。
- Grok 账号按 `grok` 平台规则匹配，不误用 OpenAI 平台规则。
- 命中透传规则后仍记录 Ops 上游错误事件。
- Go 聚焦测试、`cmd/server` 编译切片、前端类型检查和 scoped diff check 通过。

## 数据与表用途

- 本轮没有新增 migration、schema、SQL 或数据表，也不迁移历史数据。
- `usage_logs` 及既有 usage 聚合继续提供输入、输出、缓存、总 Token、请求数、费用和失败实际用量，是排行、余额与计费的事实来源。
- `ops_error_logs` 继续保存请求错误和上游错误；错误页签直接查询该表，普通错误列表保留客户端可见状态守卫，上游专用列表才包含恢复态记录。
- `users`、`api_keys`、`accounts`、`groups` 继续提供排行/错误筛选维度和用户角色；角色变更沿用既有审计与认证缓存链路。
- Grok 配额使用账号既有状态字段保存被动观察快照，本地当日费用仍从既有 usage 数据按账号和用户口径聚合。
